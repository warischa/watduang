# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-09-06#3
done: 2 artifacts shipped and live. `0ea7a3a..95642ef` (4 commits, CI 34019752003) · `b89f8c6` (CI 34025725068), both with the deploy step confirmed run, not skipped. What git cannot show: **`08cc3e5` shipped a false number and a false reason, and REFUTE caught both before the push** — the freeze note called บางกอกดริฟต์ "+17,469 B", which is the delta against the BASELINE, not against the tree the route landed on; a worktree built at `0ea7a3a` measures the pre-route total 1,776 B ABOVE the pin, so the route's own cost is 15,693 · the note's reason ("re-pinning a total that is not red is how gh#206's drift disappeared") is impossible, because the one-bomb re-baseline in the same file had already folded that drift into the constant · **my own fix then repeated gh#206's failure in miniature** — wrote "nothing is left over" while the pin absorbed 1,776 unattributed bytes; a second REFUTE attributed them to gh#215's WebGL fix (`87e2f81`), since one-bomb's play entry chunk grew by exactly that and `main.ts` is the only bundled file changed in `16e425f..0ea7a3a` · **gh#181's acceptance criterion was ruled invalid on 2026-09-04, before this session opened** — the 0.62 was a scope selector, never a threshold, and `grep '0\.62'` on the probe returns 0 hits; direction is HIGHER-better, `wire-snip-panic` reads 0.942 · `unitSpanFrac` is shape-dependent, not quality-dependent: `cursed-number` reads 0.978 with NO desktop styling because its content is plain block divs, and the probe's `deepest()` collapses every single-child wrapper so a paragraph's own box can never move the number — ADR-0056 applies, the set is owned by the extractor and the off-repo mockup, not by the `overrides.css` layer the ticket permits · **the escaping audit is per-FILE, not per-hole** — one escaped hole green-lights every raw hole beside it; reproduced red@all-raw / green@all-escaped / green@mixed against the real shipped `auditFile`, with a runtime control injecting a genuine anchor element through the raw hole → filed gh#217 · gh#206 closed immaterial on measurement: 16 CI runs failed inside the drift window and ZERO failed on the bundle-freeze step · 3 of 15 adopted plan rows were already settled the other way before the session started.
dec: owner 2026-09-06, all recorded on their tickets — deploy pre-auth, green means live · tracker pre-auth for the named set · re-pin `BASELINE_TOTAL_BYTES` to 482127 · gh#181 the per-route verdict is delegated to an agent against `docs/agents/desktop-sizing-decisions.md`, and NO numeric threshold returns · gh#192 (g) edit 3 — `EXEMPT_META` stops blanking `description` spans, `title` keeps its exemption, the fortune block's `seo.description` stays a carve-out · gh#214 reaffirmed after being shown the no-geometric-excuse objection, no width minimum. Mine: split the (g) ruling from its implementation, since the ruling was the thing at risk of being lost · reverted the whole `cannon-flag` change rather than ship what the file itself records as not expressible on that route · restated REFUTE's own false evidence rather than repeat it, after checking the range it called docs-only.
next: [ ] **gh#192 (g) edit 3 — implement the ruling recorded this session.** `EXEMPT_META` stops blanking `description`; `title` unchanged; the fortune category block keeps its wording. Verify: the gate reds on a player-count range planted in the home page description prop, and stays green on the fortune carve-out
- [ ] gh#181 — seven routes carry no verdict and `cannon-flag` has one reverted attempt; eight screenshots and the reverted patch are in project-state. Verify: a verdict per route recorded on the ticket
- [ ] gh#192 (b) product-facts strip copy (c) palette (d) nav label and panel order (e) hero mascot. **The list is in the issue BODY.** (c) is not a `tokens.css` edit alone. Verify: each answered on the ticket
- [ ] gh#217 — decide whether the audit checks every hole, or an ADR records the per-file ceiling as accepted. Verify: the ticket says which, deliberately
- [ ] gh#202 the font binary — face and licence, reserved to the owner. Verify: a referenced uncompressed face in `dist/` and the coverage gate green
- [ ] gh#213 เข้งับ — id, Thai title, หมวด, game name all still the owner's; the lift is HAND-AUTHOR. Verify: a route directory exists and the porting recipe's gates pass
- [ ] gh#184 → gh#210 — the reach half of box 3 is uncalibrated and the ticket routes it to gh#210 as owner-scoped. Verify: gh#210 says what it wants
- [ ] owner standing: gh#9 domain still NXDOMAIN, blocking gh#160 gh#29 gh#12 gh#19 · the fortune family gh#97-gh#104 · gh#112 gh#109 gh#106 gh#165
inflight: measured during this save — tree clean · `gh pr list --state open` 0 · unpushed 0 · last CI 34025725068 SUCCESS with deploy confirmed · open issues 30 · 13 play routes on disk · 1 worktree kept deliberately, `hopeful-fermi-74ec5e`, the OWNER's own. This save is docs-only and does NOT push (ADR-0049).
spent: queue 15→8 · batches 8 waves, 2 pushes, 12 dispatches, 4 adversary rounds (1 fork, 1 fork-return, 2 REFUTE), 0 escalations, 0 second attempts
errors: adopted three plan rows without reading the tickets behind them, all three already settled · fed the fork a premise taken from the handoff and never derived, which it refuted · answered a runtime question with a text grep for "three" and matched the English word in prose · built a chunk diff on basenames that collide eleven ways, so it reported all 48 as new · wrote "nothing is left over" while the pin absorbed 1,776 unattributed bytes · spent four browser runs chasing a criterion that had been ruled invalid two days earlier · scored an ADR prediction with a number measuring a different property — counted values hard-coded in `src/` when the prediction is about values ABSENT from `design/`, and the real comparison found one, not 649.

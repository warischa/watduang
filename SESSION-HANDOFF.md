# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-09-23#1
done: `e8bfe16` `522a05b` `a2d875f` `fa63ded` local, unpushed — gh#103's agent half: thirty UNREVIEWED cards, the forty deck portraits `IMG_01_011`-`IMG_01_050` owner-accepted and committed, registry and assets doc updated; gh#103 comments `5788443062` `5788527430` `5789095055` `5789174130` posted, each read back identical bar GitHub's trailing newline. What git cannot show: **the qwen-image skill was edited OUTSIDE git** — `~/claude/skills/qwen-image`, which `~/.claude/skills/qwen-image` symlinks to, is not a repository; `SKILL.md`, `scripts/qwen_image.py` and a new `scripts/chroma_key.sh` changed; the only revert is `~/claude/skills/qwen-image-backup-2026-09-23.tar.gz`; selftest green and red on both mutants tried · **the skill reproduces the registry**: composed text byte-identical for all thirty first-pass entries, flattened refs pixel-identical for all six ref files, and every later render went through its own registry mode · **qwen-image-3.0-pro is near-deterministic** — same seed, prompt and refs gave 0 differing px on 2 of 3 repeats, 0.25% on the third · **references leak more than style** — expression, a scar, freckles, gender, an outfit and a raised fist each reappeared on other cards, a "do not copy" line stopped none of them, swapping the trio fixed every case · **gh#102's ten stay on disk** as its record and as the thirty's references; the deck's M1-F5 are the new `IMG_01_041`-`050` · spend: 1 wan + 57 qwen renders, about USD 2.4 — the count is reconstructed from this session's render commands, not logged, and the price is the skill's table, not a bill · **the previous `H: gh#101` verify could never pass**: it grepped for `UNREVIEWED`, which the drafts also use as prose; the banner line itself is what now gets counted.
dec: gh#103 started before gh#101 sign-off, cards AND portraits (owner 2026-09-23, popup — recorded in comment `5788443062`) | render through the qwen-image skill, probe first (owner, same round) — the wan probe failed, qwen on a keyed green ground passed on probe 4 | re-render gh#102's ten into the new pipeline rather than accept the join (owner, popup — reverses the 2026-09-22 decline; recorded in comment `5789095055`) | accept all forty deck portraits with the kept defects named in the option (owner, popup — recorded in comment `5789174130`) | gh#103 stays OPEN (owner, popup) | new ids `041`-`050` rather than overwriting `001`-`010`, so the thirty stay reproducible and gh#102's comments stay true | no gh#102 comment about the supersession — not asked.
next: [ ] gh#103 boxes 4 and 6 — move the accepted portraits into the publish surface at the size the page loads, and state what one reader downloads; no route for this game exists under `src/pages/`, per gh#103's comment of 2026-09-23. Verify: each deck file referenced by a card on the built page, and the per-reader byte count in the closing comment · Blocked · ask: unblocks when the gh#101 page exists — nothing to decide before then
next: [ ] gh#101 — build the page from the signed-off drafts, per the ticket body's "What to build"; re-render any portrait whose card's gender, age, build, occupation, manner or mark the owner edit moved, with the command in `images/IMAGES.md`'s gh#103 section. Verify: the ticket's seven boxes, each checked the way its own wording says · Blocked · ask: owner edit — consumes H: gh#101
next: [ ] gh#160 — Cloudflare Web Analytics, chosen and not wired; its other blocker #159 is confirmed dead. Verify: per ticket, once the domain resolves · Blocked · ask: unblocks when H: gh#9 lands — nothing to decide before then
next: [ ] gh#29 — ad slots need the domain AND an owner AdSense account, site review and publisher ID; two owner acts, not one, per its own body. Verify: publisher ID recorded and ad code live · Blocked · ask: unblocks only after gh#9 AND the owner's AdSense account — do not brief it as domain-only
next: [ ] gh#19 — month-6 organic-clicks gate; needs the domain, Search Console connected to it, and six months of traffic. Verify: organic clicks recorded at month 6 · Blocked · ask: nothing to decide until the domain and the traffic window both exist
next: [ ] H: gh#101 owner edits the four drafts in the tree — `docs/copy/nuea-khu.md`, `docs/copy/nuea-khu/cards.md`, `docs/copy/nuea-khu/cards-m6-m20.md`, `docs/copy/nuea-khu/cards-f6-f20.md`; the last two are gh#103's thirty and flag two departures (ages to 54, four non-Thai nationalities). Verify: `grep -c '⚠ UNREVIEWED'` reads 0 on all four, measured 1 each at this save
next: [ ] H: gh#103 box 3 — the owner judges, on the forty, whether a reviewer can tell which batch a portrait came from; the renders are accepted but this box was never asked, per comment `5789174130`. Verify: the answer recorded on gh#103
next: [ ] H: gh#9 owner registers `watduang.com` — owner-only and financial, never an agent action; re-run whois immediately before paying, privacy on, auto-renew on; it fully unblocks gh#160 only, and is one of two owner acts gh#29 needs. Verify: `dig +short watduang.com A` resolves
next: [ ] H: gh#141 walk all 20 boxes on a real phone; gh#204 boxes 7 and 9, gh#205 box 1 and gh#219 box 1 need the same device and the same pass. gh#204's two are ONE measurement — box 9's residual is box 7's frame rate, and its concept note still reads `Measured: (not yet)`. All three tickets carry NO agent slice, per their own 2026-09-21 triage comments — do not brief any of them as code work. Verify: every box noted with its device
next: [ ] H: gh#104 item 1 — the ดูดวง หมวด deck rewrite, 23 prompts and 5 predictions; owner-reserved Thai copy under the 2026-09-10 ruling, overlapping gh#97 gh#98 which already own เซียมซี's deck. Verify: the deck no longer promises a group
next: [ ] H: gh#109 is owner-only end to end — boxes 1-2 are Thai copy and boxes 3-5 are already satisfied per its 2026-08-28 comment; there is NO agent slice. Verify: nothing; do not brief it as mechanical work
next: [ ] H: gh#12 needs an owner Google Ads account, NOT the domain — its body says `Blocked by: —`. Verify: Keyword Planner volumes recorded under `docs/research/`, never `.scratch/`
inflight: measured during this save — `gh pr list --state open` empty; one worktree, the main checkout; no background tasks; `origin/main` = `7566fe9`, and `e3864ca` `5224b46` `5293588` `4d4866c` `e8bfe16` `522a05b` `a2d875f` `fa63ded` plus this save stack locally, riding the next src push per ADR-0049 — `fa63ded` carries 29,098,190 bytes of PNGs under `images/`, outside `public/`, so it changes no `dist/`. Outside git: the qwen-image skill edits and their backup tarball, named in `done:`.
spent: queue 4→5 · batches 1 — gh#103 moved from waiting-on-edit to its two residual boxes; the gh#101 build row is new, read off the ticket body, not a new decision. 5 is the open non-`H:` count, enumerated: gh#103 boxes 4 and 6, gh#101, gh#160, gh#29, gh#19.
errors: **(1) a paid step ran on an edit that had failed** — a registry edit hit its own assert and wrote nothing, the re-render after it was chained with `;`, and three identical renders were bought (~USD 0.12); root cause: the spend was not gated on the edit that licensed it · **(2) two pass bars that could not fail, one posted to the ticket** — "alpha mean < 1" passed a painted checkerboard at 0.998, and a green-halo count read 0 by construction once the despill capped green; corrected in comment `5789095055` · **(3) numbers written before they were measured, three instances, all caught before commit** — "eight" serious female cards (nine), "35 renders" (38), "proven on a 30-portrait set" before the batch finished · (4) sent the owner a deck sheet built before the last five fixes; resent.

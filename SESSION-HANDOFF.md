# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-09-06#5
done: NOTHING COMMITTED but this save; NOTHING pushed, posted or closed. Tree carries +148/-17 across 5 files (`name-escaping.test.mjs`, `play-screen-fit-probe`, `control-floor-probe`, `strip-chip-visibility-probe`, `run-workflow-gates.sh`), review-complete but **the suite NEVER RAN — user paused on quota inside leg 1**; its partial log has no captured exit code, so it is not a verdict. What git cannot show: **3 of the resume plan's rows were dead before dispatch** — gh#217 settled by ADR-0061 whose Decision says the behavioural guard "was not made a precondition for closing gh#217", gh#181's verdict table already delivered all 8 routes, gh#210's chip cap already shipped · **`needs-triage` means the maintainer scopes it** (`docs/agents/triage-labels.md`), so 6 queue rows are owner DECISIONS, not agent work — gh#214's row 1 reserves itself in so many words ("whatever the owner rules") · **gh#202's font cannot reach the play routes, by two DIFFERENT causes** — Sarabun is registered site-wide but its load status never reaches loaded; `pinocchio-luck` hardcodes a system stack, while `wire-snip-panic` DOES use `var(--font-sans)` and redefines that token to a system stack in its own `:root` (shadowing, not absence — the more general defect) · **`pinocchio-luck`'s pinned sideways rows are a distribution, not a value** — {35,0,0} and {25,39,0} over 3 runs, the pin never reproduced; cause inferred not proven; contamination ruled out on timestamps (last read 22:09:51, `dist/` rebuilt 22:11:17) · **gh#205 row 5 is VOID for all 13 play routes** — `getStaticPaths` filters to non-playRoute games and `GameLayout` is the only ad-slot/JSON-LD renderer, so no game page exists to carry them (ADR-0050); a row rewrite, not work · `canvas-frames-probe.mjs` is wired to NOTHING, its only mention is its own header usage example, so gh#205 row 6c rests on an instrument no run executes · **croc-bite blocked TWO ways** — `three` absent 4 ways with no mechanism named anywhere, AND the extractor refuses external resources so the recipe's step 1 cannot run on that mockup · **ADR-0060's second-order arithmetic is computed off a stale base**, corrected in the ADR this session · REFUTE ran both allowed rounds, 9 findings all spot-fix: 4 of round 1's were false sentences in the disclosures I had just accepted, and round 2 found the guard I wrote to fix round 1 failed open across routes.
dec: croc-bite OUT of this artifact — 2 blockers, no code, and the fork's own cut rule forbids holding done rows behind the longest member; its brief now carries both blockers · **the re-pin needs no fresh ask** — ADR-0060 records the permission, gh#206 rules "one adjudication, not one commit … no game needs to come back and ask again", `16e425f` is the agent-executed precedent; the "an agent did not make this call" note in the freeze file is about the out-of-change drift re-pin, a different action · `three` mechanism RECOMMENDED (not ruled): exact-pinned npm dependency — no gate reads `dependencies`, and the tree-shaking annotation survives only while the library keeps its own `package.json`; owner may still read "vendored" as bytes-in-repo, which switches it. Mine: DELETED the route list from the escaping disclosure rather than add the missing fourth — naming members is the defect, a hand list rots like the stale count in that same file · guard throws PER ROUTE not over the union, because 4 routes already cap via a property a text read cannot see · pinned no winner and no count in the mascot comment, the roster owns that set · suite runs BOTH legs, since `ci.yml` never runs `npm run ci` and says so in its own comment.
next: [ ] **Run the integration suite, then the guarded batch.** Both legs, sequential, neither concurrent: `npm run ci` then a FULL `bash scripts/run-workflow-gates.sh` with no SKIP_EXPENSIVE. Verify: both exit codes captured on the line after each command and echoed as literal text
- [ ] Commit the 5 tree files as 3 commits, one per track (gh#217 disclosure · gate timing · gh#184 rows), messages via `-F <file>`. Verify: `git diff --cached --name-only` printed before each commit
- [ ] Post the 7 drafted tracker comments, close gh#181, delete branch `claude/adoring-proskuriakova-033165`. Draft text is ready in project-state, every claim re-derived. **Leave the worktree alone** — a live peer session may be its successor. Verify: one read-back per posted comment
- [ ] gh#202 — owner scopes two things: whether play routes should consume the site font tokens at all, and what replaces a pinned constant on a route whose widest element is randomised. Verify: the ticket says which
- [ ] gh#214 gh#210 gh#219 — all `needs-triage`, all owner-to-scope; root causes are recorded and drafted. Verify: each ticket carries its posted evidence comment
- [ ] gh#213 croc-bite — owner rules whether "vendored" means npm dep or bytes-in-repo, then the port runs. Verify: the ticket answers it and `src/play/croc-bite/` exists
- [ ] gh#205 — needs the builder run for rows 1/2/6c/8, a row-5 rewrite from the owner, and rows 3/4/7/9 ticked. Verify: the boxes match the tree
- [ ] gh#184 row 2 needs one local run that SAVES shots (CI keeps no artifact on green); rows 1 and 3 are owner-side. Verify: the shots exist and the ticket says so
- [ ] owner standing: gh#218 shell pill · gh#204 gh#141 real-phone frame rate · gh#192 (b) · gh#182 gh#165 gh#150 gh#112 gh#109 gh#106 · fortune family gh#97-gh#104 · gh#9 domain NXDOMAIN blocking gh#160 gh#29 gh#12 gh#19
inflight: measured during this save — tree dirty with exactly the 5 files above, deliberately NOT swept into the save commit · `gh pr list --state open` = none · unpushed on main = 1 docs commit (rides the next src push, ADR-0049) + this save · no orphaned probe ports, no orphaned build process · 1 peer session live on this checkout (`free-game-94`), so no branch surgery · worktree `hopeful-fermi-74ec5e` clean, its content proven byte-identical in main, left alone.
spent: queue 21→9 · batches 5 waves, 14 dispatches, 2 REFUTE rounds (both FAILED the diff, both correct), 1 fork, 0 pushes
errors: wrote a brief that forbade building and then ordered a command that builds, risking the numbers the same run was measuring · `cp` without `-p` destroyed the mtimes needed to clear that risk, recovered from the originals · read an exit code that was `head`'s and not `grep`'s · a name-regex swallowed a file header as a 65-character name · told REFUTE the new prose held no Thai and no count digits when it held both · logged a dead-code removal as a fix when against HEAD it was a no-op · carried 3 settled-or-blocked rows into the resume plan as buildable work.

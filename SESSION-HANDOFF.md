# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-08-29#1
done: **cannon-flag (ยิงธง) shipped as the seventh game and VERIFIED IN A REAL BROWSER, which is where its worst bug lived** — `c1d451c` ports it (14 tests, ballistic physics, raycast collision, sudden death, canvas renderer gated on reduced motion); `f0e090f` fixes what playing it at 320px exposed: `releaseCharge` was bound to WINDOW and called `preventDefault()` on every page-wide `touchend` BEFORE checking whether a charge was in progress, which suppressed the synthetic click Chrome builds from `touchend` and left BOTH angle steppers and the sound toggle dead to touch — measured 58.0° through three real taps on an enabled 44x44 control, while `.click()` moved it. Same commit raises `#cf-sound-toggle` 32px→44px (then `flex: none`, because it measured back 43px as a shrunk flex child). **power-meter's three defects closed in `0cbf38c`** — the second scoring path is deleted and the perfect window now lives IN the curve (the bar painted 9.59 while 10.00 was recorded), the burst no longer draws into a canvas detached by the last render, and per-screen cleanup is split from per-mount. Fixing the curve exposed a second copy of its shape: the auto-lock's literal `elapsed - DURATION_UP_MS >= DURATION_DOWN_MS` fired 30 ms early and recorded 0.40 for a no-tap attempt whose comment said "at exactly 0"; `CYCLE_MS` now owns the cycle end. `19cfbfa` repairs the previous entry. All three gates green at every commit: `npm run ci` 347 tests, `npx astro check` 0 errors, `npm run ci-probes` 20/20.
dec: **A fake `addEventListener() {}` stub is a hole, not a fake.** Three harness holes were closed and each is calibrated against the mutant it hid: power-meter's `matchMedia` and cannon-flag's `window` both accepted listeners and never called them, so every window-bound handler registered into a black hole — that is precisely why a 14-test suite and 20 CI probes all passed a game whose controls did nothing on a phone. `innerHTML` was a raw string with no children, so an `<a href>` inside `EMPTY_GAUGE_SVG` was invisible to the nine-phase anchor sweep — a LIVE ADR-0014 hole behind a green suite. `clientWidth`/`clientHeight` of 0 made `attachSparkCanvas` bail on its third line, so the whole burst path was unreachable from every test in the file. **Every new assertion this session was planted-and-reddened before being trusted, and one was thrown out when it failed on fixed and broken code alike** — a test that reds on both is a broken measuring tool, not a finding. `scripts/driver.mjs` gains `session.hold(x, y, ms)`; `tap()` releases immediately and cannot express a hold-to-charge control. **Three owner rulings closed 2026-08-29:** (1) for power-meter's effects, **§7 governs whether an effect EXISTS and §10 governs how it LOOKS** — all 16 effects stay, both infinite CSS loops are gone, `animation: ... infinite` is now 0 in the stylesheet, and the findings entry's count was corrected in place from three to two because it had named only two and the file had only two; (2) the Thai copy on the shell-owned screens is **reviewed and approved as shipped** — no rewrite; (3) gh#123 resolves toward the gate, so `CLAUDE.md`'s domain-term allowance now says `.md` files ONLY and code comments carry no exception, because `scripts/thai-comments.mjs` has always been the real authority and widening it with an allowlist could only weaken it.
next:
- [ ] owner: merge `feat/freeze-tap-port` to main — that merge is what publishes มือลั่น AND ยิงธง to the live site and adds them to `/c/party/`, which builds its list from the manifest and needs no code change. Live today lists only ระเบิดเวลา · สุ่มคนโดน · จับไม้สั้น; both new game URLs 404
- [ ] agent: power-meter is now unblocked on both owner questions (effects ruled, copy approved) — remaining work is mutants 2, 3, 6 and 7 from `docs/verification/power-meter-build-findings.md` are still alive, plus one this session confirmed survives — removing the reduced-motion guard from `fxFrame` alone keeps 35/35 green, because the flip already zeroes trauma and sparks and no input separates the two. Then wire it as game 8
- [ ] agent: the mechanical extractor for Thai strings and named constants out of a mockup HTML is still unbuilt; the fake-DOM half of that once-only investment is now done in both game test files and should move to the shared template
- [ ] owner: the pending FOCUS-BLOCKER queue from the archive is still untouched — Thai copy tickets, the font question, and the ad-slot chain
- [ ] agent: `ready-for-agent` backlog picked but NOT started this session — gh#132 (a stale tap on a two-tab tool page wipes the other tab's names), gh#111 (GameNav still promises a same-วง handoff and lists ดูดวง pages as เกม), gh#89 (one rule for where the 2-10 party-size claim may appear)
inflight: branch `feat/freeze-tap-port`, carrying both ports and the power-meter fixes; `origin/main` has none of it, and deploy is gated to main · 0 open PRs (checked) · re-derive position with `git status --short` and `git rev-list --count origin/main..HEAD` rather than trusting this line
spent: queue 0→8 · started as a RESCUE of an uncommitted save whose every perishable was false · **the browser pass paid for itself: three commits and 20 CI probes all green on a game whose angle buttons did nothing on a phone** · my own errors this session: wrote a self-invalidating commit count into the handoff and had to amend it out · claimed a CSS class had no rule when my grep pattern simply did not match it · misread an angle readout because the `-`/`+` buttons flanking it concatenated into the text · built a regression test that failed on fixed and broken code alike and had to throw the run out · drove three taps faster than the 400 ms arm gate and briefly read a working game as broken

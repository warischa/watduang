# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state



### S2026-09-05#3
done: 7 commits pushed `d0a3d30..9d30062`; `f3a7485` local UNPUSHED. gh#203 instrument `fd7909f` · gh#210 cap `d877681` · gh#211 merge `b5a3680` · gh#209 gate+wiring `1842f00` · gh#104 copy `83378fe` · evidence `4e5db5f` · REFUTE fixes `9d30062`. What git cannot show: **CI RED TWICE on `9d30062` and the deploy step SKIPPED, so the live site is UNCHANGED** — `play-exit` VOIDs `pinocchio-luck` burst, runner too slow to dispatch inside the 400ms arm window · **the red is MINE by machine load, not by code** — I first claimed otherwise on reachability; measured, `play-exit` ran 92/95/97s on three green runs and 112s on mine, 17s outside a 5s band, while the three walking fit legs each grew 5-10s and lanes run in parallel; cost is the composition DOM read, NOT the screenshots (~2% of a leg) · S#2's three patch files were GONE (scratch died) and the composition work survived only as uncommitted changes in worktree a023 · **exemption rows are 12-of-17 at the two phone viewports and 16-of-21 across all three; "16 of 17" mixes the two scopes and is true of neither**, and `evidence/182-375/README.md` carried it · the strip-chip probe on `d0a3d30` genuinely could not load, proved by importing the committed copy from a real file (`node -e` passes either way, it exposes builtins as globals) · `setup-badge-icon-coverage-check` read a COMMENTED-OUT assert as coverage until `9d30062`; selftest leg 8 pins it and reverting only the detector fails leg 8 alone · **`d877681`'s own message is false** — it claims the tautological control leg was replaced; `BREAK_REACH` is declared hand-run and unwired and the verdict script reds any control that sets it, retracted in `9d30062` · gh#206's DoD went into the ticket BODY not a comment, because body checkboxes are what the liveness check counts — read back 9 boxes where there were 0 · gh#203 CLOSED, and its own instrument is what killed its premise: 7 routes sit at span <=0.62 of the 1440px frame, not the 2 the handoff scoped nor the 6 the title names.
dec: owner 2026-09-05 — gh#104 copy approved as written · the frozen fortune `seo.description` UNFROZEN for two defects (calling the pages เกม, advertising the delisted love-match) · gh#182 CUT from this batch · gh#140 box 2 the animal icon stays with the SEAT so a rename never moves it, recorded on gh#152 too · push approved on the evidence line · gh#203 the narrow centred column is correct, ticket closed, `desktop-sizing-decisions` rule 3 stands. Mine: extended the owner's one-string ruling to `seo.title`/`intro`/`whenToUse` on the same object as one defect class and flagged it reversible · did NOT wire a third reach leg for gh#210 — it needs a new verdict entry and a new `EXPECTED_LEGS` shard sum, so the gap is recorded on the ticket instead of a mid-batch gate redesign · `f3a7485` is labelled NOT-A-FIX in its own message so nobody reads it as clearing the red.
next: [ ] decide the CI-red fix — gh#203 is CLOSED so its instrument has answered its question; gate the composition read OFF on CI (env flag) rather than pay a red for it, or cut lane concurrency. Verify: `play-exit` back inside the 92-97s band and the run green on an unchanged tree
- [ ] push `f3a7485` with whatever the red fix turns out to be — one ask, one deploy. Verify: CI green and the deploy step no longer skipped
- [ ] gh#210 box 3 CI half — diff a green CI artifact against the recorded local `checked=9 bad=0` and `checked=9 bad=8`. Verify: both numbers in the ticket, diffed
- [ ] the SECOND broken DoD is still unidentified — the repo names only gh#206 and the 404-line drafts file is gone. Verify: the ticket number is named from evidence or the row is dropped
- [ ] gh#182 next round — 12 rows at the two phone viewports, each either fixed or carrying an owner ruling. Verify: no row cites only `gh#182 open:`
- [ ] gh#206 re-pin is now an owner question IN the ticket, not a queued task. Verify: the owner answers question 1
- [ ] owner: gh#204 gh#205 ranking · gh#192 b-e · gh#9 domain still blocks gh#160 gh#29 gh#12 gh#19 · standing gh#165 gh#150 gh#141 gh#112 gh#109 gh#106 and the fortune family gh#97-gh#104
inflight: measured during this save — tree CLEAN · `gh pr list --state open` 0 · `origin/main` `9d30062`, 1 unpushed src commit `f3a7485` plus this save's docs commit · CI run 33964448528 RED on `9d30062` after a rerun on the identical SHA · 1 worktree left deliberately: `hopeful-fermi-74ec5e`, the OWNER's own session, its `92b9f00` merged · both agent worktrees removed after verifying by CONTENT that main held everything · probe evidence kept outside the repo at `probe-evidence-c2d4dcc4/`.
spent: queue 13 to 7 · batches 3 waves, 6 dispatches, 1 of 2 REFUTE rounds, 0 re-forks
errors: reachability checked on code but not on MACHINE LOAD, so I told the owner a CI red was not mine and had to retract it after measuring · three separate unquoted-glob failures, a trap this repo's own shell-traps doc names, one of which printed a confident "none" that was a dead command · a gate's exit code read through a pipe, so `tail`'s status was reported as the gate's · claimed gh#104's defect class closed while two fortune modules still carried the keyword, because the class query was scoped to one file · corrected a row count twice before settling that both figures were true in different scopes · pinned `PROBE_OUT_DIR` into the project-state dir and wrote 1.0GB of Chrome profiles there before pruning to 96KB.

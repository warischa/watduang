# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-09-22#3
done: `cb83df3` local, unpushed. gh#182 CLOSED with comment `5771878071`, after correction comment `5771860103`; both read back byte-identical bar the trailing newline GitHub adds. What git cannot show: **a sentence shipped in `7f95a57` was FALSE and had to be superseded in three places** — the `KNOWN_OVERFLOW` reason, the gh#182 comment built on it, and the handoff all said nobody had checked freeze-tap's primary control against the fold, when `docs/verification/evidence/182-freeze-tap-fold-2026-09-15` records n=12 on a Mac across BOTH card states with `playerReadyBtn` at 60 visible px every load and the pass-screen container closed; the true gap is narrower and is now what all three say — never measured ON THE RUNNER, and not re-measured after `6dbaf71` touched the route for the rule-reveal screen · **the worst-screen gating gap named in the 2026-09-15 close is NOT closed, only louder** — read off the probe this session: every screen is emitted to the CI log, but classification still maps each row through `worstOf` and compares that ONE screen against the two sets, so a second defect on a better screen is visible and ungated · **box 1's exception set was counted from the module, not inherited** — 18 `KNOWN_OVERFLOW` rows, 12 `owner ruling` + 6 `not a defect` + 0 unruled, against 24 `FITS_ROWS`, 42 total · **`gh issue close -F <file>` is a silent no-op** — it printed nothing, the ticket stayed OPEN, and only the state read-back caught it; `gh issue comment` accepts `-F`, `gh issue close` does not, so the comment and the close are two calls · **gh#182's comment history is 66KB** and must be read through a persisted file, not inline.
dec: gh#182 CLOSED on run `35690082586` rather than on the refuted 2026-09-15 bullet (owner 2026-09-22, popup) | `not a defect` REAFFIRMED as satisfying box 1's explicitly-ruled-on clause (owner 2026-09-22, popup — the 2026-09-15 close had counted it that way, that close was partly refuted, so the reading was put back rather than inherited; **ruling recorded in comment `5771878071`, not in any ticket body**) | the worst-screen gating gap gets NO ticket of its own (owner 2026-09-22, popup — the close comment records it as owned by nobody, deliberately) | the runner fold measurement is OWED and its approach is fixed: read `#playerReadyBtn` explicitly because that is the selector the Mac baseline read, emit report-only from inside the existing fit leg, force every draw — the probe's own largest-visible-button heuristic was considered and rejected as a different instrument | the 12px ruling itself is UNCHANGED by the correction: its basis moved from bare acceptance to consistent-with-a-Mac-measurement, never to rule-satisfied.
next: [ ] gh#181 — the eight other games sitting in a phone-width column on desktop. Open and `ready-for-human`, and absent from the previous entry's queue entirely; found by `gh issue list` this session, never read. Verify: per ticket · Blocked · ask: read its body and last comment IN FULL before briefing — nothing this session read them
next: [ ] gh#103 — the remaining thirty เนื้อคู่ cards and portraits. Style is DECIDED in S2026-09-22#1, so the only thing left blocking is the owner edit. Verify: thirty cards written and thirty portraits put to the owner · Blocked · ask: owner edit — consumes H: gh#101's output and cannot start before it
next: [ ] gh#160 — Cloudflare Web Analytics, chosen and not wired; its other blocker #159 is confirmed dead. Verify: per ticket, once the domain resolves · Blocked · ask: unblocks when H: gh#9 lands — nothing to decide before then
next: [ ] gh#29 — ad slots need the domain AND an owner AdSense account, site review and publisher ID; two owner acts, not one, per its own body. Verify: publisher ID recorded and ad code live · Blocked · ask: unblocks only after gh#9 AND the owner's AdSense account — do not brief it as domain-only
next: [ ] gh#19 — month-6 organic-clicks gate; needs the domain, Search Console connected to it, and six months of traffic. Verify: organic clicks recorded at month 6 · Blocked · ask: nothing to decide until the domain and the traffic window both exist
next: [ ] record only, no decision waits on it — `docs/agents/issue-tracker.md` still states เลขใบ+1 twice (in `## ✅ Migrated 2026-08-13 — GitHub Issues is canonical` and in `### เลขใบ vs issue number`) and the `.scratch/` keep-rule twice; deduping frees about 450B against a 12288B ceiling the file now sits 2397B under. Verify: none owed
next: [ ] measure freeze-tap's fold ON THE CI RUNNER — owner asked for it 2026-09-22 after the reason-string correction. Approach already chosen: extend `scripts/play-screen-fit-probe.mjs` to emit, REPORT-ONLY, the visible px of a per-route primary control on each measured screen, reading `#playerReadyBtn` for freeze-tap because that is the exact selector the n=12 Mac baseline read — a reading taken with the probe's own largest-visible-button heuristic was considered and REJECTED, since a different instrument cannot answer whether the Mac result holds. Emit from inside the existing fit leg rather than adding a probe leg: `EXPECTED_LEGS` in `scripts/ci-probes.sh` is a human-maintained pin and a new leg must bump it. Force every draw through the existing `DRAW_OVERRIDE` path so the TALL draw is the one measured — an unseeded fold probe re-runs the lottery and may never hit it. Scope: freeze-tap only; the sibling 320x568 rows carry 2026-09-11 Mac measurements with the same runner-gap and widening was not asked. Read `docs/agents/ci-gate-calibration.md` and `docs/agents/ci-verification.md` whole first. Verify: one CI run's log carries a visible-px figure for freeze-tap 320x568 press 1 on the forced tall draw, compared in the same comment against the Mac 60px. What the number decides: >=1 visible px upgrades the `KNOWN_OVERFLOW` reason from consistent-with to the same basis its siblings carry; 0 visible px reopens the 2026-09-22 ruling
next: [ ] H: gh#101 owner edits the draft in the tree — `docs/copy/nuea-khu.md` and `docs/copy/nuea-khu/cards.md`, both still carrying `UNREVIEWED`; the agent half is done. A field-value edit costs ten re-renders per the trigger recorded in S2026-09-18#8. Verify: neither file still carries the banner
next: [ ] H: gh#9 owner registers `watduang.com` — owner-only and financial, never an agent action; re-run whois immediately before paying, privacy on, auto-renew on; it fully unblocks gh#160 only, and is one of two owner acts gh#29 needs. Verify: `dig +short watduang.com A` resolves
next: [ ] H: gh#141 walk all 20 boxes on a real phone; gh#204 boxes 7 and 9 and gh#205 box 1 and gh#219 box 1 need the same device and the same pass. gh#204's two are ONE measurement — box 9's residual is box 7's frame rate. Verify: every box noted with its device
next: [ ] H: gh#104 item 1 — the ดูดวง หมวด deck rewrite, 23 prompts and 5 predictions; owner-reserved Thai copy under the 2026-09-10 ruling, overlapping gh#97 gh#98 which already own เซียมซี's deck. Verify: the deck no longer promises a group
next: [ ] H: gh#109 is owner-only end to end — boxes 1-2 are Thai copy and boxes 3-5 are already satisfied per its 2026-08-28 comment; there is NO agent slice. Verify: nothing; do not brief it as mechanical work
next: [ ] H: gh#12 needs an owner Google Ads account, NOT the domain — its body says `Blocked by: —`. Verify: Keyword Planner volumes recorded under `docs/research/`, never `.scratch/`
inflight: measured during this save — `git status --short` carried this save's own files plus untracked `.scratch/free-game/drafts/`, LEFT UNTOUCHED and holding only comments already posted and read back; `gh pr list --state open` empty; one worktree, the main checkout; no background tasks running. `origin/main` = `7f95a57`; `cb83df3` and this save stack locally and ride the next src push.
spent: queue 1→5 · batches 1 — the resume carried ONE row (decide gh#182's second close) and it closed; 5 is the open non-`H:` count now, the rise being the runner-fold row this session created plus the four owner-blocked rows the resume did not list.
errors: **one shipped, one caught.** (1) **Asserted an absence without searching where it would live** — wrote that freeze-tap's fold had never been measured, into a reason string that was committed AND pushed in `7f95a57`, into a GitHub comment, and into the handoff; the measurement was sitting in `docs/verification/evidence/182-freeze-tap-fold-2026-09-15`, a directory the sessions archive names and the probe's own neighbouring reasons cite. Root cause: a negative was treated as free, when it is the claim most in need of an enumerated search — the evidence tree is the set, and it was never grepped. Corrected in `cb83df3` and superseded, never rewritten, in comment `5771860103`. (2) Caught, not shipped: `gh issue close -F` no-op'd and the ticket read OPEN afterwards; the read-back is what surfaced it, which is the reason the read-back exists.

# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-08-19#1

done: **gh#44 CLOSED · gh#45 FILED then FIXED and proven · gh#46 + gh#47 FILED · 4 commits pushed, every run green.** `scripts/check-citations.mjs` +83 lines: header now states THREE ceilings, each pinned by its own selftest case so a future widening goes red before the header can go stale — prose-separated citations are invisible to both heading regexes · heading matching is exact-string · the checker cannot describe its own subject matter. No regex changed, no suppression list, archive stays the sole exemption. `ci.yml` Unit tests step now asserts node's own pass count is non-zero (`grep -qE '^# pass [1-9]'`, reading `PIPESTATUS` not `$?`) → gh#45. Evidence `docs/verification/evidence/44/` (`prose-separated-scan.md` = the 25-hit census, `calibration.md`) and `docs/verification/evidence/45/calibration.md`. Commits `c281ca1` `34e68eb` `3ca7c58` `731f2b3`. Calibration pairs, one variable each: gh#44 green 32210088554 / red 32210186617 · gh#45 green 32212299488 / red 32212382616; main green 32210346322 · 32212493023. ADR-0019 scored (its own rule found all four issues). tests 153 unchanged · queue 3→5 open agent-actionable.

dec: **gh#44 fork = state the ceiling, NOT widen the regex** — on evidence: false positives outside the archive are ≥1, and ANY doc that describes a citation contains one (the evidence file for gh#44 tripped the gate the moment it was written); the set "ways prose can separate a path from a section sigil" is owned by whoever writes the next paragraph, so it never converges — ADR-0016's test applied to the decision rule itself, not just to the guard · **gh#44 DoD box 3's premise was wrong as written** — "both currently resolve" conflated human semantics with gate semantics; under exact-match the ADR-0010 findings citation is dead, so it closed on that reading with the correction in the closing comment and the box left unticked · **census findings filed as issues, not fixed in-run** (owner call) · **h3 `### S` + N=1 is this repo's DECLARED format, not drift** — the roll script only accepts `## S` and will always abort here; manual sed move plus the 3 asserts is the supported path, see the project override.

next:
- [ ] **gh#45 CLOSE — the one unfinished step of this session (focus).** Fix and evidence are on main and green; closing was never pre-authorized (this session's GitHub-write auth named gh#44 only). Done when `gh issue view 45 --json state` reads CLOSED and carries an evidence comment naming runs 32212299488 (green) and 32212382616 (red)
- [ ] **gh#46 — CI gates hardcode the set they cover.** Kill-tested with a positive control: a 7th game carrying an in-stage anchor AND an ungated button exits 0 on both `no-nav-in-stage-check` and `arm-gate-coverage-check`, while the same two violations planted in `pick-loser.ts` turn both red. Both scripts print `TARGET_FILES.length` game modules "clean" — the LIST size, not what was checked, and it happens to read 7. Done when a 7th game turns both red, each gate asserts its own set non-empty and reconciles against `src/games/`, calibrated both ways at run level
- [ ] **gh#47 — both CSP gates are blind to the edit that breaks ads.** `csp.includes(src)` is directive-blind: move a domain from `script-src` to `img-src` and the gate stays green while AdSense dies. The inline-script gate scans `<script>` elements only and misses an `onclick=` attribute. Done when the allowlist asserts the governing directive and the inline gate covers handler attributes, or each header states plainly that it does not — no third option, ADR-0019 — each calibrated both ways
- [ ] **owner-run, unchanged:** Azure token → gh#9 domain → gh#29 AdSense. Token first; it alone unblocks gh#13 · gh#15 · gh#16 · gh#17 · gh#18, which carry 1 unchecked box each and it is the live-URL box in every case. ⚠ the moment `gh secret list` is non-empty **every push to main IS a deploy** — re-gate every push, every pre-auth in this entry is void then. Done when `gh secret list` is non-empty
- [ ] **gh#24 stays deferred, gated not prose.** Unchanged from last session. Owner product question: should this site ever hold two paused rounds at once?

inflight: measured at save — working tree carries only this save's own 2 files · **open PRs: checked, 0** · branches: `main` only, `calibrate/gh44-ceiling-pins` + `calibrate/gh45-test-count` both deleted local+remote · worktrees: 1 (main; the kill-test worktree was removed and its branch deleted) · `gh api .../actions/secrets` **total_count=0**, re-checked immediately before every push, so no push this session was a deploy · local == origin/main, 0 ahead 0 behind · GitHub writes: 1 comment, 1 close, 3 issues filed · committed direct to main, no PR

spent: queue 3→5 (3 closed-or-done, 3 filed, 2 owner-blocked carried) · closed 1, filed 3, fixed 2 · CI script gates 10→10 (none added; one existing step hardened) · REFUTE rounds 2/2 on the gh#44 diff, 7 confirmed defects fixed · ctx ~255k of 1M at save

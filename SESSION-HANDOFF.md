# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state


### S2026-09-14#3
done: shipped `dcdea0f` gh#237 gh#234 — font gate vanished-inventory FAIL + bangkok-drift fog comment; ADR-0067 records the design. What git cannot show: **the queue was again mostly not-work** — of 10 RH rows only 2 produced code, 3 were already done or mis-scoped, 5 owner-blocked · **gh#219's one-shot is already fixed** — all 3 counter routes go through one shared helper carrying a ResizeObserver over the strip and its chips, so the shape was never per-route; its box 1 needs a real phone, not a browser, by its own text · **gh#235 answered, 6 of 6 runs** — croc-bite exists in only 6 of the last 15 CI runs (route count 13→14 on 2026-09-11), first handled gap 960-1976ms with gaps 2-5 sub-millisecond, input clock 73-87ms every tap, `over it: 1 handled, 0 input` every run, so the arm gate is untouched per ADR-0059 · **gh#182 carries TWO criteria at different stages** — the fold rule is done bar owner-reserved freeze-tap, but box 1 has 8 rows across 7 routes still reasoned `gh#182 open:`, which the probe file itself defines as recorded-not-blessed · **gh#181's agent half already shipped** and its scope is 8 routes per owner ruling 2026-09-05, not the 4 its title names.
dec: font gate = declared inventory is sole authority, ADR-0067; CSS-derived alternatives rejected because the expectation dies exactly when CSS and fonts vanish together, and this build declares faces inline in built HTML | gh#234 = comment only, draw order and MIN_GAP untouched, closed | tracker comments posted to gh#234 gh#235 gh#182 gh#219, owner-approved as one batch | gh#181 needs no agent work — only its owner-look box remains.
next: [ ] gh#182 box 1 — 2 rows remain, not 8: `freeze-tap 320x568` (owner-reserved 2026-09-10 and 2026-09-11) and `one-bomb 320x568` (never measured against the fold rule; absent from both `182-320-fold` evidence dirs, so it needs a measurement before it can be ruled on). The other 7 carry the owner ruling of 2026-09-15. Verify: `grep -cE "^\s*\['.*gh#182 open:" scripts/play-screen-fit-probe.mjs` returns 2 — the row-shaped anchor is load-bearing, because a bare `grep -c "gh#182 open:"` also counts three prose lines including `UNCLASSIFIED_ADVICE`, which embeds the literal on purpose and can therefore never reach 0
next: [ ] gh#181 box 8 — owner looks at the eight 1440x900 screenshots; no agent verdict replaces it. Verify: the ticket records the owner's own verdict
next: [ ] gh#101 needs owner Thai copy — single blocker for the whole fortune family; gh#102 portraits cannot start because its ten cards do not exist. Verify: copy lands and the cards ship with the portrait box empty
next: [ ] fortune family remainder gh#104 gh#106 gh#109. Verify: each ships Thai copy and the owner reviews strings
next: [ ] gh#141 real-phone walk owes gh#219 box 1, gh#205 box 6 rows 1-2, gh#204 box 7. Verify: each records a reading taken on a real device
next: [ ] gh#235 follow-up if wanted — a timestamp inside croc-bite's own first-handler path would name the cause; the reproduction question is closed. Verify: a CI run carrying that timestamp
next: [ ] owner physical blockers unchanged — gh#9 no A record no NS, blocking gh#160 gh#29 gh#12 gh#19. Verify: A record resolves
inflight: measured during this save — `git status --short` shows only this save's own files · `gh pr list --state open` empty, checked · `main` = `origin/main` + 3 unpushed commits, `dcdea0f` src plus 2 docs saves; the src push is still owner-gated and was NOT taken · probe ports 4321/5051/9222/9344/9348 free, checked · `git worktree list` shows only the main tree · no background agents · full `run-workflow-gates.sh` green this session at `executed=46 failed=0`.
spent: queue 10→7 · batches 5 · dispatches 10 (9 CLI fleet, 1 native ui-driver)
errors: **a sweep keyed on line POSITION across a set whose membership grew** — croc-bite was absent from 9 of 15 runs and the third `play-exit GAPS` line was a different route there; produced a confident chronological regression that does not exist, killed by re-running keyed on the route name · **read a ticket's prose instead of its instrument** — concluded 2 open routes on gh#182 from the comment thread when `play-screen-fit-probe.mjs` records 8 · **briefed a dead premise onto an agent** — took gh#181's route list from the ticket BODY after reading its last comment with `head -30`, missing the scope ruling one line below the cut; 2 of the 4 routes briefed were ones the owner had ruled OUT, and the agent's REFUSED was the only thing that stopped it · **called a bypass a regression from reading the diff** instead of running the pre-change gate, which greened identically; the false claim reached a follow-up brief · **wrote a false-green attack too weak to succeed** — pointed the override at a tree that HAS fonts, where it cannot win; REFUTE found the font-free-subtree variant.

# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-09-07#4
done: pushed `06c1495..d2e2dcc` (4 commits: `d2e2dcc` gh#181 boxes 3+4 across 8 routes, plus 3 docs commits that had been waiting per ADR-0049), CI 34126895732 success with 0 non-success steps and Deploy to Azure success — the desktop type change is LIVE. What git cannot show: **I briefed gh#181 box 4 wrong and REFUTE caught it** — I told a worker `zero-trigger` was compliant and not to touch its tokens, but `zt-desktop-track-inline` is undeclared repo-wide and the existing comment reasoned the VALUE while never stating the absence · **gh#210 rows 30/31 were briefed as live agent work and the OWNER AUTHORIZED doing them on that false premise** — the tree already had it: `EXPECTED_LEGS=26` carries a comment naming gh#210 for the 24-to-26 bump, both legs are wired on the same CDP port, and the "why the pair is absent" comment row 31 wanted removed is already replaced by one explaining why it is present; root cause is reading UNTICKED DoD boxes as a work list in a repo whose own guide says boxes are never ticked, so reading them was worse than not reading them, and the fork had correctly told me to read them · same lens then cleared gh#184: the strip probe's viewports are exactly row 21's three widths and it ships a documented mutant/restore red-green pair for row 23, so only its owner-look row remains · **the fork killed a gate before it was built** — a presence check over the route set returns a null verdict because the PREDICATE is unownable (`font-size: inherit` satisfies presence, "carries a written reason" enumerates prose), which is the ADR-0056 shape on the acceptance side · **REFUTE caught a font-size REDUCTION shipping as a raise** — `cannon-flag`'s header rule had inherited the tap-floor rule's selector list, which includes two `.angle-btn` play-surface controls at 1.3rem, so 20px SHRANK them while the route's real second header `.icon-btn` went unraised, and a declaration count passes that diff either way · **the diff took both sides of one policy** — `power-meter` invented a `.btn-primary` padding with no phone antecedent while `timebomb` refused the identical move for the identical reason; resolved by dropping power-meter's, since the standard says raise the equivalents and do not invent more · `freeze-tap`'s box-3 raise reaches 2 of its 5 primary buttons because `main.js` writes the other 3 with inline styles that beat any stylesheet rule at any specificity · **the 1440x900 question resolved with NO before/after run** because the fit probe records its own expectations: `power-meter` is pinned "not a defect 2026-09-04: 76px on press 0" and read exactly 76px at press 0, `cannon-flag` is pinned "2px on press 1" and read exactly 2px worst at press 1, so the raises moved no layout · **do not read `widthFillPct` as a signal** — the probe's own header says it is deliberately unpinned because it moved 89.2 to 100 across two consecutive runs on `cannon-flag`, these games advancing on their own clock · `pinocchio-luck` never HAD a 1100px block: the grep hit is prose, and its `>=361px` block was added directly in gh#184's commit, so "never built" not "retired" · screenshots are working artifacts here, never the record, and the rule's REASON is the half to carry — `.gitignore` drops `docs/verification/evidence/**/*.png` and its own comment says why: probe screenshots regenerate, the JSON logs are the evidence, and **a CDP probe never qualifies for the one carve-out** (gh#50's webkit frames, kept because iOS WebKit had no inspector) precisely because it can always emit JSON. So a box closes on a TRACKED README carrying the numbers — the `145-timebomb-reduced` convention, 151 files tracked under `evidence/` — and never on the PNGs beside it. Quoting the path without the reason is how a fork brief this session generated a whole candidate approach on a false premise · both steps the local runner calls structurally unrunnable ran in CI and passed · SendMessage unavailable again this session, so all five REFUTE fixes were taken SELF.
dec: approach=per-route scales per the fork (shared site tokens are dead on the standard's own "per-route locals" text) | presence-check gate NOT wired, fork null verdict | power-meter padding DROPPED rather than licensing timebomb's too | box-3 acceptance = presence plus a written reason, never a px threshold per the 2026-09-04 ruling | gh#210 rows 30/31 NOT taken, already shipped | gh#184 gh#210 gh#219 all moved to `ready-for-human` | second artifact declared for the CI wiring then CANCELLED when the work proved already shipped, so the REFUTE cap stayed at one artifact one round | remote branches LEFT at owner's choice though `git cherry` marked every commit already upstream | the per-route reads-as-designed verdicts are an AGENT's call under the owner's delegating ruling, not the owner's own look, and both block-less routes' comments now say so.
next: [ ] **gh#181 box 8 — owner's 1440x900 look, and it is now overdue rather than pending.** The type change deployed before the look happened, by owner's explicit choice with the objection on the record. Verify: the ticket records the look or a waiver
- [ ] **freeze-tap's three inline-styled primary buttons.** `startGameBtn` `playerReadyBtn` `nextTurnBtn` carry inline font-size and padding from `main.js`, so the desktop rule cannot reach them. Verify: either those three read the desktop values in a real browser at 1440, or the route's comment is the accepted answer and the ticket says so
- [ ] **zero-trigger's result modal, unverified hypothesis.** Two `flex: 1` primary buttons at the raised padding could wrap; nothing measured it and no gate sees it. Verify: a real 1440 look at that modal, or a measurement of the two buttons' boxes
- [ ] **gh#181 box 1 — owner restates or strikes it.** Verify: box 1 reads as something evaluable
- [ ] **#221 triage** (the mockup enumeration drops a real mockup dir before counting). Verify: the ticket carries a label other than `needs-triage`
- [ ] **#220 — owner scopes it.** Narrow converts each absence into a presence; structural lets the registry assert a fragment must stay absent. Verify: the ticket says which and why the other was declined
- [ ] **gh#213 — owner reads the sequencing** (needs the gh#150 read). Verify: the ticket says whether "sequenced after Bomb" means that ticket closes or that the route lands
- [ ] **gh#202 — owner answers the one question.** Close on the instrument and spin box 5 out? Verify: the ticket records the split
- [ ] **gh#210 box 2, gh#219 box 1, gh#184 box 22 — owner looks.** A truncated long name's legibility, a real-device rotate both directions, and whether a scrolling strip says more exist. Verify: each ticket records the look or a waiver
- [ ] owner physical blockers, unchanged: gh#9 domain returns no A record, re-probed this session with a control that resolves (blocks gh#160 gh#29 gh#12 gh#19) · gh#141 real-phone walk (blocks gh#204, gh#205 rows 1/2) · gh#182 320/390 scroll (blocks gh#181 box 5) · owner standing gh#192 gh#165 gh#112 gh#109 gh#106 and the fortune family gh#97-gh#104
inflight: measured during this save — `git status --short` shows only this save's 2 files (`SESSION-HANDOFF.md`, `docs/sessions-archive.md`) · `gh pr list --state open` = none · unpushed on main = 0 before this save's commit, verified by `git rev-parse` ref equality and NOT by a piped count · branches `main` + `claude/rh-cafbc2` only, the latter still deliberately kept · worktrees: one, the main tree · probe ports 4321/4322/4592/9222/9333/9344-9348/9555 have 0 listeners and 0 Chrome or probe processes remain · this save's docs commit is NOT pushed per ADR-0049.
spent: queue 11 rows in — 6 done, 2 dead on liveness before any brief, 1 declined by the owner, 2 reported-only · batches 5 waves, 6 dispatches, 6 attempts, 0 failed, 1 REFUTE round of max 2, 0 re-forks, 1 suite run with 0 wasted
errors: read unticked DoD boxes as a work list and took a user decision on the false premise that gh#210 rows 30/31 were open, in a repo whose guide says boxes are never ticked — reading them was worse than not reading them · briefed a worker that `zero-trigger` was box-4 compliant without checking, and REFUTE found the token undeclared · wrote a fork brief premise claiming `pinocchio-luck` had a 1100px block, off a grep hit that was prose, and two agents independently refuted me · five instrument-shape slips in one run, all the same class of the counting method being the unchecked claim: an unquoted `--include=*.css` glob that zsh expanded into an error, a piped `ls | awk` that ate the error and read as a missing file, `node --test` given a directory instead of its glob so a MODULE_NOT_FOUND rendered as a test failure, a grep for a phrase that spans a line break returning nothing, and `git log | wc -l` reporting 1 unpushed commit on empty output because the command wrapper injects its own status line — which I first recorded as a settling race and only then diagnosed correctly.

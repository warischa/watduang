# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state


### S2026-09-05#2
done: **NOTHING COMMITTED — 5 tracks built and verified, all uncommitted; no REFUTE round, no suite run, no push.** gh#104 met 5 boxes (per-category `listHeading` field replaces the shared `เกมในหมวดนี้` page literal; fortune search copy solo-only) · gh#209 shipped a coverage gate + 7 new per-route tests so 11 of 11 routes carry one, 4 calibration legs all run · gh#210 chip cap in px with the `+N` fade sized from the same token, both legs wired into lane2, `EXPECTED_LEGS` 24→26, patch APPLIED to the live tree · composition instrument built with an 11-route baseline recorded, gated on row completeness only · 375px check PARTIAL. What git cannot show: **the strip-chip probe on `main` was DEAD** — it calls `fs.` twice with no import, so `node driver.mjs <probe>` throws `ReferenceError` before any page loads, and S2026-09-05#1's "present and calibrated" was false; an import smoke test via `node -e` passes because `-e` exposes builtins as globals · that probe ALSO read only the boot scroll offset, answering `nakedCutCount: 0` on all nine combos with the cap removed, and its offset arithmetic needed `-1` not `+1` · **its old control leg was a tautology** — hiding the `+N` band makes every clipped chip naked by definition, so it could never prove the geometry check fires; a gradient-zeroing leg can, and reported an uncovered sliver of 42.72–81.09px · **gh#203's premise is substantially wrong** — the artifact its own DoD cites is absent from this machine, and 8 of 11 routes carry a committed derived board cap from `280790a`, each annotated "RULE: the game's own grid arithmetic", under `desktop-sizing-decisions.md` rule 3 "The board is capped and centred inside the frame, not stretched to it"; only `short-stick` and `pinocchio-luck` lack one and `cannon-flag` declares those tokens inexpressible · **`party-size-claim-check` has NO hole** — `EXEMPT_META` blanking `title`/`description` is a twice-recorded owner decision and its green on a live `2-10 คน` was correct; only `categories.ts`'s fortune `seo.description` is wording-FROZEN, while manifest `seo.*` is gate-exempt but explicitly not frozen per that doc's own next sentence and the 2026-09-04 gh#192 (g) precedent · **gh#182 box 1's rewording is NOT met**: of 17 exemption rows at the two phone viewports only 1 cites an owner ruling, 16 say `gh#182 open:` — recorded, not ruled · `PlayerSetup.astro` renders on NO live page, `getStaticPaths` filters all 11 party games out · 9 routes ship a sound toggle in 6 id spellings, not 5, `timebomb` is the only sound-making route without one, `dice-loser` builds no `AudioContext` · `src/games/timebomb.ts` IS the play route's engine, so "shared synth has no toggle" and "the ticking route has no toggle" are one defect · `zero-trigger` ships 3 English `aria-label`s beside correct Thai `title`s, no gate inspects an accessible name, and `markup.html` is extractor-owned so a fix typed there is regenerated away · `pinocchio-luck` 320x568 sideways clip grew to 50px against a recorded 11px, printed not gated.
dec: owner 2026-09-05 — scope is the 7 build rows, backlog rows stay parked · gh#206 re-pin `BASELINE_TOTAL_BYTES` with the growth attributed in the same PR · rewrite both broken DoDs · push PRE-AUTHORIZED on `run-workflow-gates.sh` green every step AND zero outstanding REFUTE findings, evidence shown first. Mine: the fork chose fix-then-measure-once and I overrode that one answer to measure-first, because gh#203's diagnosis was unverifiable from the repo and the fork had rejected a baseline against the font-derived instrument rather than the font-independent one it then chose — its other two answers stand, re-fork unspent · gh#203's measurement is repo-owned box geometry (resolved grid-track count + frame cap), reported per route and gated ONLY on row completeness, never a threshold, because the owner's 2026-09-04 ruling declines a threshold on a font-derived number · gh#210's cap is `px` so the number is repo-owned; `ch` or `em` on the chip's own text hands it back to the rendering font · gh#209 defaults CHECKED so a route nobody registers reds, and its exemption is re-proved rather than trusted · `timebomb` and `dice-loser` take desktop layout in `play.css`, which `extract-mockup.mjs` does not write. Owner, at save: the Premise-from-the-ticket's-last-comment rule and the grep-cannot-answer-a-runtime-question rule go in THIS repo's `CLAUDE.md`, not global scope · the orchestrate chip's `Parent:` requirement **already existed** and was violated because it sat mid-paragraph, so it became a read-your-prompt-back gate rather than a new rule — a rule nobody reads is not a missing rule.
next: [ ] apply `w2.1-composition.patch` then wire the icon gate into the workflow list and the npm chain — the tree is RED until then. Verify: `node scripts/gate-selftest-coverage-check.mjs` exits 0 and `bash scripts/run-workflow-gates.sh` green on every step
- [ ] REFUTE pass over the integrated diff, then the suite once, then one commit per track, then the push under the standing authorization. Verify: the evidence line shown before pushing, CI green
- [ ] owner: gh#203 scope — is a capped centred column correct (only `short-stick` and `pinocchio-luck` in scope) or a desktop defect (rule 3 changes, 5 more routes enter)? Verify: a verdict recorded per route and the owner has said which reading holds
- [ ] owner: gh#104 — the frozen fortune `seo.description` advertises `วัดดวงความรัก`, delisted until gh#101. Delete the name or leave it. Verify: owner answers, one word changes or does not
- [ ] owner: gh#140 box 2 — does the seat icon travel with the name? Verify: the decision recorded with its consequence for play-route games stated either way
- [ ] gh#182 box 1 — 16 of 17 exemption rows cite no owner ruling; under the reworded criterion an undocumented overflow is still a failure. Verify: every row at 320x568 and 390x844 either cites a ruling or is fixed
- [ ] gh#210 box 3 — a CI artifact diffed against the local nine-combination zero. Verify: both numbers in the ticket, diffed
- [ ] gh#206 re-pin, LAST, after every byte-moving change. Verify: the re-pin and its attribution in one commit
- [ ] publish the two replacement DoDs — extract criteria and owner questions from the drafts file, 404 lines is too long for a ticket body. Verify: each ticket carries a falsifiable criterion set and a marked owner-questions section
- [ ] `MEMORY.md` is over its index budget and loads only partly — route detail into topic files. Verify: the startup warning is gone
- [ ] owner: gh#204 gh#205 ranking · gh#192 b-e · gh#9 domain still blocks gh#160 gh#29 gh#12 gh#19 · standing gh#165 gh#150 gh#141 gh#112 gh#109 gh#106 and the fortune family gh#97-gh#104
inflight: measured during this save — 11 modified + 9 untracked, nothing committed · `gh pr list --state open` 0 · HEAD `e986c65`, `origin/main` `d0a3d30`, one docs commit unpushed · 4 worktrees: two agent worktrees at `d0a3d30` holding the composition patch (saved, NOT applied) and the gh#210 patch (applied), plus `claude/hopeful-fermi-74ec5e` at `92b9f00`, the owner's independent aria-label session, which has committed work and also wants the workflow list and the npm chain · `gate-selftest-coverage-check` exits 1 on the unwired gate. Patches held outside the repo: `wip-gh104-backup.patch`, `w2.2-gh210.patch`, `w2.1-composition.patch`.
spent: queue 31 to 11 · batches 4 waves, 17 dispatches, 1 REFUSED consuming no attempt, 0 of 2 REFUTE rounds, 0 of 1 re-forks
errors: premise asserted from memory or one narrow probe, 6 instances — the brief's Premise line written from the handoff instead of the artifact's last word · two documented shell traps hit, an unquoted flag glob and `${PIPESTATUS[0]}` under zsh, both named in the repo's own shell-traps doc that `CLAUDE.md` makes mandatory before writing any verification command · a text grep used to answer a runtime-property question, and the wrong verdict published before verifying · a twice-recorded owner exemption called a gate hole, with a worker briefed to narrow it · a hand-off chip prompt written with no parent id, so it is a peer with no control channel and both runs now want the same two shared files.

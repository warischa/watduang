# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-09-26#2
done: RH FRESH on both axes · shipped `78ee43c` (croc-bite tooth taps now honour the arm window after a modal close: the survey claim was REAL, via reset-cancel and settings-close) · `2fd61ab` (croc-bite stylized look; round 1 was washed out, caught by opening the screenshot, fixed in round 2) · `daeb9ac` (pinocchio-luck shader, geometry and motion upgrade on hand-written WebGL, not `three`) · pushed `89f26ec..daeb9ac`, CI run `36219653760` success; live verified: `/game/croc-bite/play/` loads the chunk carrying `fromScene`, `/game/pinocchio-luck/play/` loads the one carrying the ACES tonemap, and unchanged chunks grep 0 (control) · `9d8fcd3` ADR-0068 written and rejected, local, rides the next src push per ADR-0049 · gh#103 comment `5843435656` records box 3 dropped, read back · freeze total 1,092,277 vs pinned 1,049,872 = +4.04% (the pinocchio agent's `bundle-freeze-check` run), under 1% left before +5% · `blind-103/` moved to `~/.Trash/blind-103-2026-09-26` (owner go, cleanup popup) · screenshot PNGs under `docs/verification/evidence/{croc-bite,pinocchio}-3d-2026-09-26/` are gitignored, local only; the probes and JSON are committed.
dec: item 2 go, with re-pin if red (unused, the check stayed green) | item 3 → ADR-0068, croc-bite only | gh#103 box 3 dropped, not re-barred (comment `5843435656`) | orchestrate-qwen: leave as is, so its row is dropped | pinocchio silhouette change accepted: the inside-out cylinder fix exposes the nose collar and the outer arm walls (owner popup 2026-09-26 — recorded here, not on any ticket) | pinocchio-luck `main.js` is extractor-owned, so a re-extraction wipes `daeb9ac`'s hand edits.
next: [ ] gh#101 — build the page from the signed-off drafts, per the ticket body's "What to build"; re-render any portrait whose card's gender, age, build, occupation, manner or mark the owner edit moved, with the command in `images/IMAGES.md`'s gh#103 section; **also carries gh#104's conditions for this game's own copy** — gh#104 closed while `love-match` was unregistered, so its `seo.*` was never evaluated and `love-match.ts` carries one `เกม` (grep 2026-09-23); `public/og/love-match.png` is the OG gate's named exemption and reds the gate on registration. Verify: the ticket's boxes except box 3 (dropped, comment `5843435656`), each checked the way its own wording says, and `docs/verification/evidence/104-2026-09-23/enumerate.mjs` re-run after registration with no new flag · Blocked · ask: owner edit — consumes H: gh#101
next: [ ] gh#103 boxes 4 and 6 — move the accepted portraits into the publish surface at the size the page loads, and state what one reader downloads; no route for this game exists under `src/pages/`, per gh#103's comment of 2026-09-23. Verify: each deck file referenced by a card on the built page, and the per-reader byte count in the closing comment · Blocked · ask: unblocks when the gh#101 page exists — nothing to decide before then
next: [ ] gh#9 close — after H: gh#9, verify and close with an evidence comment. Verify: `dig +short watduang.com A` resolves, the owner reports both SWA Custom domains rows `Ready`, `https://watduang.com` serves the site · Blocked · ask: unblocks when H: gh#9 lands — nothing to decide before then
next: [ ] gh#160 — Cloudflare Web Analytics, chosen and not wired; its other blocker #159 is confirmed dead. Verify: per ticket, once the domain resolves · Blocked · ask: unblocks when H: gh#9 lands — nothing to decide before then
next: [ ] gh#29 — ad slots need the domain AND an owner AdSense account, site review and publisher ID; two owner acts, not one, per its own body. Verify: publisher ID recorded and ad code live · Blocked · ask: unblocks only after gh#9 AND the owner's AdSense account — do not brief it as domain-only
next: [ ] gh#19 — month-6 organic-clicks gate; needs the domain, Search Console connected to it, and six months of traffic. Verify: organic clicks recorded at month 6 · Blocked · ask: nothing to decide until the domain and the traffic window both exist
next: [ ] H: gh#101 owner edits the four drafts in the tree — `docs/copy/nuea-khu.md`, `docs/copy/nuea-khu/cards.md`, `docs/copy/nuea-khu/cards-m6-m20.md`, `docs/copy/nuea-khu/cards-f6-f20.md`; review aid `review-101/cards.html` + `checks.md`, outside git in `~/.claude/projects/-Users-waris-c-claude-free-game/` (comment `5790035652`): ten cards aged 42-54 and four non-Thai nationalities flagged, no duplicate, every card mapped to a portrait. Verify: `grep -c '⚠ UNREVIEWED'` reads 0 on all four, measured 1 each 2026-09-26 (RH)
next: [ ] H: gh#9 owner registers `watduang.com` AND binds the apex and `www` in the SWA portal per comment `5789453647` (ruling 2026-09-23 recorded in the archive, not on gh#9) — owner-only and financial, never an agent action; re-run whois immediately before paying (`No match` 2026-09-23, again 2026-09-24), privacy on, auto-renew on, and check the registrar offers ALIAS/ANAME/flattening at the apex. Hand back: `dig +short watduang.com A` resolves and both Custom domains rows read `Ready`
next: [ ] H: gh#141 walk every box on a real phone with `docs/verification/gh141-real-device-script.md` — 20 boxes incl. gh#204 boxes 7 and 9 (one measurement, the frame rate), gh#205 box 1, gh#219 box 1 (comment `5790035284`); gh#204 and gh#205 carry NO agent slice per their 2026-09-21 triage comments, gh#219 per its 2026-09-15 comment — do not brief them as code work. Also note on a real phone, not yet on any ticket: croc-bite and pinocchio-luck frame rate after `2fd61ab` and `daeb9ac` (pinocchio desktop rose ~22→30 ms per frame on a software renderer, per its agent's probe, not a device), and the croc-bite lite tier via a coarse pointer, which emulation never reached. Verify: every box noted with device and OS on its own ticket
next: [ ] H: gh#12 needs an owner Google Ads account, NOT the domain — its body says `Blocked by: —`. Verify: Keyword Planner volumes recorded under `docs/research/`, never `.scratch/`
inflight: measured during this save — `gh pr list --state open` empty; one worktree, the main checkout, branch `main`; no background tasks; HEAD `9d8fcd3` 1 ahead of `origin/main`, and this save commit stacks on it, local; tree dirty only with this save's two files.
spent: queue 11→6 · batches 3 — 6 open non-`H:` items: gh#101, gh#103 boxes 4 and 6, gh#9 close, gh#160, gh#29, gh#19. Closed: tooth taps, croc-bite look, 3D for five (ADR-0068), gh#103 box 3, orchestrate-qwen (left); pinocchio-luck added and closed in-session.
errors: **(1) a probe that could not fail** — the live check diffed two chunk lists, both empty because the `dist/` page path was wrong, and "identical (0)" read as a pass; caught, and re-done by content grep with a control.

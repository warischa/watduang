# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state
### S2026-08-28#2
done: **the previous sitting shipped 5 code commits and recorded none of them; this entry is that record plus this run's own work.** Prior, unrecorded: `db5b16c` gh#131 · `6ed1f7f` gh#133 · `9c4e8aa` gh#89 · `54567aa` gh#78 · `601d498` gh#101-delist. This run: `1680917` + `557856c` pushed, CI 33155360622 GREEN and deployed — read the commit messages, not this line · **`601d498` left the delisted game's stylesheet imported page-globally: 11 rules / 1152B / 9 `.lm-` classes shipped inside the built game-page sheet with no page consuming them, measured in `dist/`, now removed** · the same delist had rotted a game count in 10 comment sites (3 sibling game CSS + 5 shell files + `Base.astro` + `pick-loser.ts`); fixed as a class, 2 sites deliberately left (`_el.ts` is accurate past tense, `love-match.css` is dead code per the standing decision) · tracker caught up: gh#131 + gh#133 CLOSED with evidence, neither had carried a comment · gh#89 0/6→4/6 · gh#109 relabelled ready-for-human · **gh#134 FILED** · gh#77 gh#78 gh#79 gh#80 gh#81 gh#101 gh#114 gh#120 commented · **levers box answered across all 5 tickets: gh#80 + gh#81 PASS (ticked), gh#77 + gh#78 + gh#79 DEPART (unticked)**
dec: fork = keep the adopted save order; an entry enumerating completed work + a queue is never stale, so my intake objection was wrong · window N=1 and `roll-state-window.sh` only accepts `^## S`, so the roll is manual and this entry is revised in place, never followed by a #3 · **gh#114 must NOT be closed on the previous sitting's "0px on all four tool pages" — ADR-0044 records 0px-macOS / −9px-CI for this exact mechanism and says a local green already closed gh#120's first round wrongly**; the bound (`live-region-height-check`, a `ci.yml` step, scans all of `src/pages/tool`) is the only closure evidence · **no gate pins CSS weight at all** — `bundle-freeze-check` measures the reachable JS closure only, so 1152 shipped bytes left the bundle with all 31 gates green; that is why the dead sheet was invisible → gh#134 · **audit method matters more than the verdict: whole-canvas literal matching admits false PASS, element-scoped same-node diffing does not, and it overturned 1 of 3 prior PASSes (gh#79)** · deleting the `display type step` lever means any per-game font-size now needs a same-element canvas trace — that alone is why 3 of 5 sheets depart · `card density` has no agreed definition, so the levers box is not fully decidable even for a clean sheet · **`design/canvas.json` holds TWO design sets: "หน้าเกม · 6 จอ" (`Game*.dc.html`) is live; `Siamsi*` / `DuangToday*` / `Soulmate*` are the artboards for the UNBUILT redesigns gh#97 gh#99 gh#101** — audit against the wrong one and every verdict is void
next:
- [ ] owner: gh#114 — box2 wants the offset "measured in a real browser at 320 and 390, not reasoned about" and this machine cannot do it truthfully. Cheapest route first: extend the 498px content-growth judgement already recorded on gh#120 into ADR-0024 and close on box1's second branch · or move the measurement CI-side · or leave it
- [ ] owner: gh#77 + gh#78 + gh#79 levers boxes — all three fail for the SAME structural reason, a value with no same-element canvas source: `.tb-warn` font-weight, siamsi's `perspective`/`transform-style`, short-stick's `.st-shout`/`.st-remark`. **จับไม้สั้น's passing and result screens have no artboard at all.** One ruling closes all three: commission the missing artboards, or declare functional-only elements outside ADR-0033's value scope the way `display type step` was rescoped out
- [ ] owner: FOCUS-BLOCKER — Thai copy gh#97–gh#104 · gh#75 re-spec · gh#123 / ADR-0033 / gh#120 font · gh#9→gh#29 · gh#13 gh#12 gh#19 — details in archive S2026-08-27#3
- [ ] owner: gh#132 — shared lock vs its own lock for the tools is an ADR-0039 call, and it blocks the fix
- [ ] owner: gh#78 box1 is ticked but describes the turn dots `54567aa` deleted; unticking rewrites an acceptance, so it is yours
- [ ] owner: gh#109 boxes 1–2 are Thai copy · gh#79 is 7/8 and its last box is the levers one
- [ ] agent: gh#134 — CSS has no weight or reachability gate; prefer the manifest-reachability check over a byte pin, `601d498` is the positive control
- [ ] agent, PARKED by owner 2026-08-25: re-run the hero as a true transparent cutout
inflight: tree clean (checked) · 0 open PRs (checked) · local == remote at `557856c` before this save · 38 open issues (checked) · ONE branch, ONE worktree (checked) · CI 33155360622 green and deployed
spent: queue: G1+G2 drained, G3 premise-dead, G4 re-decomposed into 2 audits · 7 dispatches (1 haiku bulk-reader · 1 Explore · 1 fable SOLVE · 3 opus reasoners · 1 fable REFUTE) · REFUTE 1/2 rounds, REWORK then green, re-forks 0 · 3 full local `npm run ci` green + 1 CI run green and deployed · 2 issues closed, 1 filed, 11 commented, 5 bodies edited · **my own errors: raised an intake contradiction the fork refuted · ticked gh#79 on the weaker audit method and had to untick it publicly · broke a subagent's archive citation with my own roll then greped a phrase the archive never used · accepted "20 test files" (really 18) and "gh#80 has 9 boxes" (really 8) from returns · nested a heredoc in `bash -c` again · assumed a bundle re-baseline was needed when the gate is JS-only**

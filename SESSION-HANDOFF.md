# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-09-08#1
done: pushed `a44d276..3e0e1b0` (6 commits: `9e691c5` gh#221 enumeration partition, `26c8e07` gh#181 box 7 residuals, `3f399bf` ADR-0062, `2275eaa` iOS WebKit WebGL, `621f34a` gh#204 boxes 4+6, `3e0e1b0` gh#205 boxes 2+6; plus `a44d276`, last session's docs save riding this push per ADR-0049), CI 34183601734 success with 0 non-success steps and Deploy to Azure success — all LIVE. What git cannot show: **my own gate shipped a self-referential proof and REFUTE caught it** — the reconciling count was computed with the same `readdirSync().filter(isDirectory)` predicate the code under test uses, so "sum N of N" could never fail on an entry that predicate drops; a symlinked mockup vanished behind a clean sum, which is gh#221's own bug class reproduced inside gh#221's proof. Expected total now comes from an UNFILTERED readdir with every entry bucketed, which is why the real root reads 15 entries and not 14 — a `.DS_Store` had been dropped without comment all along · **the design pass's recommended ADR-0056 fix would have broken the control it asked for**: lowering `FRAME_FLOOR` to `frames > 0` passes a frozen loop, measured at frames=1 by the new `BD_STOP_LOOP` leg, so 30 stays and the control was added instead; the ownable upgrade (a same-run relative check, reduced frames against baseline frames) is named and deliberately not built · **three handoff claims were false**: `ob-away` is alive and never marked the ADR-0051 fallback (it hides the board while the menu is up), NO probe in the tree asserts fallback ABSENCE so the "two drivers' vacuously true checks" have no referent, and `three` is not in the tree at all — zero imports, not installed, only ADR-0060 mentions it · **row 5's premise was dead on arrival and true by the end** — #204/#205 were briefed as mislabelled because "every open row needs the owner's phone" (false: most open rows were agent work), and became correctly `ready-for-human` only after this session did that work · **6 of the owner's 11 added boxes were already satisfied** before any work started · gh#212 turned out CLOSED, so the short-stick registry finding became #222 rather than a comment nobody reads — the same shape as last session's closed-gh#183 attribution · iOS answer: both WebGL routes get a live context in Simulator WebKit, renderer Apple GPU, calibrated against a stubbed-draw must-red, and the record states it cannot speak to real iPhone hardware because the Simulator uses the host GPU · SendMessage unavailable again, so every REFUTE fix went to fresh workers rather than the agent that wrote the code · **two prevention rules applied at owner request after the save report** (items 2 and 3 of the feedback pass): `docs/agents/domain.md` gains "a number in an ADR carries the command that produced it", and `CLAUDE.md`'s last-comment rule gains "a scout's map is not that reading". The same edit killed a stale claim in `domain.md` that said the durable decisions live in GitHub Issues "until `docs/adr/` exists" — it exists and holds 63 ADRs — and dropped a ticket count from it that rots, per this repo's own rule against numbers countable from `gh`.
dec: FRAME_FLOOR stays 30, BD_STOP_LOOP added instead (ADR-0063) | no CI leg for 2D canvas probes, the class-wide leg is #224 (ADR-0063) | ADR-0062 = declare browser-verification at 14KB, do not split — owner ruling, after no permitted cut reached 11KB | iOS probe PNGs stay gitignored: the carve-out requires the image to be the ONLY artifact and this probe emitted a log | ticked only the boxes I hold my own evidence for, never the 5 a scout assessed | ADR-0012's own falsifier CONFIRMED by ADR-0062 and recorded there
next: [ ] **`CLAUDE.md` has 96B of headroom — route something out BEFORE the next edit to it.** This save's two prevention sentences took it from 11886B to 12192B against a 12288B ceiling, so the next addition fails the gate. Route, do not shorten prose. Verify: `check-budgets.sh CLAUDE.md` reports at least ~800B free with the payload moved to a doc it points at, not deleted
- [ ] **#223 — the `--enumerate` absent-root path** still reports nothing measured and returns 0. Unblocked agent work. Verify: an absent root is a distinct outcome from a successful empty measurement, and `--selftest` reds if the two are conflated
- [ ] **#225 — the reduced-motion step scaling is uncovered.** Unblocked agent work. Verify: a `drift-rules.test.mjs` case pins the scaling factor and one deliberate change to it reds
- [ ] **#222 — owner scopes it** (short-stick has zero recorded divergences and would be fully overwritten). Verify: the ticket says whether a registry entry is added or the absence is recorded as a finding
- [ ] **#224 — owner scopes the class-wide 2D-ink leg.** Verify: the ticket says registry-driven leg or per-route hand tools retire
- [ ] **gh#204 boxes 7 and 9 — owner's phone.** Frame rate on a mid-range Android; box 9's residual IS that number. Verify: both boxes record a measurement or a waiver
- [ ] **gh#205 boxes 5 and 6 — owner's eye and phone.** Verify: box 5 records the OG-card look, box 6's rows 1-2 record the walk
- [ ] **gh#181 boxes 1, 2, 6, 8 — owner.** Box 8 has eight fresh frames waiting. Verify: each box reads as evaluable, settled, or waived
- [ ] **gh#202, gh#213, gh#210, gh#219, gh#184, #220 — owner looks or settles.** Verify: each ticket records the call
- [ ] **`docs/agents/browser-verification.md` is now at 12381B under a declared 14336B** — about two more traps of headroom. Verify: the next trap added checks the budget before writing
- [ ] owner physical blockers, unchanged: gh#9 domain has no A record on the system resolver or 1.1.1.1 and no NS either (blocks gh#160 gh#29 gh#12 gh#19) · gh#141 real-phone walk, 15 unchecked rows · owner standing gh#192 gh#165 gh#112 gh#109 gh#106 and the fortune family gh#97-gh#104
inflight: measured during this save — `git status --short` shows only this save's own files before staging · `gh pr list --state open` = `[]` · unpushed before this save = 0, by `git rev-parse` ref equality, not a piped count · branches: local `main` + `claude/rh-cafbc2` (5 commits not on main, from 2026-08-29, whether they landed by another path is UNCONFIRMED — do not delete on that line alone), remote-only `gh192-landing-rule` + `gh195-196-play-fixes` · **worktrees: 1, the main tree** — the two integrated agent worktrees were removed at save under the SH cleanup grant, after verifying main is a strict SUPERSET of each (main carries the post-review symlink bucketing that neither worktree had), and their two branches deleted, both at base `0a01a92` with 0 commits main lacks · probe ports 4321/4322/4592/9222/9333/9344-9348/9555 have 0 listeners, 0 Chrome or serve processes, simulator `721ACACC` shut down after use · this save's docs commit is NOT pushed per ADR-0049.
spent: queue 16 rows in — 4 done, 1 inverted-then-satisfied, 11 owner-blocked; plus 11 owner-added boxes mid-run (6 already satisfied, 3 done, 2 owner) · batches 5 waves, 18 dispatches, 18 attempts, 0 failed · 1 artifact x 1 REFUTE round (7 findings, all fixed and re-verified) · 1 suite run, 0 wasted · 1 advisor consult, which upheld the FRAME_FLOOR override and corrected my phrasing of it
errors: shipped a self-referential proof — the gate's expected count used the same predicate as the code under test, so it could not fail on what that predicate dropped · wrote two counts into an ADR as repo-wide totals when both came from a grep scoped to `.md`/`.mjs` · briefed gh#205 box 6c as open without reading the ticket's LAST comment, which already said it was done · imposed a no-line-number-citation rule on three briefs for a tree that gate exempts wholesale · ran a baseline comparison against a copy of the script placed outside the repo, which printed nothing, exited 0, and read as a regression · posted a comment carrying a literal `#NEW-1` placeholder for a ticket that did not exist yet · corrected "directory" to "entry" and left two lines of my own draft still asserting the old word · let the run ledger accumulate duplicate section headings through in-place edits, so its tail sections are superseded stubs.

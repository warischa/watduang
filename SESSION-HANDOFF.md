# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-09-06#2
done: 7 commits `4ac80ef..0ea7a3a` pushed, CI 34013616990 green, **deployed**. `4ac80ef` gh#216 · `1cd4e57` ADR-0051 readback · `87e2f81` gh#215 · `d3f10a2` gh#212 · `0d22c4b` gh#202 gate · `5ad1acb` gh#214 · `0ea7a3a` CI wiring. Local UNPUSHED: `34f3d00` ADR-0060 · `08cc3e5` gh#205 บางกอกดริฟต์ (13th route). What git cannot show: **the WebGL lane ran on a GitHub ubuntu runner for the first time and passed** — the sole risk named at the push ask, previously Mac-only · **gh#212's gate did not stop gh#212's own bug** and REFUTE caught it: the registry held 7 `markup.html` entries and ZERO `main.js`, so `one-bomb`/`pinocchio-luck` dry-runs exited 0 while announcing they would rewrite `main.js`, destroying ~25 hand-added lines incl. the ADR-0051 blanking fix; refusal now keys on an extractor-owned file whose bytes change, escape is `--force <owner>` (missing owner exits 2), recorded fragments stay unforceable, and **every existing route now needs the flag** since none is byte-identical to a fresh extraction · **the halt fix stranded buttons permanently** — `cloneNode` copies `disabled` and the arm gate reads a disabled control it does not own as caller intent; reproduced in a real browser, `#homeBtn` never enabled across 3s · **the engine resolves HUD nodes by id AT WRITE TIME, not captured refs**, so cloning nodes would have handed the zombie the clone — the seam is severing the id, not the node · zombie window is **1.72s not 1.3s** and it reaches assistive tech, and the arm gate never masked it (`ARM_DELAY_MS` 400 vs a 420 threshold; the gate disables buttons, not text writes) · **gh#150 stage D PROVEN for the first time** — under SwiftShader a canvas press moved `ผู้เล่น 1`→`2` and `1 ใน 25`→`1 ใน 24`, a press on empty canvas changed nothing · **gh#182 is NOT agent work** — the ticket self-escalated to six per-route owner decisions; three of its recorded numbers still do not reproduce · **gh#184's cap was already shipped and its probe already wired** (`EXPECTED_LEGS` 24→26 for gh#210), my brief carried three stale premises and the worker REFUSED, correctly · **เข้งับ cannot be ported**: `index.html` is 1,276B with zero inline `<style>`, a Vite entry over 9 modules, 4 importing `three` · gh#214's width walk found failures NOT where the ticket predicted — `short-stick` screen 2, four remove buttons at 27.2px wide — while the 29.8px case the ticket names stays UNMEASURED, and 34 of 216 controls carry a 1:1 aspect ratio so the two axes are not independent · **ADR-0033 has no gate** — nothing in `scripts/` or CI references `design/` · `BASELINE_TOTAL_BYTES` deliberately NOT re-pinned for the new route (+3.76%, inside the band) so gh#206's drift stays visible.
dec: owner 2026-09-06 — gh#202 font ships NOW, the gh#206-first condition retired (its premise was false: the freeze counts only JS chunks reached from `<script src>`, so a font never entered it) · gh#212 Q2 re-extraction CONTINUES · gh#214 measure width, report, do NOT gate · gh#213 naming parked, build the rest · push main accepting the WebGL-lane risk · **`three` vendored — reaffirmed after being shown 498,672B against a 464,658B site baseline** → ADR-0060 · บางกอกดริฟต์ JUMPS THE QUEUE · both rulings recorded on their tickets. Mine: split gh#202 into gate-now and font-binary-later, since `public/` ships verbatim · re-baselined only the SET constants, never the total · filed the escaping audit's per-file granularity rather than widening a security gate mid-review · went back to the owner on `three` rather than building on an authorization given before the measurement existed.
next: [ ] **update design** — ADR-0033 makes the canvas under `design/` source of truth for colour, size, radius, border weight and copy, and NOTHING enforces it; ADR-0050 narrowed it off party-game surfaces. Two systems: `tokens.css` site-wide, but every play route ships its own `:root` and never reads it. Verify: either a gate that reds on a code value absent from the canvas, or an ADR saying the canvas is advisory
- [ ] gh#192's four open owner calls — (b) product-facts strip copy (c) palette (d) nav label and panel order (e) hero mascot image. **The list is in the issue BODY; the last comment is only a (g) correction** — the inverse of this repo's usual rule. (c) is NOT a `tokens.css` edit alone: three surfaces copy the accent and `accent-single-source-check` requires all to move together. Verify: each call answered on the ticket
- [ ] push the 2 stacked commits — one ask, one deploy. Verify: CI green on the pushed SHA and the deploy step not skipped
- [ ] gh#213 เข้งับ — the lift is HAND-AUTHOR; vendoring does not make 9 modules one self-contained file. Naming still parked. Verify: a route directory exists and the porting recipe's gates pass
- [ ] gh#206 attribute the 16,373B from the commit range, or declare it immaterial and close. Verify: the ticket says which, deliberately
- [ ] gh#184 how a player whose chip sits inside the `+N` finds their name — the ruling hands this to the builder as a proposal. Verify: a proposal on the ticket
- [ ] gh#202 the font binary itself — face and licence. The gate refuses a woff2-only subset and a `.ttf` twin is NOT a way out. Verify: a referenced uncompressed face in `dist/` and the coverage gate green
- [ ] gh#181 eight routes, SERIAL by owner ruling, real browser at 1440x900. Prerequisite discharged: the two dialog-tainted numbers re-measured clean and unchanged. Verify: no route left above 0.62
- [ ] `short-stick` four remove buttons at 27.2px wide — newly measured, no geometric excuse. Verify: the width warning is gone for that route
- [ ] file the escaping audit's per-file granularity — its green means SOME hole in a file is escaped, not every one. Verify: a ticket exists with the reproduction
- [ ] `docs/agents/src-edit-rules.md` party-size section is STALE — gh#192's last comment overturned the description exemption. Verify: the section matches the ruling
- [ ] gh#141 rows owed for `one-bomb` and `bangkok-drift`. Verify: both rows present
- [ ] owner standing: gh#9 domain still NXDOMAIN, blocking gh#160 gh#29 gh#12 gh#19 · gh#210 CI-artifact diff · the fortune family gh#97-gh#104 · gh#112 gh#109 gh#106 gh#165
inflight: measured during this save — tree 2 files dirty (THIS save only) · `gh pr list --state open` 0 · unpushed 2, `origin/main` `0ea7a3a` · last CI 34013616990 SUCCESS · open issues 33 · 13 play routes on disk · 1 worktree kept deliberately: `hopeful-fermi-74ec5e`, the OWNER's own; both integrated agent worktrees removed this save. This save is docs-only and does NOT push (ADR-0049).
spent: queue 33→13 · batches 1 push, 22 dispatches, 2 REFUTE rounds on 1 artifact, 3 REFUSED that were all correct, 1 design pass that stopped a build
errors: took a premise from a scout's compression when the ticket said the opposite in one sentence (gh#212 "buildable") · briefed one worker on three stale premises at once, all readable in the ticket's own last comment · read "15-file Vite project" as "portable" when it meant the opposite, and briefed เข้งับ as a normal port · grepped for a token that was not in the artifact three times — a field named `kind` that is `id`, a directory named `one-bomb` that is `Bomb`, a step containing "font" that does not — each returned a clean zero that looked like an answer · quoted a JS string length as a byte count on a Thai-heavy file.

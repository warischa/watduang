# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-08-22#2

done: **2 commits `59c792b` + `261d246`, pushed · CI run 32557799076 green, ALL steps success read from the JOBS endpoint incl. `Deploy to Azure Static Web Apps` = success NOT skipped · open issues 14->12 · tests 184->187** · **gh#62 CLOSED** — the clear button is no longer rendered when `clearsSession` is false, so its dataset gate went with it: a branch that can never take its else is the guard-that-cannot-fail shape, not defence in depth; the fork caught that the obvious null-guard `clearBtn?.dataset.clearsSession !== 'no'` is TRUE when the button is absent and would have failed OPEN, so reads guard reachability instead; box 1 recorded VOID-BY-FIX per ADR-0023 (the fix deletes the gate the box asks to test) and recast structurally, boxes 2/3/4 PROVEN; mutation-tested BOTH ways — reverting the render reds the named assertion, and `if (clearBtn)` -> `if (true)` makes `astro check` fail 3x ts(18047) naming exactly 3 use sites · **gh#61 CLOSED** — the ADR-0024 geometric fix was implemented, measured 0/75, and REVERTED: it left 288-480px of permanent dead space above the tool result at 320px, which is the flip condition the fork itself had pre-registered; the re-fork then rejected all five candidates on OWNERSHIP (scroll anchoring is browser-owned and suppressed at scroll-top, moving GameNav hands the landing-zone set to whoever next edits chrome, touchend-release is a settle window in event clothes that never fires for a mouse user) and chose ADR-0016's classify-nothing window, reusing `ARM_DELAY_MS` rather than copying it; calibrated 3 ways — pre-fix build NAVIGATED to a game page, fixed build did not at 120ms and 300ms gaps, and the same pixel at +800ms navigated again, so the guard is not dead-forever; game pages proven untouched, a link tap 21.3ms after start opened `#leave-confirm` instead · **gh#53 CLOSED** — 5 boxes audited against the artifacts rather than the notes, denominators stated; box 5 is unfalsifiable as literally written ("fails on the current one", and the current tree IS the fixed tree) so it was RE-RUN from `a14fe6b^` plus today's test file rather than trusted from the record: `not ok 24`, actual `stale-version` vs expected `other-round`, control green · **`docs/agents/browser-verification.md` 12224B -> 8888B** — traps split by TASK MOMENT (driving a probe stays, judging a capture moves to `interpreting-browser-captures.md`), original trap numbers kept so inbound citations still resolve; `reduced-motion-verification.md` split out too · **3 line-number citations re-anchored and ONE WAS ALREADY WRONG** — `capture-freshness.md` pointed at two lines that now hold a blank line and a roster read, because gh#50 inserted an `onWriteRefused` assignment and pushed the described calls down; the prose read as correct the whole time, and the citation gate never saw it because it only scans ADDED lines · **gh#65 + gh#66 OPENED**
dec: ADR-0027 — a classify-nothing window closes the tool-page collapse, and ADR-0015's tool-page carve-out is NOT reversed, no dialog was added anywhere · gh#61's gate keys on `gameId === undefined` NOT `clearsSession === false`, deliberately: it is the bit the leave-confirm listener already reads, so the two guards partition every page and a tool page that omits `gameId` still fails safe · that partition does NOT extend to `clearsSession`, which defaults TRUE — filed as gh#65 rather than fixed inline, because it is a shared-component change wanting its own fork · gh#62 box 1 recorded VOID-BY-FIX not PROVEN, since ticking it would claim proof of a mechanism the fix deleted · the 75-point grid probe was deliberately NOT reused as gh#61's pass criterion — under a suppression window the geometry legitimately collides, so the old probe would have gone red on a working fix; the invariant moved from geometric to behavioural · ADR-0026's reopening fact is CONFIRMED, see its outcome section · gh#66 filed rather than fixed, same reason as gh#65
next:
- [ ] gh#65 — `clearsSession` defaults true, so a tool page copied without the prop ships an armed clear button on a page ADR-0004 forbids to touch the session. Three directions in the body; the derive-from-gameId one looks strongest on the ownership test but is a shared-component change and wants a fork. Done when gh#65 closes
- [ ] gh#66 — the two ponytail ceilings that fail GREEN: a narrowed `GAMES_DIR_OVERRIDE` run must be distinguishable from a full one by reading the CI log alone, and a CSP change that outdates `PAIRS_AS_OF` must signal without a human remembering. Done when gh#66 closes
- [ ] owner-run: gh#9 domain -> gh#29 AdSense. `dig +short watduang.com NS` EMPTY, re-verified at this save. Blocks the ONE remaining box on each of #15 #16 #17 #18 — those four tools are BUILT and shipping, do not re-scope them as unbuilt
- [ ] owner-run: gh#13 last box — the real-device script under `docs/verification/` on one real iPhone. An agent CANNOT do this; the simulator tooling reaches simulators only
- [ ] owner: gh#12 keyword planner · gh#19 month-6 organic-clicks gate
- [ ] decide whether the line-citation ban gets an enforcement path or is accepted as a convention. Probed this session: it binds READERS of `docs/agents/issue-tracker.md`, not agents specifically, and NOTHING enforces it on issue bodies — CI triggers on push and pull_request only, no non-sample hooks, and the checker's whole input is a git diff. Rule-text patches converge over readers, never over the set
- [ ] ADR-0012 still describes the traps section as `browser-verification.md`'s payload; it is now split across two files. Low, historical
inflight: measured at save — tree clean apart from this save's own writes · open PRs 0 (checked `gh pr list`) · bg tasks 0 (checked) · HEAD `261d246` level with origin/main before this save's commit
spent: queue 8->7 (3 closed, 2 opened) · batches 3 · dispatches 10 · fable 3 (1 SOLVE fork + 1 SOLVE re-fork + 1 REFUTE, no second REFUTE round needed) · GitHub writes 2 issues + 3 close comments + 3 closes · ctx ~250k at save

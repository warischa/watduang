# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-09-14#1
done: shipped `5274043` gh#232 · `181a8ee` gh#237 · `164215a` docs; pushed `037d054..164215a`; closed #232 #237 #236 #214 #210 #184 with evidence comments; MANUAL `az`+swa deploy to production rc=0 (owner-authorized, reason given "gh at a limit"). What git cannot show: **the RH queue was ~90% not-work** — of 23 plan rows only 2 were agent-actionable, the rest owner-gated or externally blocked, and three (#237 #236 #214) were already DONE and open only because nobody closed them · **a delegated liveness sweep was wrong on 2 of 22 rows** — it anchored #182 and #184 on 2026-09-08 comments that later ones had reversed, the exact failure CLAUDE.md documents for #182, reproduced by an agent · **#210 and #234 shared a shape nobody had named: the blocker was an artifact nobody had rendered**, not a decision anyone was withholding — one render closed #210 and unblocked #184 · **the deploy was byte-identical to what was already live** (`diff -rq` clean vs a rebuilt `037d054`), because every change this batch lives outside `dist/`; I proposed it without checking that first · **the owner's stated reason for going manual did not hold** — the push triggered CI run 34803799092 anyway, Actions was not blocked · **a builder's REFUSED was correct and cost nothing** — a test pinned the behaviour the ruling overturned while my brief forbade touching it · REFUTE returned 5 findings, all `[spot-fix]`, cap untouched at 1 of 2.
dec: #232 = probe DELETED, runtime arm question deliberately unmeasured (owner 2026-09-14) — branch A was priced first: 8 function rewrites across short-stick+timebomb, siamsi dropped for having no play route, 2 CI legs, and short-stick's missing pass-screen equivalent left honestly UNSIZED | #237 = an unreadable shipped face FAILS (owner), not disclosed-and-passed, not narrowed to the stylesheet's chosen face — that set is CSS's and the browser's | #210 box 2 = truncated long name LEGIBLE on all three routes (owner verdict; no probe can emit this) → #184 closed on its own written condition | #234 = fix the COMMENT only, not the draw order (owner) — nobody established fogged obstacles read better at 375px | font gate zero-font escape = a vanished inventory must FAIL (owner), distinct from a build that legitimately self-hosts nothing | real-phone queue unchanged, no waiver | ADR-0018 needs no follow-up — it is general policy, never named the deleted probe, and its own consequences say each probe needs its own decision.
next: [ ] gh#234 fix the comment so it matches the draw order, per owner ruling — fog stays where it is, MIN_GAP untouched, gh#204's ruling not reopened. Verify: the comment and the renderer agree, and the ticket records which way it was resolved and why
next: [ ] font gate — make a vanished inventory FAIL, per owner ruling. Distinguish "no self-hosted font at all" (must stay green) from "EXPECTED_STEMS members all absent" (must red); the test pinning the skips-loudly behaviour must change with it. Verify: a tree with the four expected faces deleted reds, a genuinely font-free build still exits 0, both calibrated
next: [ ] gh#182 remainder — freeze-tap owner-reserved and untouched; every other route ruled or fixed. Verify: the ticket records freeze-tap as the only open row
next: [ ] gh#101 needs owner Thai copy — single blocker for the whole fortune family; gh#102 portraits cannot start because its ten cards do not exist. Verify: copy lands and the cards ship with the portrait box empty
next: [ ] fortune family remainder gh#104 gh#106 gh#109. Verify: each ships Thai copy and the owner reviews strings
next: [ ] gh#181 gh#219 gh#210-adjacent leftovers — desktop phone-width column, the one-shot overflow counter's box 1. Verify: each records a MEASURED reading, never a calculated one
next: [ ] gh#141 real-phone walk owes gh#219 box 1, gh#205 box 6 rows 1-2, gh#204 box 7, croc-bite's 1976ms handled-pointer-gap row. Verify: each records a reading
next: [ ] gh#235 croc-bite's 1976ms CI pointer gap still unexplained — needs several runs, not one; one run is a sample. Verify: an explanation with evidence, or a measurement across runs showing it does not reproduce
next: [ ] owner physical blockers unchanged — gh#9 no A record no NS, blocking gh#160 gh#29 gh#12 gh#19. Verify: A record resolves
inflight: measured during this save — `git status --short` clean apart from this save's own files · `gh pr list --state open` empty, checked · `main` = `origin/main` at `164215a` · CI run 34803799092 was still `in_progress` at save time, NOT waited on — production already carries these bytes via the manual deploy, and they are identical to the previous deploy's · production verified live: home/play-route/sitemap all 200, Thai brand renders · rollback worktree removed, `git worktree list` shows only the main tree · ports 4178/4341/4322/9222/9339/9341 free, checked · no background agents.
spent: queue 23→9 · batches 5
errors: **an instrument reported something other than what I asked, four separate times** — a zsh glob made `rc=1` read as "no cap found" when it meant "no match for `*.css`"; a failure-grep matched `failed=0`; a delegated sweep returned confident wrong dates; a truncated issue list hid a row · read a CLOSED ticket (gh#213) as the open one, because titles here carry N−1 · overwrote `orchestrate-ledger.md` instead of the session-id form, contents unrecoverable · briefed the wrong CSS class for one route, the agent caught it · scoped a behaviour change without checking which test pinned the old behaviour, earning a correct REFUSED · **proposed and executed a production deploy without first checking whether the diff could reach `dist/` at all — it could not**, and the deploy was a byte-identical no-op.

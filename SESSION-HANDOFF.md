# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-08-30#1
done: merged+deployed `59c909c` (จับไม้สั้น + ระเบิดเวลา play routes) `b1ce157` (gh#132 tool-name race) `ce51ea7` (gh#75 home hub) `5a4c02c` (escape names in 3 play routes) `b4be63c` (probe route derivation + gitignore + bundle re-baseline) `14627d2` (Azure redaction + gh#80 pin) `d676170` (one withLock + widened gate) `ab10a97` (จับไม้สั้น canvas). What git cannot show: TWO deploys green, runs 33287503141 + 33288306530, each verified by me on the live site independently of CI — 6/6 paths HTTP 200 and `<canvas>` present in the delivered HTML of both new play pages. CI's Actions billing block was cleared by the owner making the repo PUBLIC; proven before trusting it by pushing an existing commit to a throwaway branch (45 steps ran vs 0 on the blocked run, all three Azure deploy steps `skipped` because they gate on `refs/heads/main`), branch deleted and absence verified. REFUTE round 1 on the integrated diff found player names reaching `innerHTML` unescaped; I widened it from the 1 file it named to the CLASS — 3 of 4 play routes, and power-meter + cannon-flag were ALREADY LIVE — closed with a 14-mutant sweep, 14 red 0 survived. Backlog was 42% stale: 8 of the 19 planned rows were already shipped, blocked, or owner-reserved, and only 2 were genuinely undone code. gh#143 gh#120 gh#114 gh#14 closed with evidence comments read back intact; gh#150 opened for the Bomb mockup; gh#141 now carries 5 game rows.
dec: [ADR-0051](docs/adr/0051-a-play-route-draws-with-canvas-2d-never-webgl.md) accepted — a play route draws with Canvas 2D, never WebGL, and a drawing route owes a calibrated pixel-readback check · owner 2026-08-30: ระเบิดเวลา keeps its own engine and gets a redesigned canvas play route, `Bomb` ships as its own game (gh#150) rather than replacing it, and จับไม้สั้น gets the same canvas treatment · gh#147 สุ่มคนโดน has no mockup and routes to gh#139 · probes derive their route set from the manifest, never a hardcoded list · gh#132's deletion-resurrection is an accepted documented ceiling, not a bug to fix.
next:
- [ ] agent: string-extractor seam — 15 of 16 modules under `src/games/` still inline their Thai; only `pick-loser.ts` exports COPY. Verify: that count falls, and `npm test` still passes
- [ ] owner: gh#139 decides which of the eight unshipped mockups ship — สุ่มคนโดน (gh#147) and `Bomb` (gh#150) are both parked behind it and nothing in that track moves until it lands
- [ ] owner: gh#141 five games now need a real phone in a real hand · gh#29 AdSense has no account · gh#9 `watduang.com` is still unregistered so the site lives on its Azure URL — one sitting
- [ ] agent: gh#149 stays blocked — deleting party-game landings needs every party game on a play route, and สุ่มคนโดน has neither route nor mockup
inflight: measured at save — tree carries only this save (handoff, archive, ADR-0051, memory) · 0 open PRs (checked) · no bg tasks · `main` == `origin/main` at `ab10a97` before this docs push · a `read-only-session-*` worktree from resume remains, not this session's to remove
spent: queue 5 -> 4 · batches 2 · ctx 471k at save
errors: briefed a port on a mockup that is a different game, having verified only its title, while applying a real mechanic read to its sibling · let a brief contradict its own ticket's delete box, sourced from the fork instead of the ticket text · told worktree agents to symlink `node_modules`, and `git apply` is not atomic, so it deleted four files before failing on that symlink · wrote three probes that could not measure what they claimed — `$?` read after a pipe, a planted violation of the wrong shape, and a grep matching the comments documenting the very rules it screened for.

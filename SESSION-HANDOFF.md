# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-09-18#6
done: `9bb1c43` — What git cannot show: **the top `next:` item was already moot and neither the handoff nor the ticket said so** — gh#203 closed 2026-09-05 on an owner ruling that the narrow centred column is the intended composition, and its closing comment lists `dice-loser` and `timebomb` by name inside the group that ruling accepts, so a cleaner span cannot move any decision · **a clean-screen run for exactly those two routes has existed since `eca92a8` (2026-09-11), tracked, and no gh#181 comment ever referenced it** — which is how the 2026-09-18 comment could report the rider outstanding without mentioning it; it reads board width (`dice-loser` 662 x 443, `timebomb` 560 x 414), not span, and says so itself · **the instrument's own unit filter counts a visible out-of-flow box carrying text as a composition unit and states a labelled modal would widen the span**, so the dialog-open readings are biased upward and the clean values sit lower still, further inside the accepted group — inferred from the filter's definition, never measured · **gh#203's closing comment and its own committed readings file disagree on three routes** — the file reads `cannon-flag` 0.132, `short-stick` 0.511 and `zero-trigger` 0.372 against that comment's 0.333, 0.622 and 0.989; the other eight agree exactly, and neither record says the other exists · `scripts/play-screen-fit-probe.mjs` DOES default-export a driver probe, against the capture script's header comment saying its internals are unavailable — running it is what lands on the dialog, which is the real obstacle · `docs/verification/evidence/181-clean-screen/clean-screen-probe.mjs` computes no span at all, so option (a) would have needed new probe code · gates: full `scripts/run-workflow-gates.sh`, 46 of 48 steps executed, 0 failed, 2 structurally not runnable here (the SWA deploy-token fetch needs `az`; the CI probe-scope template step).
dec: gh#181's `dice-loser`/`timebomb` rider closes by RELABEL, not re-measure (owner 2026-09-18, popup) — one bullet added to `docs/verification/evidence/203/composition-readings.md` § "What these readings do not cover", recording the disposition, the pointer to the clean-screen run, and the bias direction labelled as inferred | no number in that table was edited | `bin/ca` fallback rows are DROPPED from the queue permanently (owner 2026-09-18, popup) — ruling recorded here, not on any ticket; it was never a ticket item | gh#102/gh#103 portraits keep waiting on the owner's card edit — sequencing reconfirmed, not changed | the three-route span disagreement is RAISED, not resolved — which set the 2026-09-05 ruling used is unknown and is not guessed at | no box ticked or unticked on any ticket.
next: [ ] gh#203 — record the three-route span disagreement on the ticket: the readings file reads `cannon-flag` 0.132, `short-stick` 0.511, `zero-trigger` 0.372 against its own closing comment's 0.333, 0.622, 0.989, eight of eleven agreeing; both sides read from the tree and the ticket this session, premise sourced from the comment dated 2026-09-05. Verify: one comment on gh#203 naming both sets and which one the ruling's text used, posted and read back
next: [ ] H: gh#101 owner edits the draft in the tree — `docs/copy/nuea-khu.md` and `docs/copy/nuea-khu/cards.md`, both carrying `⚠ UNREVIEWED`; the agent half is done. Verify: neither file still carries the UNREVIEWED banner
next: [ ] H: gh#9 owner registers `watduang.com` — owner-only and financial, never an agent action; re-run whois immediately before paying, privacy on, auto-renew on; unblocks the gh#160 gh#29 gh#19 line below, which carries them as their own row. Verify: `dig +short watduang.com A` resolves
next: [ ] H: gh#141 walk all 20 boxes on a real phone; gh#204 boxes 7-9, gh#205 boxes 1-2 and gh#219 box 1 need the same device and the same pass. Verify: every box noted with its device
next: [ ] H: gh#104 item 1 — the ดูดวง หมวด deck rewrite, 23 prompts and 5 predictions; owner-reserved Thai copy under the 2026-09-10 ruling, overlapping gh#97 gh#98 which already own เซียมซี's deck. Verify: the deck no longer promises a group
next: [ ] H: gh#109 is owner-only end to end — boxes 1-2 are Thai copy and boxes 3-5 are already satisfied per its 2026-08-28 comment; there is NO agent slice. Verify: nothing; do not brief it as mechanical work
next: [ ] gh#102 gh#103 เนื้อคู่ portraits — the ten cards exist at `docs/copy/nuea-khu/cards.md` but are UNREVIEWED; read `docs/agents/assets.md` before spending. Verify: ten portraits put to the owner · Blocked · ask: owner edit the ten cards and drop their `⚠ UNREVIEWED` banner, then go
next: [ ] gh#160 gh#29 gh#19 items 2-4 — none is independently executable and all three wait on the same domain registration; gh#160 is Cloudflare Web Analytics, chosen and not wired. Promoted to its own row 2026-09-18 — it had been buried in the tail of the gh#9 line, which is the shape the master rule now forbids. Verify: per ticket, once the domain resolves · Blocked · ask: unblocks when gh#9 lands — nothing to decide before then
next: [ ] H: gh#12 needs an owner Google Ads account, NOT the domain — its body says `Blocked by: —`. Verify: Keyword Planner volumes recorded under `docs/research/`, never `.scratch/`
inflight: measured during this save — repo `git status --short` clean apart from this save's own files, `gh pr list --state open` empty, no worktrees, no background tasks. `origin/main` = `5ecd439`, HEAD 21 ahead before this save, all docs commits staying local per ADR-0049, this save's included. `~/.claude` was NOT touched this session and stays dirty with 14 entries this session never made; `b198e4a` from the previous session is still its HEAD-side fix.
spent: queue 5→3 · batches 1 · dispatches 0 — counted as the last RH run's Execute Plan row count per the master rule that landed 2026-09-18, not as the handoff's own prose. That run rendered 5 rows: surface the stranded gh#181 evidence, close the gh#181 rider, the gh#102/gh#103 portraits, the `bin/ca` rows, and the gh#160/gh#29/gh#19 cluster. Now 3 — both gh#181 rows closed this session, `bin/ca` was dropped by owner ruling, the portraits and the cluster stay open, and the gh#203 span disagreement opened. The earlier 3→2 counted the previous entry's prose and was wrong under the new rule.
errors: **built a plan on a live-measurement premise without reading the closed ticket that governs the number** — `docs/runbook.md` § "Read closed issues before opening a new question" names this exact failure and was not consulted until the popup was being drafted; gh#203 had been closed on the point since 2026-09-05. Root cause: the `next:` line named gh#181's comment as its source but never the decision that would consume the measurement · **asserted a file's capability from a neighbouring file's comment** — said `scripts/play-screen-fit-probe.mjs` does not export its internals, taken from the capture script's header rather than from the file; it default-exports a driver probe. Caught and corrected before the popup, because a false cost would have biased the owner's choice · **seven RH round trips against a ≤3 target**, all of them spent rediscovering evidence the handoff already could have pointed at.

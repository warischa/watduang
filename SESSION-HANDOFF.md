# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-09-26#1
done: RH 2026-09-24 FRESH on both axes; no owner answer arrived (gh#103's last comment still `5789453941`) · **outside this repo:** `~/claude/Skill-Dev/3D` commit `e67dd99` on `master`, which has no remote — `threejs-web` gains `rules/stylized-look.md` + `references/stylized-starter.html`, rendered in the browser pane at desktop 15 calls / 8 programs and the `mobile` preset 10 / 5 with no console output; 12 new `scripts/probes/three/verify.sh` checks green on 0.186.0, must-red against this repo's 0.170.0 fails exactly `transmissionResolutionScale` and `Timer.connect`; `selfcheck.sh` 7/7, `plugin validate --strict` passes; installed `3d-studio` 0.1.3→0.1.4, cache `skills/` identical to the repo (`diff -r`); that repo's pre-existing dirty `SESSION-HANDOFF.md`, `docs/adr/0001-project-layout.md` and `docs/sessions-archive.md` left untouched, not in the commit · 3D-upgrade survey of all 20 entries (16 in `src/games/manifest.ts`, 4 in `src/tools/manifest.ts`): `three` importers = 1, `src/play/croc-bite/main.js`, which runs `PCFSoftShadowMap`, a DPR cap of 2 with no phone tier and no environment map (grep 2026-09-26) · a temporary `.claude/launch.json` for the preview server was created and removed.
dec: `threejs-web` look guidance = a rules file, not a new skill · depth = recipe + runnable starter · ship = bump 0.1.4 + update the plugin + local commit (owner, popup 2026-09-26 — recorded here, not on any ticket) | reference `ohmiler/opus-5.5` has no license (GitHub API `license: null`) → techniques and values only, recorded as #11 in that repo's `REF-INDEX.md` | still standing: blind-test bar proposed on gh#103 comment `5789453941`; orchestrate-qwen commit-only; gh#9 binding by the owner (S2026-09-23#6, archived).
next: [ ] croc-bite tooth taps vs the arm gate — reproduce first: a survey-agent claim of 2026-09-26, not verified, that tooth taps resolve by raycast on `pointerup` with no arm window, outside `armAllButtons` (ADR-0057, ADR-0059); a chip for it was spawned 2026-09-26. Verify: the route's `arm-reveal-paths.test.mjs` gains a tooth-tap case that fails on the unfixed build, or the claim is shown false with the handler named
next: [ ] gh#103 box 3 — score the owner's ten picks with `blind-103/make.py --score` (outside git, in `~/.claude/projects/-Users-waris-c-claude-free-game/`), commit `sheet.jpg` + `key.json` + the score under `docs/verification/evidence/` (read `docs/agents/assets.md` first — a binary); **`key.json` must not reach git before the picks, it publishes the answer**; post the result against the bar in comment `5789453941`. Verify: the score and its guessing probability quoted on gh#103 · Blocked · ask: the owner's ten tile numbers, and the bar accepted as proposed (a) or changed (b) — `sheet.jpg` (no key) was sent to the owner in chat 2026-09-23 and unanswered at this save; no pick exists on gh#103 or in chat (a reviewer who has never seen the deck is the stronger test)
next: [ ] croc-bite visual upgrade per `3d-studio:threejs-web` `rules/stylized-look.md` — PMREM environment, blob shadows in place of its shadow maps, a lite tier for phones (premise: grep of `src/play/croc-bite/main.js` 2026-09-26). Verify: screenshots at 1280x800 and the `mobile` preset, lite-tier `renderer.info` calls below desktop, the route's `arm-reveal-paths.test.mjs` green, `bundle-freeze-check` green or re-pinned under a recorded ruling · Blocked · ask: go for an src change whose added `three` code may leave the ±5% band in `scripts/bundle-freeze-check.mjs` — (a) go, re-pin under a new ruling if red, (b) not now; runs after the tooth-tap row (same file)
next: [ ] 3D for siamsi, dice-loser, timebomb, short-stick, wheel — ADR-0060 admits `three` for เข้งับ only and gh#206's growth ruling is "ONCE" per `scripts/bundle-freeze-check.mjs`; a second `three` route splitting a shared chunk and redding all three freeze legs is the survey's inference of 2026-09-26, not measured. Verify: a new ADR naming the routes and a recorded bundle ruling, then per-route screenshots at both tiers · Blocked · ask: open `three` beyond croc-bite — (a) new ADR, (b) croc-bite only
next: [ ] gh#101 — build the page from the signed-off drafts, per the ticket body's "What to build"; re-render any portrait whose card's gender, age, build, occupation, manner or mark the owner edit moved, with the command in `images/IMAGES.md`'s gh#103 section; **also carries gh#104's conditions for this game's own copy** — gh#104 closed while `love-match` was unregistered, so its `seo.*` was never evaluated and `love-match.ts` carries one `เกม` (grep 2026-09-23); `public/og/love-match.png` is the OG gate's named exemption and reds the gate on registration. Verify: the ticket's seven boxes, each checked the way its own wording says, and `docs/verification/evidence/104-2026-09-23/enumerate.mjs` re-run after registration with no new flag · Blocked · ask: owner edit — consumes H: gh#101
next: [ ] gh#103 boxes 4 and 6 — move the accepted portraits into the publish surface at the size the page loads, and state what one reader downloads; no route for this game exists under `src/pages/`, per gh#103's comment of 2026-09-23. Verify: each deck file referenced by a card on the built page, and the per-reader byte count in the closing comment · Blocked · ask: unblocks when the gh#101 page exists — nothing to decide before then
next: [ ] gh#9 close — after H: gh#9, verify and close with an evidence comment. Verify: `dig +short watduang.com A` resolves, the owner reports both SWA Custom domains rows `Ready`, `https://watduang.com` serves the site · Blocked · ask: unblocks when H: gh#9 lands — nothing to decide before then
next: [ ] gh#160 — Cloudflare Web Analytics, chosen and not wired; its other blocker #159 is confirmed dead. Verify: per ticket, once the domain resolves · Blocked · ask: unblocks when H: gh#9 lands — nothing to decide before then
next: [ ] gh#29 — ad slots need the domain AND an owner AdSense account, site review and publisher ID; two owner acts, not one, per its own body. Verify: publisher ID recorded and ad code live · Blocked · ask: unblocks only after gh#9 AND the owner's AdSense account — do not brief it as domain-only
next: [ ] gh#19 — month-6 organic-clicks gate; needs the domain, Search Console connected to it, and six months of traffic. Verify: organic clicks recorded at month 6 · Blocked · ask: nothing to decide until the domain and the traffic window both exist
next: [ ] orchestrate-qwen, outside this repo — 4 commits ahead of `origin/master`, `7b927ac` among them (rev-list 2026-09-24); `SKILL.md` 14,940B (`wc -c` 2026-09-24) against the 12KB default, heaviest `## Dispatch` 6,118B (`check-budgets.sh`, this save) → move detail into its `reference/`. Verify: 0 ahead and the budget check no longer FAILs · Blocked · ask: the owner took commit-only 2026-09-23 — push (a), the cut (b), or leave (c)
next: [ ] H: gh#101 owner edits the four drafts in the tree — `docs/copy/nuea-khu.md`, `docs/copy/nuea-khu/cards.md`, `docs/copy/nuea-khu/cards-m6-m20.md`, `docs/copy/nuea-khu/cards-f6-f20.md`; review aid `review-101/cards.html` + `checks.md`, outside git beside `blind-103/` (comment `5790035652`): ten cards aged 42-54 and four non-Thai nationalities flagged, no duplicate, every card mapped to a portrait. Verify: `grep -c '⚠ UNREVIEWED'` reads 0 on all four, measured 1 each 2026-09-24 (RH)
next: [ ] H: gh#9 owner registers `watduang.com` AND binds the apex and `www` in the SWA portal per comment `5789453647` (ruling 2026-09-23 recorded here, not on gh#9) — owner-only and financial, never an agent action; re-run whois immediately before paying (`No match` 2026-09-23, again 2026-09-24), privacy on, auto-renew on, and check the registrar offers ALIAS/ANAME/flattening at the apex. Hand back: `dig +short watduang.com A` resolves and both Custom domains rows read `Ready`
next: [ ] H: gh#141 walk every box on a real phone with `docs/verification/gh141-real-device-script.md` — 20 boxes incl. gh#204 boxes 7 and 9 (one measurement, the frame rate), gh#205 box 1, gh#219 box 1 (comment `5790035284`); gh#204 and gh#205 carry NO agent slice per their 2026-09-21 triage comments, gh#219 per its 2026-09-15 comment — do not brief them as code work. Verify: every box noted with device and OS on its own ticket
next: [ ] H: gh#12 needs an owner Google Ads account, NOT the domain — its body says `Blocked by: —`. Verify: Keyword Planner volumes recorded under `docs/research/`, never `.scratch/`
inflight: measured during this save — `gh pr list --state open` empty; one worktree, the main checkout, branch `main`; no background tasks; HEAD `9940c6f` is 1 ahead of `origin/main` and this save commit stacks on it, both local, riding the next src push per ADR-0049; tree dirty only with this save's two files; `~/claude/Skill-Dev/3D` clean apart from its three pre-existing files.
spent: queue 8→11 · batches 1 — 11 open non-`H:` items, enumerated: croc-bite tooth taps, gh#103 box 3, croc-bite visual upgrade, 3D for the other five, gh#101, gh#103 boxes 4 and 6, gh#9 close, gh#160, gh#29, gh#19, orchestrate-qwen. Nothing closed; three added.
errors: **(1) dependent actions issued as if independent** — an Edit whose anchor ended mid-statement put a comment inside a `const` declaration, and a browser batch sent alongside a later failing Edit rendered the unfixed file; both caught by the console read, three wasted render rounds · **(2) an instrument read on a stalled subject** — `renderer.info` read while the preview pane was hidden returned `1`, then `4` (loop paused, camera never positioned) before `document.hidden` was checked; the gotcha now sits in `rules/stylized-look.md` · **(3) a claim wider than its probe** — that repo's `REF-INDEX.md` said the property "does not exist before 0.186" from two sampled versions; corrected before commit.

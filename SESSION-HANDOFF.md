# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-09-10#2
done: pushed `41b47c3..9cb76b1` in two deploys — `bbe338b` `bd5d22b` `ea0cf12` `b5367e6` then `6174b52` `3fe89e7` `9cb76b1`; local and UNPUSHED: `6b25599` (croc-bite re-land) `70b0660` `133e413` (gh#214 re-land) `1df350a`; gh#230 closed COMPLETED, five fortune labels flipped to ready-for-agent, comments recorded on gh#150 gh#181 gh#182 gh#204 gh#214 gh#230 gh#231. What git cannot show: **FOUR agent REFUSALS each killed a defect in MY brief, and every one was right** — gh#214's ARIA premise was false (zero `grid`/`gridcell`/`row` authored anywhere in the tree, verified with two grep shapes, so the exemption would match the empty set its own ruling forbids); gh#181 was briefed off the ticket BODY while 15 unread comments had reversed its scope (owner ruled all eight routes, and named two of my four explicitly out of scope), struck its metric (`widthFillPct` is report-only, moved 89.2→100 across consecutive runs), and forbidden its 320px half (a gutter is allowed) — executing it would have deleted a wrapper margin whose own comment records the ±9px particle edge-crossing it prevents; a fourth refused my gh#214 reach-fix brief outright because its timing premise was false, and gh#150's ruling had two halves in tension (the engine exposes NOTHING — `window`/`globalThis`/`export`/`CustomEvent` all zero against a must-red fixture returning 2 — and `renderScene` draws no tiles in `MENU`) · **my own CI diagnosis was wrong**: I called the gh#214 red a slow-runner timing problem, twice, confidently; the real cause was `if (engineIsDead()) installNoWebglRound()`, so on a live context the cells were never installed at all · **CI's runner HAS a live WebGL context, now MEASURED in-page** (`IN_PAGE_CONTEXT webgl2=live webgl=live experimental-webgl=live 2d=live`) — a claim asserted in THREE artifacts and false in all three, and the diagnostic now prints every run with its own control leg · **gh#150 was already done** when the queue called it build work: Stage B registered, Stage C's readback measures one-bomb, clean leg PASS + stub-control FAIL · **gh#182's "99px and 76px below the fold" are the buttons' HEIGHTS** — real distances 45px and 10px past the 568 line, both with 0 visible px, containers scroll so unseen not unreachable; its recorded 411px block is the LUCKY load, worst 438px on 2 of 6 · **gh#204's `drive-four-kinds.png` never showed four kinds** and box 2 is geometrically unsatisfiable at phone width (`MIN_GAP=8` forces ≥120m, sprite half-width `1.6·(W/2)/z`, cone 7×10px at 375×812) · **one-bomb's play route has ZERO anchors while timebomb has 2** — no crawlable outbound link on an SEO business model · **`jq '.body|length'` counts CHARACTERS and `wc -c` counts BYTES** — every read-back this session compared two units; re-checked in bytes, all byte-exact · **the bundle pin went green while 196 bytes short** of the integrated tree, the same trap croc-bite's own re-land had just fixed at 253 bytes.
dec: gh#214 grid cells exempt keyed on the ARIA role the element declares, then on the premise proving false — author the roles FIRST, accepting a markup change across routes, changed screen-reader announcement, and `gridcell` needing `row`+`grid` ancestors | gh#230 raise the declaration to `[2, 20]` rather than lower the clamp, accepting a changed OG count line and party-size-claim-check classification, because lowering risks the saved-group data loss the write-back ceiling exists to prevent | ดูดวง copy direction = build to what gh#97 and gh#99 already specify, no tone spec written, accepting that the strings still need the owner's eye and may be written twice | gh#204 box 2 becomes a claim about the GENERATOR (recorded seed + table + histogram, frame as illustration), `MIN_GAP` NOT changed, accepting that a phone player never sees four distinct kinds | gh#181 box 1 threshold struck — the per-route verdict against the codified scheme IS the criterion, no replacement number, box 2 still owner-reserved under two conflicting rulings | gh#182 answered by ONE general rule for all six: the primary control must be above the fold at 320px, accepting this may be unachievable on freeze-tap's 438px worst-load card without cutting content | gh#150 option A — repo-owned code only, engine parked in `MENU`, accepting NO 3D stone board and NO engine audio for anyone with a GPU (most real phones); option B (upstream hook exposing tile screen-rects) remains the upgrade path, option C declined for reversing the 2026-08-30 WebGL-stays ruling.
next: [ ] push the four local commits — croc-bite goes LIVE as a new game plus `three` bare-pinned at 0.170.0, bundle total 1049872, gates green at declared=47 executed=45 failed=0. Owner ask, not pre-authorized. Verify: CI green at the push sha with Deploy success
next: [ ] check CI's own bundle-freeze total against 1049872 and re-pin from CI's number if it differs — croc-bite's re-land says believe CI over the local figure. Verify: the pin equals what CI prints
next: [ ] gh#231 box 4 closes on that same CI run — croc-bite measuring rather than UNMEASURED is the observable the whole diagnosis was made against; then close gh#231. Verify: the run shows croc-bite measured, and the ticket closes with that evidence
next: [ ] croc-bite tracker work once live — flip gh#213's label and add the gh#141 row per shipped game. Verify: both recorded
next: [ ] the one-bomb anchor gap — count anchors across EVERY built play page before calling it one route's omission or fleet-wide drift; two routes were compared, not the fleet. Verify: a per-route count exists
next: [ ] `docs/agents/ci-verification.md` sits at 12277B against 12288B — ELEVEN bytes. It needs a real split or a declared budget, never another prose trim. Verify: the file is under its ceiling with headroom a trap can be added to
next: [ ] the control-floor probe's post-trigger guard fails OPEN — a CDP evaluate landing mid-navigation resolves as a plain null, the error side is never read, and the walk then scores the landing page's controls under the play route's name. Found by gh#231, likely its own ticket. Verify: a null read reds instead of proceeding
next: [ ] the fortune family is unblocked and five labels are ready-for-agent — gh#97 gh#98 gh#99 gh#102 gh#103; gh#101 still waits on 102 and 103, gh#104 item 1 rides 97 and 98, gh#106 box 3 reconciles with 97, gh#109 boxes 1 and 2 are the same class on the tool pages. Verify: each ships its Thai copy and the owner reviews the strings
next: [ ] gh#182's remaining four design calls under the new general rule — freeze-tap and wire-snip-panic have measured evidence, pinocchio-luck short-stick how-close-is-near zero-trigger do not. Verify: each of the six records a decision
next: [ ] gh#150's four follow-ups, none blocking — haltOnContextLoss is now stricter than the round needs, the `disabled`-clearing decision in the sever has no test pin, reduced motion reaching the engine's own flag is INFERRED not confirmed, one NOT_SEVERED reason string is stale. Verify: each recorded or fixed
next: [ ] gh#181 boxes 2, 5, 6 and 8 stay owner-reserved — box 2 under two conflicting rulings, box 5 blocked on gh#182, box 6 possibly gate-measurable now that gh#220 shipped, box 8 the owner's own look; and dice-loser and timebomb need re-measuring on a clean screen because their readings were taken over a reset dialog. Verify: each records a decision
next: [ ] owner physical blockers unchanged — gh#9 has no A record and no NS, blocking gh#160 gh#29 gh#12 gh#19; gh#141's real-phone walk still owes gh#219 box 1, gh#205 box 6 rows 1-2, gh#204 box 7, gh#210 box 2. Verify: each records its reading
inflight: measured during this save — `git status --short` shows only this save's two docs · `gh pr list --state open` empty, checked · deployed and LIVE at `9cb76b1`, CI success, Deploy to Azure success · FOUR src commits local and unpushed, awaiting the owner ask · nine agent worktrees removed in this save's cleanup · NOT verified: the deployed pixels, and CI's own bundle-freeze figure.
spent: queue 12→12 · batches 4
errors: briefed gh#181 off the ticket body alone while 15 comments held rulings that reversed its scope, its metric and its 320px requirement — `gh issue view --json body` does not return comments, and confirming the declared blocker had closed felt like grounding the premise · diagnosed the gh#214 CI red as slow-runner timing and told the owner so twice with unearned confidence, when the board was never installed on a live context at all · relayed an owner ruling into gh#230's brief without recording it on the ticket first, so the worker correctly refused to close on a ruling that existed only in conversation · compared `jq '.body|length'` against `wc -c` for every published read-back, two different units, and called the deltas newline normalisation · wrote a brief asserting a chrome outbound link and an ad slot on a route that has neither · let the bundle pin ship 196 bytes short of the integrated tree until a direct measurement caught it.

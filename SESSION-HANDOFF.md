# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-09-21#1
done: no src commit — deliverables were a push, three tracker comments and one investigation. Pushed `5ecd439..fdb9367`, the 29-commit docs/images stack, first push in three sessions; gates run over that exact range first (`added-lineno-citation-check` 9 files / 928 added lines rc=0, `check-citations` rc=0, selftest confirms it fails closed rather than green on an empty range). Deleted merged branch `feat/croc-bite-engine` (was `6756330`, `--merged main` confirmed — the check `-d` does NOT make, it compares upstream and this branch had none). Comments `5757382181` gh#204, `5757382463` gh#205, `5757465723` gh#181 — each read back byte-identical apart from GitHub's trailing newline, checkbox counts identical before and after on all three. What git cannot show: **gh#204 and gh#205 hold ZERO open agent work** — gh#204 is 7 met / 2 device (boxes 7 and 9, one measurement: the concept note's frame-rate line still reads `Measured: (not yet)`, re-measured this session), gh#205 is 8 met / 1 device (box 1); the queue had been carrying gh#204 as "boxes 7-9", but box 8 was recorded met 2026-09-06 · **gh#181 boxes 1/3/4/6 ruled void, so that ticket now has zero outstanding agent work** — boxes 2 and 7 confirmed satisfied and need only the owner's tick, box 8 reserves itself · **the CI red on `fdb9367` was not from the push** — docs/images only, `dist/` byte-identical to the last green, and a re-run of the identical SHA went green · **root cause found and measured**: the fit probe's press-1 screen on freeze-tap is `div.pass-container`, which renders `cond.prompt` drawn by an unseeded `Math.random()` over `TRIGGER_CONDITIONS`; at a real emulated 320x568 the nine prompts give pass-screen heights of 456px (seven of them) or 483px (the two symbol prompts, which wrap to three lines) — a 27px swing with identical element counts, which is why both CI attempts reported `ink 11` while disagreeing, and why only the narrowest viewport ever differs · **two of my own readings were refuted by measurement, not by argument** — the summary field `worst at press N, M ink` names the worst-SCORING screen, not the set visited (both attempts graded all three: ink 9/11/6), and the font-swap race died when the real probe run against a `dist/` with `dist/fonts` deleted (control proven HTTP 200 vs 404) returned 0px on every row · **gh#103 is still blocked** — its 2026-09-15 comment names TWO conditions and gh#102 closing cleared only one; gh#101's copy still carries `UNREVIEWED` (3 hits in `docs/copy/nuea-khu.md`, 2 in `docs/copy/nuea-khu/cards.md`, counted this session) · **gh#205's 2026-09-06 bundle question is moot, not open** — it asks whether to re-pin `BASELINE_TOTAL_BYTES` to 482,127; the constant now reads 1,049,872, re-pinned past both candidates by later work (which change is inferred, not traced).
dec: gh#181 boxes 1/3/4/6 VOID and box 5's premise goes with them (owner 2026-09-21, popup — **ruling recorded in comment `5757465723` and here, not in the ticket body**); the FORM is a comment, not a body edit and not a tick, per the 2026-09-15 gh#219 convention, and the four boxes stay in the body unticked so a later comment can restore them | pushed the docs stack now rather than holding per ADR-0049 (owner 2026-09-21, popup) — 29 commits and 8.4MB of binaries on one disk with no remote copy, and nothing in the stack touches `src/ public/ scripts/` so it cannot change `dist/` | img2threejs skill installed at `~/.claude/skills/img2threejs` pinned v2.0.0 `a669e9a`, NOT in this repo (owner 2026-09-21, popup) — `added-lineno-citation-check` does not exempt `.claude/`, so its docs would red the gate; gitignored in `~/.claude/.gitignore`, and the img2 plugin harness was declined | no `FITS_ROWS` pin moved and no `OVERFLOW_TOLERANCE_PX` widened — both forbidden, by the probe's own failure message and by `docs/agents/ci-gate-calibration.md` respectively.
next: [ ] freeze-tap's fit measurement is decided by the route's own RNG — a green means a short prompt was drawn, not that the screen fits. Evidence and both refuted hypotheses: `docs/verification/evidence/freeze-tap-fit-race-2026-09-21/FINDINGS.md`. Verify: two consecutive runs of the same commit report the same px for that row · Blocked · ask: seed the page RNG in the probe (the gh#204 precedent, but it pins whichever draw the seed yields and can pin the fitting one) (a), measure the worst draw (b), or change the screen so its height does not depend on the draw (c) — (c) is the route's problem and needs a ruling on whether a 483px pass screen is acceptable at 320px, which is gh#182's question
next: [ ] file the freeze-tap finding as a ticket — nothing on the tracker carries it, and it lives only in the evidence file and this entry. Verify: an open issue citing the 456/483 measurement · Blocked · ask: file it (a) or fold it into gh#182, which is CLOSED and owns the "no play screen scrolls at 320px" rule (b)
next: [ ] the fit probe's row summary reads `worst at press N, M ink`, which states the worst-scoring screen in the grammar of a set and cost this session a wrong diagnosis carried into a brief. Change that formatter to name both, e.g. `worst of 3 screens: press N (M ink)`. Verify: the string names the screen count · touches `scripts/`, so it is src and rides the next src batch, not a docs save
next: [ ] gh#103 — the remaining thirty เนื้อคู่ cards and portraits. Verify: thirty cards written and thirty portraits put to the owner · Blocked · ask: reuse the accepted style as-is, or fix hair sameness and age spread first — the ten were accepted WITH those defects, so carrying them to forty is a choice, not a default; consumes H: gh#101's output and cannot start before it
next: [ ] gh#160 — Cloudflare Web Analytics, chosen and not wired; its other blocker #159 is confirmed dead. Verify: per ticket, once the domain resolves · Blocked · ask: unblocks when H: gh#9 lands — nothing to decide before then
next: [ ] gh#29 — ad slots need the domain AND an owner AdSense account, site review and publisher ID; two owner acts, not one, per its own body. Verify: publisher ID recorded and ad code live · Blocked · ask: unblocks only after gh#9 AND the owner's AdSense account — do not brief it as domain-only
next: [ ] gh#19 — month-6 organic-clicks gate; needs the domain, Search Console connected to it, and six months of traffic. Verify: organic clicks recorded at month 6 · Blocked · ask: nothing to decide until the domain and the traffic window both exist
next: [ ] H: gh#101 owner edits the draft in the tree — `docs/copy/nuea-khu.md` and `docs/copy/nuea-khu/cards.md`, both still carrying `UNREVIEWED`; the agent half is done. A field-value edit now costs ten re-renders per the re-render trigger recorded in S2026-09-18#8. Verify: neither file still carries the banner
next: [ ] H: gh#9 owner registers `watduang.com` — owner-only and financial, never an agent action; re-run whois immediately before paying, privacy on, auto-renew on; it fully unblocks gh#160 only, and is one of two owner acts gh#29 needs. Verify: `dig +short watduang.com A` resolves — probed empty again this session
next: [ ] H: gh#141 walk all 20 boxes on a real phone; gh#204 boxes 7 and 9 and gh#205 box 1 and gh#219 box 1 need the same device and the same pass. gh#204's two are ONE measurement — box 9's residual is box 7's frame rate. Verify: every box noted with its device
next: [ ] H: gh#104 item 1 — the ดูดวง หมวด deck rewrite, 23 prompts and 5 predictions; owner-reserved Thai copy under the 2026-09-10 ruling, overlapping gh#97 gh#98 which already own เซียมซี's deck. Verify: the deck no longer promises a group
next: [ ] H: gh#109 is owner-only end to end — boxes 1-2 are Thai copy and boxes 3-5 are already satisfied per its 2026-08-28 comment; there is NO agent slice. Verify: nothing; do not brief it as mechanical work
next: [ ] H: gh#12 needs an owner Google Ads account, NOT the domain — its body says `Blocked by: —`. Verify: Keyword Planner volumes recorded under `docs/research/`, never `.scratch/`
inflight: measured during this save — `git status --short` carried only this save's own files, `gh pr list` open empty, no worktrees, no background tasks left running. `origin/main` = `fdb9367` and the tree is level with it: this save's docs commits are the only local-ahead bytes, staying local per ADR-0049. CI run `35577195693` ended green on attempt 2 after a red attempt 1 on the same SHA; the red's artifact `browser-probe-output-35577195693-1` is downloaded and read, and its evidence is in the FINDINGS file, not only in the run log which expires.
spent: queue 5→6 · batches 3 — counted as the last RH run's Execute Plan row count, non-`H:` only. gh#181 closed; three opened (the freeze-tap fix, filing it, the formatter line).
errors: **one class, three instances — wrote a conclusion before running the check that could refute it.** (1) Read the probe's `worst at press N, M ink` field as the set of screens visited without reading what the formatter emits, concluded "the two attempts graded different screens", and shipped that into a spawn_task brief as its premise; the per-press lines in the artifact refuted it. (2) Wrote the font-swap race into the FINDINGS file as the verdict before the discriminating leg ran; the no-font leg then returned 0px on every row. (3) Called the re-run green "proof of a flake" — the exact sentence gh#190's body opens with and its own comments spend four rounds disproving. Separately: read a wrapped run's verdict from `${PIPESTATUS[0]}` under zsh and got an empty string, which `docs/agents/shell-traps.md` documents by name.


# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-09-11#1
done: 14 commits `9643462..2838b8a` in 3 pushes. What git cannot show: **the local gate fleet was GREEN on the tree that then reded CI** — `scripts/ci-probes-verdict.mjs` never reads `coordinateHitsMeasurable`, so a probe recording "I could not measure" still counts as a pass, and siamsi was already unmeasurable locally · that red (`no-nav-in-stage-control`, 6 of 7) was the CONTROL leg, real leg passed, Deploy skipped, nothing broken shipped · **`eca92a8`'s 7/7 on that same leg was LUCK** — `#ss-keep` lands within ~5px of the fold on both machines (Mac 895, CI 901), so the same code passes or fails on font metrics · **0 crawlable anchors on all 14 built play pages**, an ADR-0050 delivery gap not a design choice, and every gate was forbid-only so none could see an absence · `arm-gate-coverage-check` was green over an exemption whose symbol gh#99 had deleted, and its selftest built its fixture from the entry it was checking · a CDP protocol error read as a legitimate null across 6 mappers, making `webgl-pixels` blame the runner's GPU for a destroyed context · **the stepper pin shipped FALSE once** — both clamps removable, still 3/3 green, because it read back through `currentCount()` · `play-crawl-link-check` calibrated at RUN level (break run 34562916043: its own step red, all others green, Deploy skipped, branch deleted unmerged) · tracker: gh#231 closed, gh#213 + gh#182 relabelled `ready-for-agent`, filed #232 #233 #234 #235.
dec: ADR-0066 (landing stable exit → own category; ADR-0014 + ADR-0041 given forward pointers, not rewritten) | gh#231 box 2 = no pre-dispatch gate required, closed | `haltOnContextLoss` retired — restart called `startMatch`, zeroing scores to recover a backdrop nobody plays on | CDP guard written on the POSITIVE set (`res.result` present) because Chrome owns the error-envelope shape and enumerating it never converges | gh#182 reading = 0 visible px violates, partly visible does not; `freeze-tap` + `wire-snip-panic` owner-reserved (both need tap-target/content cuts) | no-nav walk scrolls to the control rather than the page being shortened — a ดูดวง page, no above-fold rule binds.
next: [ ] `ci-probes-verdict.mjs` ignores `coordinateHitsMeasurable` — a probe that says it could not measure still passes; same class as the CDP fail-open fixed this session. Needs a ticket + fix. Verify: a run with an unmeasurable leg exits non-zero
next: [ ] gh#182 `wire-snip-panic` — owner call; above the fold costs wire columns 154.8px→58px + cable art to zero. Verify: owner records a decision
next: [ ] gh#182 second reading — whole-control-above-fold would also convict `zero-trigger` and pinocchio answer C. Verify: owner picks a reading
next: [ ] gh#102 gh#103 เนื้อคู่ portraits — blocked: `love-match` deliberately unregistered until gh#101, and gh#102's body records the host model returns OPAQUE images regardless of prompt. Verify: gh#101 ships first
next: [ ] #232 arm-gate-probe unwired · #233 crawl-link record · #234 bangkok-drift fog · #235 croc-bite 1976ms handled gap. Verify: each triaged
next: [ ] gh#97 box 4 needs a real browser run (build was forbidden mid-session); gh#97 box 7 + gh#98 box 5 are report-only. Verify: box 4 has a browser reading
next: [ ] fortune family remainder gh#101 gh#104 gh#106 gh#109. Verify: each ships Thai copy + owner reviews strings
next: [ ] owner physical blockers unchanged — gh#9 no A record, no NS, blocking gh#160 gh#29 gh#12 gh#19; gh#141 real-phone walk owes gh#219 box 1, gh#205 box 6 rows 1-2, gh#204 box 7, gh#210 box 2, and croc-bite's new row. Verify: each records a reading
inflight: measured during this save — `git status --short` shows only this save's files · `gh pr list --state open` empty, checked · `main` = `origin/main` at `2838b8a`, CI success, Deploy success · all 4 probe ports free, no worktrees but main, no background agents (fork agent `a7e4cfd152b609b44` was still registered running hours after returning and was stopped at save) · NOT verified: deployed pixels, whether Chrome fires `webglcontextlost` on that canvas, engine audio in MENU after a loss.
spent: queue 17→8 · batches 20
errors: reported "nothing is running" from CI/ports/git — three instruments that cannot see a background task, the same wrong-subject class logged twice against workers earlier the same session · read `$?` through a pipe and reported a gate green when it was rc=1 · invented a `--list` flag that launched the full fleet mid-work · grepped a file mid-write twice and drew a conclusion from it once · asserted REFUTE "missed" a site when my own grep matched a success line · attributed a CI red to a chrome change the worker then measured as coincidental · cited a control leg's 7/7 as evidence when it was luck · put line numbers in briefs repeatedly against a standing rule · commissioned a test, accepted a too-generous explanation for a green mutant, and reported it working.

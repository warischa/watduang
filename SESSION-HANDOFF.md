# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-09-05#1
done: 8 commits pushed in 2 batches, `origin/main` = `d0a3d30`. Batch 1 `1c3d1a0` (gh#207 escaping tier, `--no-audit`, gh#152 power-meter, gh#140 four routes merged from a peer session, gh#184 chip tail) CI green 15m7s. Batch 2 `d0a3d30` (fit probe sharded by route) CI green 8m4s. What git cannot show: **gh#140 was already built** — its own tests pass 25 of 25 and the tracker was simply wrong; the spec reader called box 1 unmet by matching `คนที่` inside a comment whose words are "Replaces the numbered defaults", mention counted as use · **ADR-0054 ruling 4 is struck through and superseded by ADR-0053** and I nearly briefed the dead one, which would have shipped per-game lists against a shared-roster ruling · **gh#184's third comment REVERSES its second** — the `+N` approach is chosen, not rejected; reading the second as the last word nearly shipped the superseded design · the `+N` counter denominator is **4 routes with a strip, not 11** · **the first strip probe compared chip geometry to the pill's own rect while the fade lives in an absolutely positioned pseudo-element outside it**, so no CSS width could ever have moved that check and its "4 of 6 combos" finding was untrustworthy; corrected, the real defect was 3 rows · the ruling's own open worry is answered favourably — every seat stays swipeable on all 6 phone combos, verified by looking · **18 evidence PNGs are gitignored, 0 tracked**, and the ignore rule's own comment says the JSON logs are the evidence · the icon-list scan's first disclosure comment was FALSE about its own blind spots · in the fit probe **exactly one check was set-level** (stale pins), far less than assumed · the shard rollback signal lived only in a log `standalone` discards on a PASS, so the trigger I wrote was unfollowable until the rows became notices · **CI and this Mac agree within ~1% on lane timing**, settling whether the Mac numbers were usable · `--no-audit` removes a 302s tail but bought **no median time** (3s to 5s across the two runs, n=1 each).
dec: owner 2026-09-05 — gh#183 closed as superseded by gh#203 (its boxes are a strict subset) · lane split BY ROUTE approved on the CI numbers, lifting the 2026-09-04 deferral · บางกอกดริฟต์ gh#204 gh#205 stays unranked, clear the backlog first · power-meter folds into the shared cast, same order every game per ADR-0054 ruling 2. Mine: a probe nothing calls is green by construction, so the strip-chip pair is deliberately NOT wired — its predicate demands the fade cover a whole chip sliver and chip width is font metrics on the rendering machine, the gh#202 trap; gh#210 carries the cap-then-size fix · shard coverage is proved by a route-id union per leg, never by the leg count, which is hand-maintained and blind to a route no shard owns · gh#206's "change the measurement" option was already the status quo and I offered it without reading the gate first.
next: [ ] gh#203 — per-route desktop verdicts at 1440x900 with screenshots, then owner sign-off. Verify: a verdict recorded per route and the owner has said they read as designed
- [ ] gh#182 box 6 and gh#140 box 3 — the 375px check neither batch covered. Verify: every surface rendering a player name checked at 375px, no overflow or reflow
- [ ] gh#210 — cap the chip width, size the fade from the cap, then wire the strip-chip pair into lane2. Verify: nakedCutCount 0 on all nine combos locally AND in a CI artifact, the two diffed in the ticket, and the control reds when the band is force-hidden
- [ ] gh#209 — a gate requiring any route that builds its own setup row to render the icon. Verify: the gate reds on a route whose badge is a digit
- [ ] owner: gh#206 — re-pin the baseline centre or let it ride to red; headroom is small, read it with the bundle gate rather than from here
- [ ] owner: gh#204 gh#205 ranking · gh#192 b-e · gh#9 domain still blocks gh#160 gh#29 gh#12 gh#19 · standing gh#165 gh#150 gh#141 gh#112 gh#109 gh#106 and the fortune family gh#97-gh#104
inflight: measured during this save — `git status --short` clean, 0 commits ahead of `origin/main`, `gh pr list --state open` 0, one worktree (the peer's and the gh#207 agent's both removed), latest CI on main success. Strip-chip probe present and calibrated but wired into no runner, on purpose.
spent: queue 14 to 6 · batches 2 src pushes, 2 CI runs, 3 local gate suites, 2 REFUTE rounds both FAIL, 8 findings, all resolved.
errors: read the second of three ticket comments as its last word, inverting a ruling · counted a denominator over all routes when only four had the feature · reported evidence as committed without asking git, the second instance of that class · wrote a guard's disclosure that was false about its own blind spots · wrote Thai into a comment a gate forbids · claimed a comment used an escape while having pasted the glyph · offered the owner a decision option that was already the shipped behaviour · broke my own must-red twice, once by a regex that missed the declaration and once by a formula that miscounted, both producing a red I could not attribute.


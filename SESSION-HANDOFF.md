# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state


### S2026-09-15#3
done: pushed `bb1293c..4f6027c` (5 commits; CI 34937962488 success, verified per-step from the API — 59 steps, 58 success, the 1 skip is the conditional failure-artifact keeper). gh#182 CLOSED. Local and unpushed: `b57967a`, `b7d3f88`. What git cannot show: **gh#182's last row was two defects, not one** — fixing the pass screen exposed `ruleReadyBtn` on the press-0 rule-reveal screen at 0 visible px, masked because the fit probe walks to ONE screen per route and keeps only the worst, so a second defect on a better screen stays invisible until the worse one is fixed · **that ticket's own fold reference carried a false sentence for four days** — it said freeze-tap's press-0 screen has no primary control at all, and that sentence is why the second defect went unrecorded; superseded in `747643d`, not rewritten · **my gh#235 causal claim was wrong and reached the owner's decision** — I explained the recorded 960.1-1976.1ms handled gaps by the engine's deferred arming, but `play-exit-probe.mjs` presses the exit-confirmation button and holds zero references to a tooth or `pressTooth`; the arming is real and now independently confirmed at n=6, it is simply not on the measured path · **this handoff's own memory-index floor was wrong** — recorded ~18.9KB with hooks stripped, measured 13551B, so all 165 memories were kept and the index went 21876B to 13566B by dropping index hooks alone, which every file duplicates in its own `description:` · **a `not-runnable-here` line is not the skip you assume** — the gate suite's 46/46 excludes the two expensive browser legs structurally, so the live fit probe is NOT inside that green; `npm run ci-probes` is, and it carries `BREAK_WALK=1` control shards · **gh#102 carried the same mislabel as gh#103**, found only by checking the family rather than the ticket named · **gh#181's whole screenshot set was stale** — all eight captured 2026-09-07 against routes changed 09-09 and 09-14.
dec: gh#182 freeze-tap = fix not rule, by owner answer; both screens fixed by reclaiming padding, margin and gap, no content cut, no control under 44px, media-scoped so desktop is untouched | gh#219 boxes 2+4 = hold to the repo convention, evidence recorded by comment and no box ticked — the earlier popup mis-stated the prior agent's reason as unexplained when the repo's own tracker doc states it as convention | gh#235 = confirm externally, no diagnostic code shipped to players | gh#102 and gh#103 both to `ready-for-human`, gh#235 to `ready-for-agent` | push approved against the evidence line shown before executing | `feat/croc-bite-engine` left alone deliberately — 0 commits ahead of main, and it is the natural branch for the croc-bite work.
next: [ ] gh#235 the actual cause of the 960.1-1976.1ms spread on the `play-exit` control — deferred arming is confirmed but refuted AS the cause. Unchecked lead: main-thread contention from croc-bite's synchronous board rebuild and arm-gate install at round start, not guarded off in the no-WebGL lane. Verify: a run attributes the spread to a named mechanism on the measured path
next: [ ] gh#181 box 8 — owner's own look, fresh screenshots delivered this session. One box-7 verdict is contested: power-meter reads "as designed" while its screen spans 0.465 of the 1440px frame. Verify: the ticket records the owner's verdict
next: [ ] push `b57967a` and `b7d3f88` — local, unpushed, ride the next src push per ADR-0049. Verify: `git log origin/main..HEAD` is empty
next: [ ] gh#101 owner Thai copy — single blocker for gh#102 gh#103 gh#104 gh#106 gh#109. Verify: copy lands
next: [ ] gh#141 real-phone walk owes gh#219 box 1, gh#205 box 6 rows 1-2, gh#204 box 7. Verify: each records a reading taken on a real device
next: [ ] gh#9 domain — `dig +short watduang.com A` and `NS` both empty, re-checked this session; blocks gh#160 gh#29 gh#12 gh#19. Verify: an A record resolves
next: [ ] the fit probe measures one screen per route and keeps the worst, so a second defect on a better screen stays invisible; gh#182 closed without closing this. Verify: the probe records a row per screen, or a ticket records that it will not
inflight: measured during this save — `git status --short` clean · `gh pr list --state open` empty · `origin/main` = `4f6027c` with 2 local commits ahead before this save’s own docs commit · no worktrees · all four ui-driver dispatches returned and the backgrounded suite, probe and CI-watch runs all exited · gate suite re-run on THIS save’s tree rather than on the pushed commit — `declared=48 executed=46 failed=0 not-runnable-here=2`, with both meta-checks that enumerate `scripts/` green, so the new gh#235 probe script trips neither; `npm run ci-probes` 28 legs including 3 fit shards and 3 control shards, measured at `4f6027c`.
spent: queue 9 to 7 · batches 1 push · dispatches 4 ui-driver
errors: **explained a probe's numbers from a code path it never presses** — read the engine's source, found a real mechanism, wrote it into a ticket as the cause, and the owner picked an instrument on it; the probe presses a different button entirely · **carried an agent's framing into a popup unchecked** — told the owner a prior agent's choice not to tick was unexplained when the repo's own doc states it as convention · **cited a floor from this handoff without re-measuring it** — the memory-index floor was 13551B not ~18.9KB, and acting on it would have merged away fourteen distinct lessons for nothing · **told the owner the push was 4 commits when it was 5** — the previous session’s stacked docs save rode along per ADR-0049, which is correct behaviour I had read that morning and still did not count into the number I put in the popup.

# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-09-06#1
done: 10 commits `8ad3b08..178a09c`, all pushed, CI green, **deployed** — site at `white-plant-05ad7c600.7.azurestaticapps.net`, 12 play routes. `8ad3b08` play-exit · `b4ea5e5` gh#182 rows · `9243421`+`f8f0a74` gh#209 · `9301b01`+`7be2360`+`59e81c6` gh#182 · `16e425f` กับระเบิด · `0cb6a8e` guard classifier · `178a09c` probe phases. **CLOSED gh#211 gh#140 gh#152 gh#209; FILED gh#212 gh#213 gh#214 gh#215 gh#216.** What git cannot show: **yesterday's CI red was the probe measuring its own latency, NOT the gh#203 composition read** — `touch()` awaited 2 CDP round trips per contact so the gap was 80ms + 2×RTT ≈ 330 against a 400 cliff; max gap 213.9→108ms, and CI now records its own band (11 `::notice::` GAPS lines per green run, 155.8 / 175.9 / 237.9ms across three greens — before this a green run carried NO number) · **`ci-probes.sh` runs every lane `--headless --disable-gpu`**, so `getContext('webgl')` is null in all of them: the composition probe was reading `webglUnsupportedNotice` as a live unit, and ADR-0051's "round still playable without 3D" is why the DOM board exists at all, not polish · **the two CI reds after the 12th route were the MACHINE** — `play-exit` failed on `pinocchio-luck` not `one-bomb`, and at throttle 40 `cursed-number` left the round while `one-bomb` stayed while CI ranked `one-bomb` 1st and this Mac 5th: **inverted rankings** · `aspect-ratio` transferred the 44px height floor into automatic min-width — 44px stones on a 33.5px pitch, overlapping targets, last stone off-screen, **while the floor probe reported a clean 44px** · `.hud.hidden` scales 0.98 and a rect is measured after transform, so already-44px buttons read 43.12 · **backdrop-closing a modal opened a stone with the same tap** (`.ob-tile.is-open` 0→1, zero buttons disabled; a plain `tap()` does not reproduce it — the press must outlast the fade) · the bundle re-pin 397772→458110→464658 **absorbed 16,373B of pre-existing unattributed drift**, which is gh#206's own question 1 · `one-bomb 320x568` went 23→75→97px: 23 vs 75 was a **press-index** difference not a machine one, 75→97 is the menu-scroll fix making the card reachable · **the "gone" 404-line DoD drafts file was never gone** — it is `dod-drafts-gh112-gh165.md` in project-state, and #139's "เข้งับ is a 4.0K stub" measured `index.html` alone against a real 189,210B / 15-file Vite project · `name-escaping`'s `auditRoute` takes the FIRST of main.js/main.ts, and `one-bomb` is the ONLY route shipping both.
dec: owner 2026-09-06 — Thai name `กับระเบิด` (owner-typed), id `one-bomb` · DOM tile grid is the PRIMARY input path · gh#213 เข้งับ does NOT wait for gh#202's font, strip refs, system fonts · บางกอกดริฟต์ third · bundle growth approved ONCE for all three games (#206) · gh#182 `cursed-number` exception covers the ROUTE not just 320 · gh#181 covers all 8 measured routes not the 4 its title names · gh#212 record divergences, do NOT reconcile the mockups · gh#202 self-host Thai webfont as a SUBSET · CI red → cut probe concurrency, never relax UNMEASURED. Mine: parked both pushes for one ask each until the owner said "deploy ด้วย" · removed the exemption COUNT from the 375px README rather than correct it a third time · left `play-exit-guard` UNMEASURED blocking, since relaxing it is gh#190's owner call.
next: [ ] gh#150 stage D — the grid is PRIMARY only on the no-3D path; with a live context the engine owns the round in a closure exporting nothing. Verify: a tap on the board drives the round with WebGL available
- [ ] gh#215 `webglcontextlost` mid-round leaves a blank canvas with a live HUD — real on Android, invisible headless. Verify: `WEBGL_lose_context` mid-round, round still playable
- [ ] ADR-0051's pixel-readback probe — needs its OWN GPU-enabled lane; under `--disable-gpu` a `readPixels` assertion is green by construction. Verify: calibrated against a stub renderer that draws nothing
- [ ] gh#182 six 320px redesigns (`freeze-tap` 187 · `how-close-is-near` 194 · `short-stick` 191 · `wire-snip-panic` 111 · `zero-trigger` 131 · `pinocchio-luck` 136) — budget committed at `docs/verification/evidence/182-320-blocks/`; every largest block IS the CTA/roster/gameplay surface, nothing to delete. Verify: no row cites only `gh#182 open:`
- [ ] gh#214 tap floor measures height only — 29.8px-wide stone passes; needs an owner ruling, 9×44px cannot fit 320px
- [ ] gh#216 `name-escaping` scans one module per route · gh#212 extraction drift, owner Q2 gates the build
- [ ] gh#213 เข้งับ port · gh#204/#205 บางกอกดริฟต์ (third)
- [ ] owner: **gh#9 domain** still NXDOMAIN and blocks gh#160 gh#29 gh#12 gh#19 · gh#206 attribute-or-accept the absorbed 16,373B · standing gh#165 gh#150 gh#141 gh#112 gh#109 gh#106 and the fortune family gh#97–gh#104
inflight: measured during this save — tree 2 files dirty (THIS save's roll only) · `gh pr list --state open` 0 · unpushed 0, `origin/main` `178a09c` · last CI 34001181275 SUCCESS · open issues 33 · 1 worktree kept deliberately: `hopeful-fermi-74ec5e`, the OWNER's own · this save is docs-only and does NOT push (ADR-0049: docs-only still pays full probes, so it rides the next src push).
spent: queue 13→17 · batches 4 pushes, 22 dispatches, 1 fork, 2 REFUTE rounds on 2 artifacts, 1 REFUSED that was correct
errors: carried a scout's claim into a brief as fact — the ES-module top-level `return` was legal, refuted by `node --check` plus a must-red · briefed the arm gate as the CI red's cause when `armAllButtons` never touches `#play-exit`, refuted by an ownership trace · pointed at `FIT_SHARDS` as the concurrency lever when a barrier makes fewer shards buy zero headroom, refuted by timings · read a `--selftest` flag that does not exist as a failing probe until I read the error · nearly filed a fabricated gate hole twice (a derived gate list and an md/non-md split, both correct by design) · published a gh#184 comment repeating the claim `9d30062` exists to retract, having read the retraction.

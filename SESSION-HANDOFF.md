# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-09-01#1
done: 6 commits PUSHED in 2 batches — `0fbdcbb` `04fcde5` `354e5ff` (CI 33465236171) then `08be978` `6e965cf` `ff1f22b` (CI 33467799692), both success incl. the deploy step. What git cannot show: **the inherited gh#187 picture was wrong in a load-bearing way** — on the 8 unfixed routes the CONFIRM branch already re-armed; only close/cancel plus the rules/help/avatar closers were bare, so the same dialog shipped guarded on one button and open on the two beside it. Each route's `arm-reveal-paths` test enumerates reveal RECEIVERS (`innerHTML` / `style.display` writes), and a closer that only hides a modal writes neither — the class was invisible to the instrument built to find it, BY CONSTRUCTION not by oversight. Same shape in gh#185: `play-exit`'s test built its set from directories owning a roster bridge and asserted a floor of nine against exactly nine, so timebomb and dice-loser sat outside the loop by construction. **F1's premise was STALE before I started** — the play-screen-fit pair had already run green on a real Linux runner in CI 33405521676, so only the probe's own red direction was owed; and box 5 looked unsatisfiable because the file's header says scroll is "REPORTED, never asserted", until the inverted `FITS_ROWS` guard turned out to assert exactly the pinned rows. **Four measured corrections to claims I had accepted**: the markup numbered defaults were NOT player-visible (every node sits in a container hidden until its script write, so severity was overstated and I had already repeated it); `how-close-is-near` DOES use the shared cast, via its own seat helper routing through `mascotNames`; the comment-stripper count is 14 implementations across 11 files with 8 fail-open, not 7/11 at implementation granularity; and only gh#173 of seven audited tickets was fully provable. **Two instrument bugs found, neither by a gate**: timebomb's boom readback returned one frame before the explosion painted, so under reduced motion it captured the ticking image byte-identical to the ticking read — a real explosion and a dead renderer looked the same, and full motion hid it; and the gh#179 calibration evidence was written as `.log`, which this repo's gitignore drops, so committed proof would have silently not existed. The markup numbered-default scan also knew only Arabic digits — a Thai-numeral default would have walked through a green guard in a Thai-first product.
dec: owner rulings 2026-09-01 — gh#187 closing a modal IS a reveal ADR-0017 gates, all 8 routes gated, recorded ADR-0057 · gh#185 box 5 timebomb keeps mounting the shared edit control deliberately, recorded in ADR-0053 · gh#151 box 2 the tick SOUND is not a leaking channel, it is the game, closed as an accepted ceiling even though pitch encodes fraction-elapsed · 2 src pushes approved on shown evidence · tracker widened past the old pre-auth to gh#151/#179/#185/#186 · out-of-scope findings fixed now rather than filed. Mine, logged: took gh#186 approach C (conservation keyed on repo-owned canonical hexes) over spot-fixing the regexes or a parser rewrite, per ADR-0056 — B is dead because no parser ships and parse-failure is a second unowned set · did NOT fix the REFUTE finding that 3 routes arm with no exception list, filed gh#188 instead, because it changes behaviour beyond the owner's ruling on an inference nobody drove to a wrong outcome · left `party-size-claim-check` on a recorded ceiling not the abort, because one legitimate comment quoting the gh#89 ruling would be a false red · left gh#151/#179/#186 OPEN despite every box ticked, since the owner authorized ticking and not closing · left gh#185 box 2 unticked because it is a browser round-trip and the shipped test is a source-shape assertion · proved F1 locally with a GROSS mutation and skipped the throwaway branch, per the owner's rule and the fork's finding that a marginal break only calibrates this machine's threshold.
next:
- [ ] agent: gh#182 — no play screen scrolls at 320px or 390px. Unblocked: gh#179's measurement is closed and its probe reds on a pinned row. Verify: the play-screen-fit pair green with the newly-fitting rows added to the pinned set, and the pins re-recorded from a CI run rather than from macOS
- [ ] owner: gh#189 — is the ban "no numbered PLAYER default" or "no numbered placeholder anywhere"? cannon-flag still ships turn-order and shot counters of the same dead-bytes shape. Nothing is fixable until this is ruled, because it decides whether the markup scan widens. Verify: the rule is stated in the ticket and the scan's scope matches it either way
- [ ] agent: gh#188 — 3 routes arm with no exception list, so a dismissal re-enables buttons the game itself disabled. Verify: the Escape-then-Enter path on wire-snip-panic is shown harmless or fixed, and a test names the buttons whose disabled state the game owns and fails if the arm window clears them
- [ ] agent: gh#172 — one box left: a slash-star inside a REGEX literal is untested; only the string case has a fixture. Verify: a regex-literal fixture is added and the gate still sees a live exclusion beneath it
- [ ] agent: gh#185 box 2 — the only unticked box in the batch. Needs the module harness to gain a real DOM; it currently breaks on the default-plus-named import shape both non-bridge routes use. Verify: set the flag, load the route, assert it cleared — a real round-trip, replacing the source match on all eleven
- [ ] owner: gh#151 gh#179 gh#186 are fully ticked but still OPEN — say whether to close them · gh#165 sound toggle · gh#75 box 7 · the reduced-motion look on 5 games
- [ ] owner: gh#9 the domain, which gates gh#160 gh#19 gh#12 · gh#29 AdSense has no account · gh#141 carries 15 real-phone rows · #180 #181 #183 desktop fill stay deferred
inflight: measured at save — tree carries ONLY this save's 2 docs · 0 unpushed commits, `origin/main` level at `ff1f22b` · 0 open PRs · 44 open issues · 1 worktree, 6 local branches incl. `claude/rh-cafbc2` deliberately kept · 3 agent worktrees and their branches removed, each proved to hold 0 commits main lacks · no bg tasks (checked) · watduang.com still NXDOMAIN, Azure origin remains the live target
spent: queue 8 → 7 · batches 2 src pushes (2 CI runs, both green first try) · REFUTE 2 artifacts x 1 round, 0 blocking findings both times · fork 1 · 12 agents
errors: briefed a marginal break where only a gross one proves anything on a cross-platform pin set — the fork caught it mid-run · asserted a probe sat in a CI lane when it is wired nowhere, so my REFUTE brief asked a question about a budget that does not exist · repeated a verifier's "a player can see this" to the owner without checking, and it was false · wrote a comment claiming escapes above a line using literal glyphs, which would also have reddened the Thai-comment gate · planted a mutant with the wrong element anchor and read the resulting green as evidence until the plant count showed 0.

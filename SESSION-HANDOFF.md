# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state


### S2026-09-15
done: shipped `8354f16` gh#182 — 7 KNOWN_OVERFLOW rows moved to `owner ruling 2026-09-15`, handoff crit fixed; pushed `164215a..1f5626b` (last session's backlog). What git cannot show: **the queue was 10 of 12 rows dead or owner-reserved at liveness** — `dig +short watduang.com A` and `NS` both return EMPTY, so the gh#9 chain is confirmed blocked, not assumed · **gh#182's "8 rows" is a transcription error with a source** — the ticket's own last comment writes 8 and then enumerates 9; the enumeration governs, a row-shaped grep returns 9, and a bare `grep -c` returns 12 because three matches are prose including `UNCLASSIFIED_ADVICE`, which embeds the literal on purpose, so the old crit could never reach 0 · **the ruling was scoped 9→7 by checking, not by accepting the pick** — `freeze-tap` is owner-reserved AND the only route the 2026-09-11 table found in actual violation, and `one-bomb` was never measured against the fold rule at all (absent from both `182-320-fold` evidence dirs), so a blanket reason would have asserted an unmeasured pass · **the probe's class regex demands `<class>: <N>px ` at the front**, so the owner's approved prose had to be appended after the measurement, never substituted for it — a blind dispatch of the approved wording would have red-ed · **CI 34920487938 proves the gh#237 font gate EXECUTED, not skipped** — selftest ok, 73 reachable codepoints against 191 mapped across all 4 declared faces; 1 of 59 steps skipped and it is the conditional failure-artifact keeper · **gh#235 has two mechanisms in one `roundReset` listener** — a synchronous 3D reset and a 1200ms deferred first turn — and CI's `--disable-gpu` on every browser lane but the ADR-0051 readback rules the synchronous one OUT for the lane that produced the six runs; the deferred-arming step is NOT established and the recorded 960.1ms minimum argues against a pure-timer account.
dec: gh#182 ruling scope = 7 rows, `freeze-tap` and `one-bomb` excluded, both put to the owner before the edit and both excluded by owner answer | ruling wording owner-approved verbatim BEFORE writing, because no Verify can check it | gh#182 relabelled `ready-for-human` with a recorded reason — what remains is not purely owner work, `one-bomb` needs a browser measurement an agent can take | REFUTE skipped deliberately and logged: no gate/guard/measurement mechanism added, wording pre-approved, validator calibrated red-then-green | gh#235 label left UNCHANGED against the owner's pick, because the finding moved the choice of instrument back to the owner — parked, not overridden.
next: [ ] push `8354f16` — the earlier go covered the previous four commits only, this is a new ask. Verify: `git log origin/main..HEAD` empty and CI green on the run it triggers
next: [ ] gh#235 label — owner picked unblock on the premise an agent would instrument; the two-mechanism finding moves which instrument back to the owner, so `ready-for-human` is now correct. Verify: the ticket records the owner's choice of instrument
next: [ ] gh#182 `one-bomb 320x568` fold measurement at 320x568 — agent-doable browser work, not yet ordered; it is the only non-owner row left on box 1. Verify: a recorded reading, then the row carries a ruling reason
next: [ ] gh#182 box 1 remainder — `freeze-tap 320x568` stays owner-reserved. Verify: `grep -cE "^\s*\['.*gh#182 open:" scripts/play-screen-fit-probe.mjs` returns 1 once one-bomb is ruled, 0 once freeze-tap is
next: [ ] gh#181 box 8 — owner looks at the eight 1440x900 screenshots; no agent verdict replaces it. Verify: the ticket records the owner's own verdict
next: [ ] gh#101 needs owner Thai copy — single blocker for the whole fortune family, and the page is delisted until it ships; gh#102 portraits cannot start because its ten cards do not exist. Verify: copy lands and the cards ship with the portrait box empty
next: [ ] fortune family remainder gh#103 gh#104 gh#106 gh#109. Verify: each ships Thai copy and the owner reviews strings
next: [ ] gh#141 real-phone walk owes gh#219 box 1, gh#205 box 6 rows 1-2, gh#204 box 7. Verify: each records a reading taken on a real device
next: [ ] owner physical blockers unchanged — gh#9 no A record no NS, blocking gh#160 gh#29 gh#12 gh#19. Verify: A record resolves
inflight: measured during this save — `git status --short` shows only this save's own two files · `gh pr list --state open` empty, checked · `main` = `origin/main` + 1 unpushed commit `8354f16`, src, owner-gated and NOT taken · `git worktree list` shows only the main tree · probe ports 4321/5051/9222/9344/9348 all free, checked · no background agents · full `run-workflow-gates.sh` green this session at `declared=48 executed=46 failed=0 not-runnable-here=2`, run on this diff.
spent: queue 12→9 · batches 3 · dispatches 2 (1 agy preflight probe, 1 codex reader)
errors: **attributed a CI measurement to a code path from a source comment alone** — drafted a gh#235 comment headlined "the main thread is not busy" and nearly published it, when the same listener runs `setupTeeth`/`resetAll` synchronously; what actually settles it is the lane's browser flag, which I had not checked until challenged · **carried a queue count from prose instead of its own enumeration** — briefed gh#182 as "8 rows, unreconciled" when the ticket enumerates 9 beneath the wrong word · **briefed a prohibition the return then ignored** — the reader brief forbade `file:line` and named the gate, and every citation came back as `file:line`; stating the ban did not bind it, and only re-deriving each finding kept it out of source.


# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-08-24#2

done: **4 commits `d675b72` + `957641c` + `78d3fa6` + `72b9684`, pushed · CI runs 32710621773 / 32712973237 both success, per-step conclusions read from the JOBS endpoint, `Deploy to Azure Static Web Apps` = success on each · `npm test` stays 188 — gate work lives in `scripts/`, the `node --test` count cannot move · gh#71 and gh#72 both CLOSED, open issues 13->11** · **gh#71's warn path is calibrated at the level it ships at** — the mechanical reason the gap survived was NOT test design: the selftest's shared `run()` helper used `execFileSync`, whose return value on an exit-0 run is stdout only, and the annotation goes to stderr, so no case built on that helper could ever have seen it however written; swapped to `spawnSync`. New case copies the script's own bytes to a temp tree and spawns it twice. Run-level, throwaway branch, one variable apart: stale `bb25788` run 32708885792 -> annotation PRESENT, fresh `64c2a0d` run 32708951665 -> ABSENT, BOTH GREEN — conclusion cannot distinguish the legs, only the annotation carries signal; read via the check-runs annotations API, never the run summary · **gh#72 closed with 2 of its 3 boxes WITHDRAWN, evidenced on the ticket, not silently dropped** — five strippers exist not four (`arm-gate-coverage-check.mjs` landed in `dbd641f` before filing), three semantic families, only checkpoint+stable-exit congruent and only merge-upward is safe; the merge was refused on the OWNERSHIP answer: comment grammar is language-owned, so one shared incomplete stripper makes one ceiling load-bearing in five gates with five different preconditions · what shipped instead is a per-gate pin in `leave-confirm-check.mjs` and `arm-gate-coverage-check.mjs` that measures each gate's OWN scan set and reds on a `//` inside a quoted value, calibrated three ways per gate (planted hazard, clean tree, stubbed detector) · **box 3 REVERTED after implementation** — pointing the selftest's `shipped` at the live config turned `assert.equal(hashCsp(shipped), PAIRS_HASH)` into a hard fail on any real CSP edit, reversing the 2026-08-23 warn ruling, and CI's `--selftest && <scan>` would ALSO short-circuit the scan that does the warning; separately it made six `.replace()`-derived fixtures silent no-ops once their anchors drift · **a survey of all 13 CI-invoked gates found NO other instance of that class**, but turned the check back on this session's own new text and found its ceiling disclosure wrong on 2 of 3 legs, fixed in `72b9684`
dec: ADR-0030 — a fixture must not be sourced from the artifact it tests; the pull toward "one fact, one home" is right about production facts and wrong about fixtures, and the three harms (severity inversion · fixture decay · `&&` short-circuit) are all invisible on a green run · the two new pins fail CLOSED behind CI's `&&` deliberately: a precondition break makes the gate fail-open, which is ADR-0019 doctrine, and the disclosure now states that a red also SKIPS the scan rather than costing "one annoyed commit" · a `//` inside an HTML comment is a TRUE positive not a false red — measured, the strip eats a real attribute sharing the line — so it was removed from the known-false-reds list rather than kept as harmless · two agent-reported claims were refuted by re-derivation this session and one orchestrator measurement was refuted by an agent, so re-derive before acting stays the rule in both directions
next:
- [ ] owner-run: gh#9 domain -> gh#29 AdSense. `dig +short watduang.com NS` EMPTY, re-verified at this save. Blocks the ONE remaining box on each of #15 #16 #17 #18 — those four tools are BUILT and shipping, do not re-scope them as unbuilt
- [ ] owner-run: gh#13 last box — the real-device script under `docs/verification/` on one real iPhone. An agent CANNOT do this; the simulator tooling reaches simulators only
- [ ] owner: gh#12 keyword planner · gh#19 month-6 organic-clicks gate
- [ ] optional, agent-doable: the two pins disclose a SECOND missed class (per-line backtick parity misses a `//` on an interior line of a multi-line template literal). Zero such lines today, verified by a per-file backtick-depth walk, NOT by grep — a grep cannot see it. Done when either the walk lands or the first hit appears and forces it
inflight: measured at save — tree clean (`git status --short` empty) · open PRs 0 (checked `gh pr list`) · bg tasks 0 (checked) · throwaway branch `calib/gh71-warn` DELETED, remote and local both verified gone · HEAD `72b9684` level with origin/main before this save's commit
spent: queue 5->3 (2 drained, 0 new tickets — the survey found no new instance) · dispatches 6 (fork, inventory, gh#71 impl, pin, box-3 impl, REFUTE, survey; 1 round each, 0 retries) · fable 2 (fork + 1 REFUTE round, the standing gates) · GitHub writes 3 comments + 2 closes · ctx ~23% of 1M at drain gate

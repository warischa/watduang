# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-09-09#1
done: **zero src commits — tree clean at `2fe7da5` the whole run, no code touched, no build, no CI.** Tracker-and-memory round working the RH queue. What git cannot show: **a live content-rule breach is shipping right now** — `public/og/siamsi.png` carries the pre-rename title `เซียมซีปาร์ตี้`, the tagline `ส่งมือถือวนรอบวง คนละใบ` and `เล่นฟรี 2-10 คน`, i.e. a ดูดวง surface promising the group experience gh#104 exists to remove, and the `2-10 คน` line is the site-wide claim CLAUDE.md forbids outside สุ่มคนโดน; `daily-fortune.png` also says `2-10 คน` and `/c/fortune/` inherits `site.png`'s `ส่งวนกันทั้งวง`. **I opened `siamsi.png` and read it; the other two rest on the enumerating agent's pixel read and are labelled that way on the ticket.** No source query can see any of them — OG text ships as pixels, which is why five sessions of grepping missed it · `public/og/love-match.png` is tracked and publicly reachable although the game is deliberately delisted per the manifest's own comment pending gh#101 · **the handoff's "six tickets closed" was five** — gh#220 was open, and its own last comment says why (box 3 asserts an unenumerated set) · **gh#104's "19 of 24" was 23 of 24**, the earlier regex missing cards 10/15/20/23, and two Thai substring traps changed the answer not just the tidiness (`ดวง` contains `วง`; a stoplist entry for `ห้วง` swallowed `ให้วง`) · the ดูดวง set is **2 pages + hub, not the ticket's three** — `love-match` declares the category on disk but the manifest records it delisted · `RECORDED_IDLE` holds exactly **3** routes (`freeze-tap`, `how-close-is-near`, `power-meter`), read by importing the module · tool-copy registry is **134 `rows` keys / 79 pending**, and my first count assumed a `status` field the file does not have and returned 0 — the shape had to be read before the number meant anything · gh#181's `ready-for-agent` was stale by its own last comment; **box 6 may now be measurable** because gh#220's fix landed ~8h AFTER that comment was written · tracker writes went out under a 5-item owner pre-auth, each shown before posting and read back; a 6th (gh#220's label) fell outside the grant per `reference/operational.md` and was asked separately.
dec: gh#213 → **its own run**, owner re-decided after I supplied a false premise | tracker writes 1-5 pre-authorized, evidence shown before each post | residuals → comment where an open home exists, new ticket otherwise (`docs/agents/issue-tracker.md` has no reopen-vs-new rule; owner chose) | gh#226's `wire-column` residual re-homed to gh#214, gh#224's and gh#112's filed as #228 and #229, both `ready-for-human` | gh#220 boxes 1/2/4/5 ticked against its own 2026-09-08 evidence, box 3 left open, relabelled `ready-for-human` | memory consolidation run at round end but **deliberately NOT merged to the inherited <140 lines** — that number was a prior session's own invention, master's bar is 200, and reaching 139 needs ~21 merges that would blur distinct mechanisms | Run A carried no code artifact, so no fork and no REFUTE — correct per the skill's Shape table, not an omission.
next: [ ] **The three OG cards are the live breach — highest priority.** Regenerate `siamsi.png` and `daily-fortune.png` without the group/`2-10 คน` copy, and decide whether `/c/fortune/` gets its own card instead of inheriting `site.png`. Read `docs/agents/assets.md` FIRST — Thai rendered the wrong way shatters into dotted circles while the script exits 0. Verify: each shipped card is opened and read, and no ดูดวง card states a player count or phone-passing
- [ ] **gh#213 — its own `/orchestrate` run, never folded into a batch.** This is the ADR-0060 vendoring event: `three` tree-shaken 498,672B + game code 68,850B against `BASELINE_TOTAL_BYTES` 482,127 → reachable total ~1,049,649 (>2x), gzipped `three` alone 126,423B ≈ 10x กับระเบิด's whole page. Verify: it ships as its own artifact with its own deploy ask and a re-pinned baseline
- [ ] **gh#104's rewrite — 23 prompts + 5 predictions, owner copy.** The 50-row work list is posted on the ticket. Overlaps gh#97/gh#98 which already own the เซียมซี deck — cheaper to ride those than rewrite twice. Verify: each flagged string either describes one person with one phone, or the ticket records why it stays
- [ ] **One ruling gh#104 needs:** is a pass-phone panel present in the DOM but `hidden` reader-visible for box 1? Verify: the ticket records yes (gate it) or no (appendix note)
- [ ] **gh#220 box 3 — the ask is already stated on the ticket, do not restate it.** Verify: the ticket names the other two absence-shaped stylesheet decisions, or records that the count was one
- [ ] **#228 · #229 — the two re-homed residuals, both owner calls.** Verify: each records a ruling or a mechanism
- [ ] **gh#192 (b), gh#182, gh#205 boxes 5-6, gh#204 boxes 7 and 9, gh#214, gh#210, gh#184, gh#219 — owner's eye, phone, or sentence.** Each question is already stated on its own ticket. Verify: each records the call
- [ ] **gh#181 box 6 may have moved** — gh#220's fix postdates the comment that reserved it. Verify: the ticket records whether box 6 is now measurable, or stays the owner's
- [ ] **Rewrite this file's own memory criterion.** The prior `<140 lines` target is unreachable without blurring distinct memories. Index is 156 lines / 19,319B against master's 200-line / 25KB bar. Verify: no future handoff carries the <140 number
- [ ] **Read the fit probe's warnings, never its exit code.** A grown `KNOWN_OVERFLOW` row warns and still exits 0. Verify: any fit claim quotes the notice line, not the exit status
- [ ] **`claude/rh-cafbc2` — 8 commits, 16 files, 847 insertions vs main; owner ruled keep.** Verify: a session lands or abandons it explicitly
- [ ] owner physical blockers, unchanged: gh#9 domain has no A record and no NS (blocks gh#160 gh#29 gh#12 gh#19) · gh#141 real-phone walk · owner standing gh#106 gh#109 and the fortune family gh#97-gh#103
inflight: measured during this save — `git status --short` clean before staging · `gh pr list --state open` = empty · `git rev-list --count origin/main..main` = 1 before this save (`2fe7da5`, last session's docs commit, unpushed by ADR-0049); count it with that command, never from a number here · branches: local `main` + `claude/rh-cafbc2`, remote-only `gh192-landing-rule` + `gh195-196-play-fixes` · **worktrees: 1, the main tree** · no build, no browser probe, no port opened this session · environment witness unchanged: **CI 34219465181 at `c729431`, Deploy to Azure success** · `~/.claude/CLAUDE.md` still dirty from a rewrite this session did NOT make, plus 4 dirty `~/.claude/agents/*.md` — left untouched, never staged · retired memory originals archived outside git at `<project-state>/memory-retired-2026-09-09/`.
spent: queue 14 rows in at RH → 12 closed out (7 tracker operations + 1 enumeration), 1 moved to its own run, 1 done-but-short-of-its-inherited-number · 2 waves · 2 dispatches (1 `bulk-reader`, 1 `deep-reasoner`), both attempt 1, 0 escalations · ~34 SELF calls · 0 builds, 0 suite runs, 0 REFUTE (no code artifact).
errors: **used a grep to refute a correct claim** — searched gh#213 for `three`, hit "three external resources … Google Fonts", and killed the handoff's true statement that the ticket needs the `three` 3D library; both senses live in that ticket, and the false correction reached an `AskUserQuestion` popup, so the owner's answer had to be re-asked on the record (memory `one-token-two-referents`) · **briefed a liveness pass whose verdicts came back derived from unchecked-box counts**, which is the class `an-open-ticket-is-not-undone-work` already names; caught it and re-derived the two rows whose action depended on the decision, but the brief should have forbidden the count heuristic outright · **wrote a registry walk against a shape I had not read**, assuming a `status` field, and it returned 0 rows before I inspected the JSON · **ran two verification greps in zsh**, both dying to `no matches found` on unquoted `--include=*.json` globs, which `verify-in-bash-not-zsh` already covers.

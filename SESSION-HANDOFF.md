# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-09-18#2
done: `74922d9` `5b0b1ce` — gh#101's เนื้อคู่ copy draft as `docs/copy/nuea-khu.md` + `docs/copy/nuea-khu/cards.md`, local only per ADR-0049. What git cannot show: **the draft is findable because two gh#101 comments name its paths** — the 2026-09-16 draft was lost precisely because none did, and a locally-committed file is invisible to a tracker reader until the next src push, which both comments state · **gh#102 gh#103 did NOT unblock** — the ten cards exist but carry `⚠ UNREVIEWED`, so the block moved from "no cards" to "the owner's edit" · **the owner ruled the open band during this session** and it is recorded on the ticket, not only in chat · **content sweeps were calibrated red on a planted string before being trusted** (alcohol and pair-score patterns), and the UNREVIEWED-marker check was calibrated on a marker-free file · **Thai in a commit subject is this repo's practice** — `git log --format=%s -300 | grep -c '[ก-๛]'` returns 10 including this session's · gates green: check-citations, added-lineno-citation-check, party-size-claim-check (95 surfaces), thai-comments (310 files, 0 Thai comment lines).
dec: open band `50 ขึ้นไป` = relative form `อีกประมาณ N ปีจากนี้`, exact-age question rejected because it adds a fourth question to a three-question screen (owner 2026-09-18, recorded on gh#101 the same day) | criterion 2's restatement must say "above the reader's own age by construction", never a threshold — still owner wording | the draft lands as `.md` under `docs/`, never `src/**`, so unreviewed copy cannot ship | two commits not an amend, so the first message stays accurate about the tree it made | split at 15261B into flat index + same-named subdirectory, the convention `docs/agents/tool-copy-provenance.md` set | no box ticked on gh#101.
next: [ ] gh#101 owner edits the draft in the tree — `docs/copy/nuea-khu.md` and `docs/copy/nuea-khu/cards.md`, both carrying `⚠ UNREVIEWED`; the agent half is done, this is the owner's pass. Verify: neither file still carries the UNREVIEWED banner
next: [ ] gh#101 criterion 2 restatement — owner's wording; it must say "above the reader's own age by construction" at `50 ขึ้นไป` rather than name a threshold, per the ruling on gh#101's 2026-09-18 comment. Verify: the body's criterion 2 names the closed band and the by-construction clause
next: [ ] gh#9 owner registers `watduang.com` — re-run whois immediately before paying, privacy on, auto-renew on; unblocks gh#160 gh#29 gh#19 items 2-4. Verify: `dig +short watduang.com A` resolves
next: [ ] gh#181 box 2 — restate against the 2026-09-10 primary-control-above-the-fold rule or strike it as box 1's threshold already was; owner's wording, no further measurement helps. Verify: the ticket records the choice
next: [ ] gh#181 box 7 — reword so the per-route verdict cites the tracked readings rather than the gitignored images; proposed on the ticket, not applied. Verify: the body no longer asks for screenshots
next: [ ] gh#141 walk all 20 boxes on a real phone; gh#204 boxes 7-9, gh#205 boxes 1-2 and gh#219 box 1 need the same device and the same pass. Verify: every box noted with its device
next: [ ] gh#104 item 1 — the ดูดวง หมวด deck rewrite, 23 prompts and 5 predictions; owner-reserved Thai copy under the 2026-09-10 ruling, and it overlaps gh#97 gh#98 which already own เซียมซี's deck. Verify: the deck no longer promises a group
next: [ ] gh#109 is owner-only end to end — boxes 1-2 are Thai copy and boxes 3-5 are already satisfied per its 2026-08-28 comment; there is NO agent slice. Verify: nothing; do not brief it as mechanical work
next: [ ] gh#102 gh#103 เนื้อคู่ portraits — the ten cards now exist at `docs/copy/nuea-khu/cards.md` but are UNREVIEWED, so the block moved from "no cards" to the owner's edit; read the assets doc before spending. Verify: ten portraits put to the owner
next: [ ] gh#12 needs an owner Google Ads account, NOT the domain — its body says `Blocked by: —`. Verify: Keyword Planner volumes recorded under `docs/research/`, never `.scratch/`
next: [ ] owner supplies the path to `bin/ca` — re-checked 2026-09-17, `ca` is still not on `PATH`. Verify: `ca-selftest` runs green with the new rows
next: [ ] route a section out of `CLAUDE.md` — measured 12034B of 12288B at this save's preflight, 97.9%, and that headroom is what blocked this session's one-line prevention edit; move per the override's homes table, never raise the ceiling. Verify: `check-budgets.sh CLAUDE.md` passes with at least 1KB spare
inflight: measured during this save — `git status --short` before the save showed only this save's files, `gh pr list --state open` empty, no worktrees, no background tasks; `origin/main` = `5ecd439` and HEAD was 4 ahead before this save, all docs commits staying local per ADR-0049, this save's included.
spent: queue 11→11 (gh#101's re-draft closed, the `CLAUDE.md` headroom routing task opened) · batches 1 · dispatches 0
errors: **wrote a doc before measuring its declared budget** — the draft landed at 15261B against the 12288B ceiling every `.md` under `docs/` carries, which this repo's own save-session override states and the `every-md-pays-the-12kb-sweep` memory already records; measured only after writing, then split · **shipped a wrong word in a public comment** — called the relative form "unfalsifiable by construction" on gh#101 when `อีก N ปี` is trivially true, not unfalsifiable · **over-applied the Thai carve-out** — wrote the cards file's section headings in Thai when the rule allows Thai only as the copy itself; caught by the owner, not by me, and no gate covers `.md` scaffolding.

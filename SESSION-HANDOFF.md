# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-09-07#3
done: pushed `052b778..06c1495` (2 commits: `1b5cee4` gh#219 strip-counter observer + its test · `06c1495` gh#210 per-leg summary line), CI 34111676542 success with 0 failing steps, Deploy to Azure step success. `118f040` docs number-corrections stacked locally, NOT pushed per ADR-0049. What git cannot show: **the fork killed a design that would have shipped GREEN** — a strip-only ResizeObserver never fires for the case that matters, because chips carry `max-inline-size` so a below-cap name reflowing on the Sarabun swap moves a CHILD's right edge with the strip's own box unchanged, and the wired chip-visibility probe seeds every chip AT the cap, so no existing leg exercises that path at all; the shipped observer watches the strip PLUS every non-counter child · **two tracks I declared disjoint were not** — wiring gh#219's check as a CI leg would have touched `EXPECTED_LEGS` and the verdict script, which are Track B's files; re-cut so `src/play` and `scripts` are owned by one track each and gh#219's check became a stub-driven test in the EXISTING test file, leaving `EXPECTED_LEGS` at 26 · **REFUTE caught the new summary line contradicting itself on the one leg that runs** — the control force-hides the band with `visibility`, which PRESERVES geometry, so the coverage edge stayed a real coordinate inside the strip and every off-edge chip read POSITIVE slack while the naked count included all of them; the exit code was correct throughout, so only the line itself could ever have disclosed it · fix is slack ABSENT when there is no band (not moving the coverage edge, which would need a second inconsistent definition of a naked cut), plus a coherence guard placed at the VERDICT layer rather than in the probe because it holds wherever the arithmetic moves and, unlike the browser-side template, has a test harness · **the control count is 8, not 9** — one of the nine combos has no hidden chip, so with the band gone there is nothing there to be a naked cut · **gh#210 box 3 closed off a GREEN CI LOG, not an artifact** — the probe-output upload is conditional on failure and is skipped exactly when everything passes, which is what made that box unanswerable before; 6559 log lines retrieved, 2 hits, local 33.59px against CI 34.02px with bad=0 on all nine on both machines, so the two machines agree to 0.43px on an axis recorded as differing wildly (one exemption row moved 0 to 75px) and a future divergence there is a real change, not noise · **gh#181 boxes 3/4/6 measured and box 3 FAILS 8 of 8** — six in-scope routes ship a 1100px block carrying 0 `font-size` and 0 `padding`, only the 44-to-56px control floor, while `short-stick` and `pinocchio-luck` have NO 1100px block at all; the tracer reads 4 `font-size` and 2 `padding` by the same brace-accurate instrument, so the zeros discriminate rather than reflecting a blind probe · box 4 fails on 5 of 8 (`freeze-tap` `timebomb` `dice-loser` omit the track token silently, `power-meter` collides two rules under one token name, `short-stick` has no tokens, `pinocchio-luck` retired its block with no reason) · **box 6 passes BY CONSTRUCTION on six and is VACUOUS on two** — the extractor owns only `markup.html`/`style.css`/`main.js`, so `overrides.css` and `play.css`, where every desktop rule in that ticket lives, are outside the registry's reach entirely and the check says so in its own output · registry parsed exactly: 23 fragments across 9 routes all `deliberate:true`, `markup.html` keys on 8 routes, `main.js` on 2, ZERO `style.css` — this supersedes any earlier "all markup, zero main.js" reading · **gh#219 box 4: FOUR routes ship a player strip, not three** — `cursed-number` has three real strips filled by its own render function, imports the shared module nowhere, and wraps with the horizontal axis left visible, so it has no trailing edge, no counter and no stale number to have · **the browser proof's discriminator is an independent recount in the SAME evaluate call** — base band stayed `+8` while that recount read `4`, which is the only way to tell "nothing changed" from "changed but stale" · **#220's hazard confirmed by measurement** — the mockup wrapper carries 2 live `min-height` declarations, the shipped stylesheet carries 0 with a prose comment in their place, and all three `overrides.css` wrapper blocks set no floor, so a re-extraction restores it unneutralised · **NEW instrument gap, unticketed** — the divergence script's mockup enumeration filters candidate dirs on an exact `index.html`, and `short-stick`'s mockup dir holds a suffixed one, so it is dropped BEFORE the count while the report's dir/paired/unpaired arithmetic still closes and discloses nothing; 12 of 14 dirs hold an exact index.
dec: mechanism=ResizeObserver over strip+children per the fork, ownership answer recorded in the module (observed set = the loop `trailingOverflowCount` runs, repo-owned, ADR-0056) | observer stored in a module `WeakMap` not under the `dataset` flag (dataset holds strings, not objects; get-or-create also survives a module reload that leaves the flag set) | slack=absent-when-no-band, NOT coverage-edge-moved | coherence guard at the verdict layer, not the probe | `EXPECTED_LEGS` untouched at 26, no new CI leg this run | transform gap NAMED not guarded (an observer reports box size, the count reads a rect including transforms; two routes scale their active chip, nothing toggles it in place today) | gh#217 relabel SKIPPED — it was closed and the label existed to prompt that close | gh#160 given `ready-for-human`, not merely stripped of `ready-for-agent`, since a label-less ticket is worse and its blocker is human work | NO comment on gh#214 — outside the six-ticket comment grant, its words cited in the report instead | `CLAUDE.md` trim RECOMMENDED AGAINST, owner may override — it passes at 11874B and the recoverable bulk is incident narratives whose specificity IS the rule | SendMessage unavailable this session, so REFUTE's fixes were taken SELF (each smaller than a dispatch).
next: [ ] **gh#181 boxes 3 and 4 are now agent work with a measured baseline.** Box 3 needs a real type/spacing scale per route, not a widening; box 4 needs the track token declared or its absence reasoned, on the five named routes. Verify: each in-scope route's desktop block carries `font-size` and `padding` declarations, and every token is either declared or has a written reason
- [ ] **file the mockup-enumeration gap as its own ticket.** The exact-`index.html` filter drops a mockup dir before counting and the report still closes. Verify: the ticket exists and names the filter plus the suffixed dir it drops
- [ ] **gh#213 — owner reads the sequencing.** Does "sequenced after Bomb" mean that ticket closes or that the one-bomb route lands? Verify: the ticket says which; the chain was already departed from either way, which is posted
- [ ] **gh#202 — owner answers the one question.** Close on the instrument and spin box 5 out? Verify: the ticket records the split, and box 5's new home names `wire-snip-panic` and `pinocchio-luck`
- [ ] **#220 — owner scopes it.** Narrow converts each absence into a presence; structural lets the registry assert a fragment must stay absent. Verify: the ticket says which and why the other was declined
- [ ] **gh#181 — owner restates or strikes box 1 and answers box 8.** Verify: box 1 reads as something evaluable, box 8 answered or waived
- [ ] **gh#210 box 2 and gh#219 box 1 — owner looks.** A truncated long name's legibility, and a real-device rotate both directions. Verify: each ticket records the look or a waiver
- [ ] owner physical blockers, unchanged: gh#9 domain still NXDOMAIN, re-probed with a live positive control (blocks gh#160 gh#29 gh#12 gh#19) · gh#141 real-phone walk (blocks gh#204, gh#205 rows 1/2) · gh#182 320/390 scroll (blocks gh#181 box 5) · gh#150 read (blocks gh#213) · owner standing gh#192 gh#165 gh#112 gh#109 gh#106 and the fortune family gh#97-gh#104
inflight: measured during this save — `git status --short` clean before the save · `gh pr list --state open` = none · no probe, serve or Chrome listeners (`lsof` on 4321/9222/9344-9348 = free, `pgrep` = 0) · branches `main` + `claude/rh-cafbc2` only, the latter deliberately kept · worktrees: one, the main tree · unpushed on main = 1 before this save (`118f040` docs), plus this save's commit, both docs-only and NOT pushed per ADR-0049.
spent: queue 12 rows in — 6 done, 4 parked on the owner, 1 measured-only, 1 declined with a reason · batches 6 waves, 11 dispatches, 11 attempts, 0 failed, 1 REFUTE round of max 2, 0 re-forks
errors: wrote REFUTE's PREDICTED control count into three source comments without measuring it, and the real value was 8 — that forced a second full 579s suite run, because the push condition is green after the FINAL fixes and my correction landed after the first green · read a compound command's exit code as the suite's three times running; the harness reports the LAST command's status, so a trailing `tail` or `echo` reports 0 over any verdict, and the real code came from the runner's own recorded line · put backticks in a comment sitting INSIDE a browser-side template literal, which closed the template; the file's own neighbouring comments use double quotes for exactly that reason and I had them on screen · wrote a verification probe whose lazy regex truncated the template body and reported a parse failure on a file `node --check` accepted, caught only by running the same probe against HEAD as a control · measured a 1100px media block with an `awk` range that ran to end of file, a superset, and nearly refuted a correct agent finding · counted a registry's `_readme` filename mentions as entries · truncated two `grep -A` windows short of the declaration under test, once making a present value look absent · read an empty field from a missing file as a zero rather than as a failed instrument. One class throughout, and it is the same one the last two entries record: the counting method is itself a claim and it is the half that gets skipped. Ten slips, none reached a file or a ticket; three were caught only because a control was run against a known-good case first.


# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state


### S2026-09-04#3
done: 2 src commits PUSHED, `origin/main` = `5a7a18b` — `1b2f9d3` (gh#202 horizontal axis, 5 files) + `5a7a18b` (4 exemption rows). CI green on the second, deployed. What git cannot show: **REFUTE r1 REJECTED the first design outright** — the axis was folded into `overflowPx`, whose row key carries no axis, so a row excused for vertical scrolling silently absorbed a brand-new sideways clip; rebuilt with its own gated number, own map, own ranker, gating every row · **its unit test could not fail** (a filter plus its complement, and a constant equal to its own literal); now falsifiable and I reproduced the mutant myself rather than trusting the claim · **the first CI run went RED and the axis was calibrated on ONE machine** — 4 rows on only 2 routes, and one row read 0px locally against 75px on the runner, so a single-machine reading of this axis is worth nothing · **a failing probe leg printed only its last 3 lines**, which on the fit probe were 3 warnings and no error, so the CI log said a leg failed and showed nothing that could have caused it — the real reason lived only in the uploaded artifact, and the leg now prints its error lines first · the play-exit probe carried **THREE** false load comments, not the 2 on record, and the replacement was ALSO stale because `nav` clears local storage in the same commit that added the retry, so that retry now rests on nothing · **"ten runs, zero variance" has no artifact** anywhere under the verification tree, only session prose · CI cost measured: browser probes are ~85% of a median run, `npm ci` median is 5s and the 302s I quoted was a 1-in-30 tail matching npm's default fetch timeout on the audit request, checkout is 1-2s · shallowing the clone is a SILENT blinding — a depth-1 clone scans zero files and exits 0.
dec: owner 2026-09-04 — push approved with the suite evidence · **play routes MAY carry gutters at 320px**, content need not reach the physical edge, recorded on gh#181 and gh#203; the criterion as originally written would have been met by deleting the very reserve that stops a shake crossing the viewport. gh#180 CLOSED, its ruling resolved it without CSS; gh#183 and gh#184 are NOT closable and gh#183's body still describes the half that moved to gh#203. gh#206 gh#207 filed. Mine: an exemption map records the LARGER of two machines and rows reading zero on BOTH are deliberately not pre-exempted, because an exception nobody can trigger is a licence to regress · splitting a probe pair across lanes is rejected by that file's own comment, since each lane runs its own browser and the control would stop being one-variable · an outlier is not a cost, and I quoted a 1-in-30 tail as typical after writing the rule against it · a grep can match prose and I reported a comment as a live call, the second instance of that class · read the whole block before proposing a change, because the paragraph three lines below the one I read already refused my plan.
next: [ ] rebalance the browser probe lanes off `LEG_SECONDS` from a real CI run — they are ~85 pct of the run and the only lever that matters. Verify: per-leg seconds collected from a CI artifact, and the slowest lane's total drops with no probe pair separated from its control
- [ ] add the no-audit flag to the npm install step. Verify: a run whose install step prints the package count with no audit line, and no gate references audit
- [ ] commit the `LEG_SECONDS` instrumentation once the running suite confirms it. Verify: suite exit 0 and the leg-seconds lines present in its probe output
- [ ] gh#181 needs the owner to view screenshots and say the routes read as designed; gh#183 needs its body rewritten to the half it kept; gh#184 has 3 boxes needing a browser
- [ ] owner: rank gh#204 gh#205 against the current focus · gh#192 (b)(c)(d)(e) unanswered · gh#9 domain still blocks gh#160 gh#29 gh#12 gh#19 · standing gh#165 gh#150 gh#141 gh#112 gh#109 gh#106 and the fortune family gh#97-gh#104
inflight: measured during this save — `git status --short` shows only `scripts/ci-probes.sh` modified, deliberately uncommitted pending its suite · gate suite run 4 STILL RUNNING in the background, no exit marker yet · `gh pr list --state open` 0 · local equals `origin/main` at `5a7a18b`, 0 ahead · latest CI on main success · 1 worktree, the two agent worktrees removed this session.
spent: queue 10 to 8 · batches 7 waves, 16 dispatches · 4 full gate-suite runs, 1 red and it was the new gate catching real defects · REFUTE 2 rounds, both FAIL, 10 findings, every one closed or disclosed.
errors: quoted a single outlier run as a typical cost after writing the rule that an outlier is a claim about the instrument · grepped for git usage and matched a COMMENT, reporting a file as a history consumer when it holds zero git argv tokens, the second instance of mention-counted-as-use · proposed splitting a probe pair from leg COUNTS while the comment three lines below refused exactly that, and lane1's two legs each walk every route so it was never idle · accepted a comment fix after checking only that its new wording had provenance, never that the code still had the problem it described · read a test verdict through a pipe so the exit code was the pager's · adopted an inherited plan's ordering without checking it, when the ticket itself named its blocker.

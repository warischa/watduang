# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state
### S2026-08-26#3

done: **5 commits `96b385f`→`708e79b`, pushed. First push CI RED on `added-lineno-citation-check`, fixed in the 5th commit** · **วงล้อหมุนผิด was THREE defects, not one** — `#wheel-group` had no `transform-origin` and SVG defaults to `0 0`, so every rotation pivoted on the svg corner and swung the disc off its hub (found only by driving a browser; tsc+tests+build were green throughout, rendered CSS has no gate) · `reveal()` re-sliced the disc after announcing, pointer landed on a different name (found by arithmetic) · the eliminate checkbox could redraw at rest over a live reveal, restoring defect 2 on one tap (found by REFUTE, in my own approved fix) · new `scripts/wheel-pointer-name-probe.mjs` hit-tests the painted wedge vs the result text, 3 spins x 320/390, calibrated per defect and the two fail with DISTINGUISHABLE symptoms (real-wrong-name vs pointer=null); in the red leg 2 of 6 spins passed by luck, so a single-spin probe would have called it healthy · shared name panel → one `<textarea rows="20">`, chips/add-button/empty-state deleted, 9 rows under `max-height:700px` via `calc(14.4em + 25px)` (88%→42% of a 568px phone; em not px so it survives a raised browser MINIMUM font size — NOT the default-font-size setting, which never scaled an explicit 15px) · duplicates KEPT and that broke both callers: wheel+draw eliminated by NAME via `Set<string>`, so two แนน died together and a 2-person wheel announced `(คนสุดท้าย)` after one spin — now keyed on roster POSITION (`Set<number>` + `remainingSlots`/`slotTokens`), safe because `players` is assigned in exactly one place, the `watduang:start` handler, which also clears the eliminated set and the rotation · `โดน: ` off the result line only, 4 other occurrences + `manifest.ts` kept per owner · 6 stale strings on 3 pages lost `ด้านบน` and `เลือกหรือ` (gh#112 box 2) · gh#115 crawl gate read ZERO tool pages, now 4 tool + 6 game, counts traced to what was scanned, calibrated per page 4/4 · `ci-verification.md` 11447→7399B routed to `ci-gate-calibration.md` on the ADR-0012 seam · `browser-verification.md` trap 6 (a sample ON `rect.bottom`/`rect.right` hits an unpainted sub-pixel row; the 196-vs-19 scan was ad hoc and never committed, which is why all 9 committed `elementFromPoint` scripts classify clean) · new `tool-copy-provenance.md` split per page (a growth registry cannot sit 3 bytes under a fixed ceiling) · 3 artboards redrawn + panel moved below the wheel, ordering proved by DOM byte offset · gh#114 confirmed on THREE pages incl the wheel at +88/+56, always there and merely unmeasured · 2 REFUTE rounds, the cap
dec: owner 2026-08-26: duplicates KEPT and wheel+draw fixed to eliminate by index, not deduped · textarea 9 rows on small screens, not 20 · gh#114 ADR-0024 exemption PARKED, owner decides · gh#112 re-scope POSTED as a comment not a body edit · `ด้านบน` DELETED not swapped to `ด้านล่าง` (position-free copy cannot rot) · empty-state element removed rather than reworded · agent-authored Thai is permitted when DISCLOSED in a committed provenance file, which is gh#112 box 4 — ADR-0026 says the set is guarded at authorship because authorship is unrecoverable from the tree
next:
- [ ] **FOCUS owner: gh#114 ADR-0024** — the ad slot moves at round start on draw, team AND wheel; number is clean. Exemption or fix. Done when the ADR records the decision
- [ ] owner: is `refreshReserve` in `team.astro` broken or merely bypassed? Its own comment claims to cover the moment that measured red. Done when one of the two is established by a probe
- [ ] agent: file 3 tickets NOT filed this session (owner never answered) — the draw test pin is partly theatre and would miss an indexOf-by-name regression; `reveal()` adds to the eliminated set even when the checkbox is unchecked so ticking it later retroactively eliminates earlier picks; gh#114 should record three pages
- [ ] agent: `added-lineno-citation-check` has NO package.json entry, so no local sweep can invoke it and it can only fail after a push. Done when `npm run` reaches it
- [ ] agent: that same gate honours GITHUB_EVENT_BEFORE only inside CI and silently scans HEAD~1 locally while printing that it did. Done when a local invocation can reproduce a chosen range, or the script says plainly that it cannot
- [ ] agent: `check-citations.mjs` does not recognise prose-form citations, which is why two evidence files quoted a heading that had not existed for some time. Done when that form is either policed or documented as unpoliced
- [ ] agent: the crawl gate falls open on a MISSING category while throwing on a missing navClass, and reads the category off the page under test (ADR-0030 class). Done when both are closed or recorded
- [ ] owner: gh#111 box 3 — a word for the union of เกม and ดูดวง pages. ADR-0040 left it open on purpose. Blocks closing gh#111
- [ ] owner: Thai copy for gh#101, gh#97, gh#99. Blocks every content ticket
- [ ] owner: does a category page get the top bar? Done when the allowlist is decided
- [ ] owner: gh#89 rule — done when it names permitted surfaces, not banned strings
- [ ] owner: judge the reworked name panel and the wheel by eye. Only machine checks so far this round
- [ ] owner: the ADR-0033 deviation is recorded only in `canvas.json`; decide whether it also needs a real ADR amendment
- [ ] owner-run: gh#9 domain then gh#29 AdSense, still blocking the last box on gh#15 gh#16 gh#17 gh#18 · gh#13 real-device script · gh#12 · gh#19
- [ ] agent, PARKED by owner 2026-08-25, needs a new decision: re-run the hero as a true transparent cutout
inflight: measured at save — dirty paths are this save's own writes only · 0 unpushed once this save pushes · 0 open PRs (checked) · 1 worktree, the main checkout (checked) · 0 background tasks beyond the CI monitor (checked) · one branch only, `main`, 0 ahead 0 behind, so the merge asked for was a genuine no-op and was NOT performed — asserted on the graph, not on an "Already up to date" message
spent: queue 13→15 · batches 5 · ctx 32.5% of 1M at the last measure · 0 tickets filed (owner did not answer) · 1 tracker comment posted · CI red once and fixed · 3 self-inflicted measurement errors caught (grep -lc counted 42 files where 9 matched · session.evaluate needs an explicit `return` · GITHUB_EVENT_BEFORE is ignored locally)

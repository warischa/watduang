# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-09-18#3
done: `32c7efd` `e88c4b3` `a7531a2` `ab03fe3` `6d3c254` — CLAUDE.md routed 12034B→11262B, three criterion rewordings posted. What git cannot show: **three issue BODIES were edited and no commit records them** — gh#181 boxes 2 and 7, gh#101 criterion 2, each built by a script that REFUSES on ≠1 match, diffed before applying, read back byte-for-byte (8 checkboxes, box 8 still ticked, only trailing newlines differ) · **that refusal guard was calibrated red** on a body whose line was already replaced, before any green was trusted · **a MOVE out of CLAUDE.md was never available** — `docs/agents/issue-tracker.md` 12078B and `docs/agents/src-edit-rules.md` 12114B sit inside 250B of their own ceilings and `docs/agents/triage-labels.md` is a label lookup table, the wrong seam under ADR-0012, so the routing was deletion-to-pointer with NO destination doc growing · **the ≥1KB crit was met by 2 bytes** (1026B spare), so the next CLAUDE.md edit reopens that task · **box 7 carries a second defect its ticket never raised** — it says "four routes" while `docs/verification/evidence/181/README.md` § "Route set" has asked since 2026-09-08 for "eight", and the two sets overlap by TWO not four, `how-close-is-near` and `cursed-number` being named in the title and never measured at 1440×900 · **that "2026-09-05 ruling" is cited from an agent-written doc, never read back from the owner's own comment** · gates green: check-citations (after it caught one dead citation of mine), added-lineno-citation-check 0 new, thai-comments 310 files 0.
dec: gh#181 box 2 = restate against the 2026-09-10 reachability rule, not strike (owner 2026-09-18) | gh#181 box 7 = reword the images clause ONLY, route count left exactly as written, a deliberate deferral not an oversight (owner 2026-09-18, after a multiSelect answer returned both "eight" and "four" and had to be re-asked) | gh#101 criterion 2 = above the reader's own age by construction, never a threshold, proven at the open band (owner 2026-09-18) | CLAUDE.md narrative routes by the homes table's "history of revised decisions lives in the ADR or the archive" — rules stay, dated evidence goes | ticket-body edits are fetch → single-match replace → diff → read back, never a hand-assembled `--body`.
next: [ ] gh#181 route count — does box 7 govern four routes or eight? Evidence and both consequences are in `docs/gh181-box7-route-count.md`; the 2026-09-05 ruling it rests on must be read back from the owner's own comment first, not from the README. Verify: the ticket states one number, and the title plus § What to build agree with it
next: [ ] gh#101 owner edits the draft in the tree — `docs/copy/nuea-khu.md` and `docs/copy/nuea-khu/cards.md`, both carrying `⚠ UNREVIEWED`; the agent half is done. Verify: neither file still carries the UNREVIEWED banner
next: [ ] gh#9 owner registers `watduang.com` — owner-only and financial, never an agent action; re-run whois immediately before paying, privacy on, auto-renew on; unblocks gh#160 gh#29 gh#19 items 2-4. Verify: `dig +short watduang.com A` resolves
next: [ ] gh#141 walk all 20 boxes on a real phone; gh#204 boxes 7-9, gh#205 boxes 1-2 and gh#219 box 1 need the same device and the same pass. Verify: every box noted with its device
next: [ ] gh#104 item 1 — the ดูดวง หมวด deck rewrite, 23 prompts and 5 predictions; owner-reserved Thai copy under the 2026-09-10 ruling, overlapping gh#97 gh#98 which already own เซียมซี's deck. Verify: the deck no longer promises a group
next: [ ] gh#109 is owner-only end to end — boxes 1-2 are Thai copy and boxes 3-5 are already satisfied per its 2026-08-28 comment; there is NO agent slice. Verify: nothing; do not brief it as mechanical work
next: [ ] gh#102 gh#103 เนื้อคู่ portraits — the ten cards exist at `docs/copy/nuea-khu/cards.md` but are UNREVIEWED, so the block is the owner's edit; read the assets doc before spending. Verify: ten portraits put to the owner
next: [ ] gh#12 needs an owner Google Ads account, NOT the domain — its body says `Blocked by: —`. Verify: Keyword Planner volumes recorded under `docs/research/`, never `.scratch/`
next: [ ] owner supplies the path to `bin/ca` — re-checked 2026-09-18, `ca` is still not on `PATH`. Verify: `ca-selftest` runs green with the new rows
inflight: measured during this save — `git status --short` before it showed a clean tree, `gh pr list --state open` empty, no worktrees, no background tasks; `origin/main` = `5ecd439` and HEAD was 10 ahead before this save, all docs commits staying local per ADR-0049, this save's included. `~/.claude` carries 3 pre-existing modified files this session never touched and did not stage.
spent: queue 12→9 (box 2, box 7's images clause, gh#101 criterion 2 and the CLAUDE.md routing closed; the gh#181 route count opened) · batches 2 · dispatches 0
errors: **committed past a gate's red** — joined `check-citations` to `git commit` with `;`, so the gate printed `1 dead citation(s)` and the commit landed anyway; the gate also exits 0 while printing it, so neither the separator nor the rc would have stopped it · **shipped a half-path citation into a public ticket body** — `evidence/181/README.md` resolves from neither the citing file nor the repo root, and no gate watches an issue body, so it was caught only when the same string was checked inside a `.md` · **built a multiSelect popup holding two mutually exclusive options** — "state eight" and "keep four" were both selectable and both selected, costing a whole extra round to disambiguate what one radio question would have settled.

# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-09-13#1
done: pushed `944352e..037d054` (4 commits), CI run 34739212949 success — 58 steps success, 1 skipped, 0 failed, **Deploy to Azure success**. Closed #233 #150; evidence comments on #236 #237 gh#182 #234 gh#102. What git cannot show: **5 of the RH queue's 12 rows were dead or materially reshaped at intake** — my own RH line briefed gh#182 as owner-blocked when its last comment had already replaced six per-route decisions with one rule AND moved the label to `ready-for-agent` · **there is no gh#101/gh#102/gh#103 cycle** — gh#102's own body carries `Blocked by` pointing at gh#101, gh#101's body says it ships ten cards with the portrait box empty, and the tree has no cards, so it is a two-phase chain whose only blocker is owner Thai copy; I had declared it settled from two sources before the third reversed it · **the font ticket's premise was the smallest of three holes** — coverage was pooled across faces so regular could cover for bold, and pairing was derived from disk so a rename gave exit 0 on a divergent pair (REFUTE reproduced both) · asserting cmap identity COSTS the same as reading the woff2, because it requires reading it — I priced it at ~15 lines vs ~100 in a popup and was wrong · **#236's connector carried a real unmeasurable run for the first time on any machine** · **`overflow:hidden` on wire-snip-panic reverted** — measurement showed the 63px it would clip contains the primary control itself · **agy's workspace trap is CLOSED at 1.2.2** (measured, C1) so writers stayed agy and cross-lineage review held · **I re-dispatched a builder that was still running and put two writers on one file** — `out-<step>.json` is written at COMPLETION, so a live agy dispatch reads `NOMETA` exactly like a killed one, and the skill says NOMETA means killed, re-dispatch. T3 then finished having done the whole job (edit + fold probe n=3 + horizontal check + route test 7/7 + 7 gates green + evidence). It ended clean only because T3 landed first. I compounded it by diagnosing T3's "failure" from `runtime/stderr-52342.log`, **which is dated Sep 5, eight days stale** — those logs are never cleaned when non-empty and nothing maps pid to dispatch. Codex's T3b was the only real browser-lane failure (built clean, Chrome exited before CDP); native ui-driver worked first try twice.
dec: `reference/ceilings.md` is VOID — measured at codex 0.153.4/agy 1.1.27, now 0.154.0/1.2.2; re-measured only the agy workspace ceiling, not the file | #237 = assert cmap identity (owner), not woff2-only, not dropping the .ttf | pinocchio-luck primary control = `#ready` only (owner) → route does NOT violate; its `.answer` option C 0px reading stands on record but is not the designated control | wire-snip-panic lands partly-visible-and-reachable, the recorded precedent, because the 2026-09-12 ruling rests on this family having NO clipping context | woff2 reader ceiling NAMED not chased — it answers which codepoints a cmap maps, never whether a browser can load the file, a set the format owns | `--expected` restored bound to `--dist` after I deleted it and reded 8 tests | image generation routed to codex per owner, so `docs/agents/assets.md`'s opaque-default measurement (qwen path) does not cover it | #232 = runtime arm question deliberately unmeasured per owner ruling 2026-09-14; probe deleted rather than wired into CI | #237 = a shipped face the gate cannot read FAILS (owner 2026-09-14), not disclosed-and-passed, and not narrowed to the face the stylesheet selects — that set is owned by CSS and the browser, not the gate.
next: [ ] font-coverage-check's zero-font escape bypasses `EXPECTED_STEMS` — if every expected face disappears the gate exits 0 with SKIP, so a vanished inventory reads exactly like a build that self-hosts nothing. Found by REFUTE 2026-09-14, PRE-EXISTING and not caused by the FAIL change; closing it means ruling when "nothing self-hosted" is legitimate. Verify: a tree with the four expected faces deleted reds, while a genuinely font-free build still passes
next: [ ] #234 bangkok-drift fills the fog rect before drawing sprites so they stay crisp — confirmed correct by two independent reads, fix needs a visual check no gate encodes. Verify: rendered frames compared before and after
next: [ ] gh#101 needs owner Thai copy — it is the single blocker for the whole fortune family; gh#102 portraits cannot start because its ten cards do not exist. Verify: copy lands and the cards ship with the portrait box empty
next: [ ] gh#182 remainder — freeze-tap owner-reserved and untouched; every other route now ruled or fixed. Verify: the ticket records freeze-tap as the only open row
next: [ ] fortune family remainder gh#104 gh#106 gh#109. Verify: each ships Thai copy and the owner reviews strings
next: [ ] #184 #181 #210 #219 — chip strip, desktop phone-width column, the +N band's ungated max width, the one-shot overflow counter. Verify: each records a MEASURED reading, never a calculated one
next: [ ] gh#141 real-phone walk owes gh#219 box 1, gh#205 box 6 rows 1-2, gh#204 box 7, gh#210 box 2, croc-bite's 1976ms handled-pointer-gap row. Verify: each records a reading
next: [ ] owner physical blockers unchanged — gh#9 no A record no NS, blocking gh#160 gh#29 gh#12 gh#19. Verify: A record resolves
inflight: measured during this save — `git status --short` clean apart from this save's own files · `gh pr list --state open` empty, checked · `main` = `origin/main` at `037d054`, CI success, Deploy success · ports 4178/9339 free, checked · no background agents · NOT verified: whether a real phone feels croc-bite's first tap as laggy, and whether the two locally-unrunnable CI gates would behave differently on a non-main branch.
spent: queue 12→9 · batches 6
errors: adopted my own RH line briefing gh#182 as owner-blocked without reading the ticket's last comment · declared the gh#101 ordering settled from two sources when the third reversed it · **four separate `head`/`tail` truncations reasoned from as if complete** — hid a cited test file, undercounted 14 routes as 10, and caused a load-bearing flag to be deleted (8 tests red) · wrote a Verify stating a symptom ("does not scroll") that a clipping mutant satisfied · put a wrong cost (~15 vs ~100 lines) into an owner popup · read an exit code through a pipe, the trap this repo documents · reported a port leak that had cleared between two probes · **re-dispatched a live worker on a NOMETA that meant "still running", then justified it from an 8-day-stale stderr log**.

# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-08-31#2
done: merged+deployed 5 commits `92cda55..7980b79` — `2ef122a` (gh#171 runner) `b90fce5` (gh#170 five gates retargeted, two probes kept) `8bb2cc8` (nine routes fixed + `_arm-gate` onArm hook) `e762e52` (nine set-pinning tests) `7980b79` (worktree exclusion where the stripper cannot erase it). CI 33368402120 success incl. deploy. What git cannot show: **the runner caught a red step nine hand-run gates missed** — I verified by hand, called it green, and `run-workflow-gates.sh` failed on repo-root-walk, a step I had never run. gh#171 catching me the day it shipped. **The rewritten gates found far more than gh#170 estimated**: 12 controls under 44px not 2, then a 13th once the taller rows made it measurable; 5 motion routes not 1, two of them the mirror defect (JS guard present, stylesheet unguarded); arm-gate's 9 confirmed exact. **A silent break nobody was hunting**: arming a route kills saved-group restore, because `.click()` on a disabled control dispatches nothing — no error, no exception, no red test. Four workers hit it independently and answered it three different ways before `drive()` unified all nine bridges; short-stick needed a second-order fix on top, its seat loop read the flag the arm had just set. **stripComments desync, second gate**: repo-root-walk pairs a glob's slash-star with the next star-slash — 22 openers against 3 real closers — and blanked the exclusion literal I had just added. Same class as ADR-0055, filed not worked around twice. REFUTE 2 rounds, both ship; round 2 found both new detectors hunting in their dead branch. Corrections to S#1: `claude/rh-cafbc2` was NOT deleted, still local at `18b9e76`, 8 commits and 847 lines; `ad-slot-game-probe` does not import a delisted file, love-match exists; pinocchio's `#start` at an invalid count was never startable, `startMatch` re-checks — affordance, not exploit. 29 open at resume, 29 now (2 closed, 1 opened).
dec: owner rulings 2026-08-31 — reduced-motion by-eye review DEFERRED to post-deploy (ADR-0046 amended, a decision not a lapse) · pinocchio takes option (b), re-validate when the arm window closes · short-stick's re-render seam stays unarmed · the 400ms modal hold is accepted, those three modals have no other exit. Mine, logged: DECLINED the granted pre-auth to delete two probes — condition met, premise refuted, both were named by a live gate's success line · declined REFUTE's full-inversion for the runner and shipped deny+require instead (ADR-0056), because an allowlist is a second GitHub-shaped set that rots silently · spent REFUTE round 2 deliberately, the fix crossed a production-credential surface · fixed violations in-batch rather than allowlisting, per the owner's standing gate rule.
next:
- [ ] agent: gh#151 boxes 5-7, the evidence run. Boxes 1, 3 and 4 are VERIFIED DONE this session (bounds pinned; live region carries no time; the accumulated-interval mutant reds — re-run by me). Verify: canvas readback reds against a stubbed renderer in BOTH motion modes, plus a walked 320px round, evidence committed under `docs/verification/`
- [ ] agent: gh#172 the stripComments desync in repo-root-walk — its stripper pairs a glob opener with the next real closer, so any script glossing its scope with globs can have a real exclusion erased. Verify: a script carrying both a glob comment and a live exclusion passes, calibrated both ways, and the hoist workaround in checkpoint-writer-check comes out
- [ ] agent: six of nine routes still have no `arm-reveal-paths` test, so a new reveal path ships ungated behind a green route-level gate. Verify: each new test reds on an unclassified receiver and on a removed arm call
- [ ] owner: gh#152 unblocked by ADR-0053 · gh#165 no sound toggle anywhere but cannon-flag · gh#75 box 7 struck but the ticket needs your close
- [ ] owner: gh#9 the domain, which gates gh#160 gh#19 gh#12 · gh#29 AdSense has no account · gh#141 carries 15 real-phone rows
inflight: measured at save — tree clean apart from this save's own docs · 0 open PRs, checked · no bg tasks · main == origin/main at `7980b79` · CI 33368402120 success incl. the deploy step · 1 worktree (the checkout), 6 local branches incl. `claude/rh-cafbc2` which is UNMERGED and deliberately kept · watduang.com still does not resolve, the Azure origin is the live target
spent: queue 6 -> 4 · batches 1 src push (green first try) · REFUTE 2/2 · 13 agents
errors: ten briefed claims wrong, six broken probes, none caught by re-reading my own work · four passed a ticket's or a comment's number through as fact — the 44px count was 6x low, an exception mechanism was never implemented, a fix was aimed at an extractor-overwritten file, and a gate's file list was read as where buttons are built · twice briefed a rule from the wrong ADR, telling a guard detector to scan raw (fails open) and pinning a per-directory presence check where ADR-0046 requires per-mechanism · once stated an inference as near-fact, which hid 21 ungated buttons and was caught only because I had labelled it inferred · one brief-versus-brief contradiction, exempting a route in one and requiring the exempted thing in another, caught by the stale-exemption leg of the test I had commissioned.

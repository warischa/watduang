# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-09-08#5
done: `516c1bd` `ea0ca9c` `a90084e` `11419ae` `cbd3feb` `69599b6` `c729431` pushed as `267674f..c729431`; CI 34219465181 success, 56 steps (53→56, the three new gates), 1 non-success = `Keep the probe output when anything fails` **skipped** — failure-only step, correct — Deploy to Azure success, LIVE. One `/orchestrate` run closed six tickets: gh#220 gh#112 gh#226 gh#165 gh#227 gh#224. **Every track accepted on attempt 1; 0 escalations, 0 re-forks, REFUTE 1 of 2 rounds.** What git cannot show: **the fork rejected my parallel plan on a mechanism I had not seen** — `gate-selftest-coverage-check` reds any `scripts/*check*.mjs` on disk not reached by BOTH executors, and `scripts.ci` is ONE line of 1166 chars with 36 `&&`, so two tracks adding a gate entry is a same-line lost update; `package.json`, `ci.yml`, the manifest and `ci-probes.sh` became orchestrator-reserved and were wired at integration · the fork also corrected the shared-surface list: `run-workflow-gates.sh` needs no hand edit, it parses `ci.yml` live · **FOUR ticket premises were false and each was verified before briefing**: `timebomb` HAS a `markup.html` (gh#165's last comment and gh#227's body both say it does not, and `git ls-files` lists 9 files) · gh#165 box 1's own predicate was a guard that could not fail, classifying by `AudioContext` in the main script when timebomb has that string in a COMMENT only and the constructor is two imports away, so the "produces sound, no control" count was 0 today and not 1 · the sound-button count is TEN not "the nine", because `how-close-is-near` spells its button `btnAudioToggle` and a case-sensitive match misses it · **gh#213's three owner questions were already answered on 2026-09-06** as `croc-bite` / จระเข้งับ / `party`, and I put them to the owner anyway from reading only the LAST comment — same truncation class as last session, one comment further back · **gh#104's handoff claim "already out of src/" is FALSE**: 19 of 24 SHIPPED เซียมซี prompts direct the reader at a group (`ให้วงฟัง`, `บอกวงว่า`, `ให้ทุกคนช่วยกันเดา`) against that file's own header "one person, one draw", measured by importing the module because the deck is a runtime array; `party-size-claim-check` is green and correct since none names a number, which is the exact gap the ticket body predicted · **gh#182 is NOT blocked on a missing claim** — its 2026-09-05T17:14 comment enumerates all six routes in a block-budget table and names the three non-reproducing numbers; the 2026-09-08 finding conflated the 2026-08-31 SETUP-screen six the body forbids with the 2026-09-05 PLAY-screen six, and the "run #179 and close" exit is dead because all six rows already sit in `KNOWN_OVERFLOW` so the probe is green by construction · the browser walk independently reproduced two of those three numbers, `short-stick` 156px against a recorded 191 and `freeze-tap` 160px against a recorded 187 · **REFUTE found a guard that cannot fail** in code I had already accepted: the new arm test matched a lazy but UNBOUNDED span, so deleting the first-paint arm let it run ~4000 chars forward and match the call inside `closeResetDialog` — reproduced before fixing, then sliced · **and a player-facing hole**: `one-bomb` is the only route where both entry files import `shell/audio`, so reverting `main.ts`'s no-WebGL sound wiring left the text gate green, every test green, and a no-WebGL device with a dead sound switch · the fit probe's growth check only `console.warn`s and is absent from its exit condition, so a grown `KNOWN_OVERFLOW` row cannot red — read its warnings, never its exit code · `SendMessage` unavailable again, so every REFUTE spot-fix was done by me rather than the authoring agent.
dec: approach A′ (shared tree, no concurrent builds, one builder per wave) over the fork's recommended B′ — worktree base is session-start HEAD here, which strands every wave after the first, and the fork named that as its own flip-fact | `package.json` · `ci.yml` · `src/games/manifest.ts` · `scripts/ci-probes.sh` reserved to the orchestrator, each brief reporting what it wants added | ADR-0064 NEW — a site-wide preference is not the checkpoint slot; many writers are safe because a colliding write costs one tap, not an unreconstructible round; it also now records the no-migration decision that six route files cite it for | ADR-0063 Decision 1 WITHDRAWN by its own trigger clause, original text kept | per-route sound keys deleted, NOT migrated | `bundle-freeze-check` re-baselined SET leg only, `audio.js` back as a shared chunk because gh#227 gave it eleven consumers — `BASELINE_TOTAL_BYTES` deliberately unchanged, measured total inside the ±5% band | gh#224's `RECORDED_IDLE` calibration gap NOT patched — the false `ci-probes.sh` comment was corrected instead, the mechanism is ADR-0063's named flip-fact and is owner-reserved | gh#226's uneven-pill-row finding NOT fixed — a height floor would move a row currently pinned as fitting | gh#192 boxes (c)(d)(e) ticked, (b) left open on its own comment's recommendation.
next: [ ] **gh#224's leg pair calibrates only 7 of 10 routes — owner call.** The 3 in `RECORDED_IDLE` report UNMEASURED on the clean AND control leg, so a route whose paint emitter dies while its render loop keeps clearing stays green on both. This is the flip-fact ADR-0063 named for itself. Verify: the ticket records either an owner ruling or a mechanism that reds on a dead emitter
- [ ] **gh#104 — 19 of 24 shipped เซียมซี prompts are owner copy to rewrite.** Verify: each prompt either describes one person with one phone, or the ticket records why it stays
- [ ] **gh#220 box 3 asserts a set nobody enumerated** — "three absence-shaped stylesheet decisions across six routes"; two independent greps find only `cursed-number`'s. Verify: the ticket lists the other two, or records accepting one
- [ ] **gh#112 leaves 79 of 134 registry rows at `agent-authored-pending-owner-review`.** CI now prints that count every run. Verify: the count falls, or the ticket records that pending is an accepted resting state
- [ ] **gh#192 (b) needs one owner sentence.** Its own comment says do not build it: the home page has no strip to fill and two of the three strings already sit in the hero line above. Verify: the ticket says build it with changed copy, or drop it
- [ ] **gh#181's `ready-for-agent` label is stale** — its last comment says boxes 1, 2, 5, 6 and 8 remain the owner's. Verify: the label matches the ticket text
- [ ] **gh#213 unblocked and unbuilt.** Id, Thai title and หมวด were answered 2026-09-06; it is parked on SCALE — a hand-author lift with the first runtime library, against a bundle baseline that would need a re-pin. Verify: it ships as its own artifact with its own deploy ask, never folded into a batch
- [ ] **gh#226 residual, reported not gated:** `wire-column` is 43px wide at 320px, one under the tap floor, and is a div so the control-floor selector never reaches it. Same class as gh#214's dense-board cell. Verify: the ticket records a ruling or a widening
- [ ] **Read the fit probe's warnings, never its exit code.** A grown `KNOWN_OVERFLOW` row warns and still exits 0. Verify: any fit claim in a report quotes the notice line, not the exit status
- [ ] **gh#205 boxes 5 and 6 · gh#204 boxes 7 and 9 — owner's eye and phone.** Verify: each records a measurement or a waiver
- [ ] **gh#214 gh#210 gh#184 gh#219 — owner looks or settles.** Verify: each records the call
- [ ] **`claude/rh-cafbc2` — verified fully superseded this session.** Both ADRs exist under renamed files, the route was rebuilt, mascots moved; nothing unique is stranded. Owner ruled keep. Verify: a later session lands or abandons it explicitly
- [ ] **The memory index is at 160 lines against a 200-line read limit** — a hook asked for it at this save and it was deliberately deferred, because merging memories wrong loses facts permanently and a rushed pass at save time is how that happens. Verify: run `/anthropic-skills:consolidate-memory` in a session that starts with it, and the index reads under 140 lines with no fact dropped
- [ ] owner physical blockers, unchanged: gh#9 domain has no A record and no NS (blocks gh#160 gh#29 gh#12 gh#19) · gh#141 real-phone walk · owner standing gh#106 gh#109 and the fortune family gh#97-gh#103
inflight: measured during this save — `git status --short` clean before staging this entry · `gh pr list --state open` = empty · `git rev-list --count origin/main..main` = 0 at push time, and anything committed AFTER that is this docs save, unpushed by ADR-0049 — count it with that command, never from a number written here · branches: local `main` + `claude/rh-cafbc2`, remote-only `gh192-landing-rule` + `gh195-196-play-fixes` · **worktrees: 1, the main tree** · probe ports 9222/9333/9344/9592/4321/4322 checked, 0 listeners, no orphaned Chrome · environment witness for the next resume: **CI 34219465181 at `c729431`, Deploy to Azure success** · `~/.claude/CLAUDE.md` still dirty with a rewrite this session did NOT make — left untouched, never staged.
spent: queue 15 rows in at RH → 6 tickets shipped and closed, 9 still owner-blocked · 6 waves · 8 dispatches (1 fork · 5 workers · 1 REFUTE · 1 ui-driver) · every track attempt 1 · ~52 SELF calls · 1 full gate suite, 2 browser probe runs, 1 accidental suite kill · 3 new gates wired.
errors: cited a RETIRED instrument in a brief — the games half of `narrow-overflow-probe` has been empty since gh#149, and I matched a filename instead of asking whether it still measures anything; the agent caught it · ran `run-workflow-gates.sh --list`, which is not a flag, so it launched the FULL suite including browser probes exactly when wave 2 needed the machine, after briefing three agents not to do that; `kill` was ignored and the process group needed `kill -9` · accepted gh#165 as green while it shipped THREE failing tests, because both my brief and my check ran only the tests that ticket's DoD named and `src/shell/audio.ts` has 21 importers — gh#227's stub fixed it by luck, not by design · put three questions to the owner that the tracker had answered on 2026-09-06, from reading only the last comment when the answers sat one comment back · wrote a summary line assuming a JSON container was an array when it is an object keyed by the Thai run, and it threw · read an exit code through a pipe three separate times and got an empty string each time.

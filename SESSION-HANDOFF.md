# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-08-23#1

done: **3 commits `8312149` + `801a979` + `c408128`, pushed · CI runs 32623481964 / 32638324424 / 32640746076 all green, per-step conclusions read from the JOBS endpoint, `Deploy to Azure Static Web Apps` = success NOT skipped on each · tests 187->188 · open issues 12->11, 5 closed (2 inherited + 3 filed and closed same session)** · **gh#65 CLOSED** — `clearsSession` DELETED not re-polarised; button derives from `gameId !== undefined`, the bit the leave-confirm partition already reads, so two bits that had to agree became one; fork rejected the build-gate on ownership (it enumerates `src/pages/tool/` as a PROXY for "pages that must not clear" and a future non-tool non-game mount escapes it); `!== undefined` not truthiness, empty-string id would fail unsafe-invisible; deleting the prop from `Props` is load-bearing — a straggler mount now fails `astro check` ts(2322); dist byte-identical across 14 pages, diff calibrated by a one-byte mutant · **gh#66 CLOSED** — scan counts now trace to files actually read not target-list length (ADR-0019 shape); `PAIRS_HASH` = sha256 of the CSP string, content hash NOT mtime because `actions/checkout` stamps every file with checkout time · **gh#67 CLOSED** — `findMisplacedPairs` iterated `REQUIRED_PAIRS` only and nothing rejected an unknown directive name, so `worker-src 'self'` would kill AdSense subresources while all 27 pairs passed; `main()` now rejects unknown directive names exit 1, calibrated 5 ways incl. the leg proving a new SOURCE on an existing directive still passes (ceiling 2 intact, not over-widened) · **gh#68 CLOSED** — `#stage` is declared in `src/layouts/GameLayout.astro`, scanned by NO nav gate; pinned to EMPTY (no static children at all, not no-anchors); calibrated on the discriminating pair, identical `<a href="/games/">` text reds inside the stage and passes in chrome · **gh#69 CLOSED** — 4 drifted `ponytail:` markers re-anchored to symbols, numbers deleted not renumbered · **8 static-gate blind spots closed** — `csp-inline-check` missed `<button type="button"onclick="pwn()">` (tokenizer starts a new attribute after a quoted value; ADR-0005 bypass, reproduced against HEAD), `arm-gate-coverage-check` never extracted `export function`/arrow render fns so NO condition ran on them, `validate-games` printed `0 game(s) OK` exit 0 on an empty set, `crawl-check-gamenav` reported full coverage over zero pages, `leave-confirm-check` let a comma-separated selector's first compound vouch for an ungated second · **1 claimed hole REFUTED and reverted** — `type="text/javascript;charset=utf-8"` does NOT execute (headless Chrome, 3 positive controls firing on the same page); spec essence-match needs the type STRING to equal the essence, parameters disqualify · **2 over-wide fixes narrowed before landing** — the handler needle matched inside attribute VALUES so `<a href="/tools/once=1">` would have red the build, and the no-nav whole-text scan flagged a comment CITING ADR-0014
dec: ADR-0028 — an invariant is enforced at the layer that DECLARES the element, not the layer that fills it · gh#67 unknown-directive rejection is a HARD FAIL, owner ruled 2026-08-23; this does NOT reverse gh#66 whose warn is scoped to snapshot staleness only, and the three rulings are written out in `main()` so a reader separates them without opening an issue · `PAIRS_HASH` WARNS not fails, owner ruled 2026-08-23, emitted as a `::warning file=` annotation so GitHub surfaces it on the run summary; I argued against it (a warn is a quieter fail-green) and was overruled — recorded on gh#66 · the line-number-citation ban on issue bodies is ACCEPTED AS CONVENTION guarded at authorship, owner ruled 2026-08-23, ADR-0026 had held it open pending exactly that · gh#68 rejected a `dist`-side check: built HTML is a pure function of the same source line, so it adds a build dependency and guards nothing extra · gh#68 rejected scanning `GameLayout.astro` whole — that would ban the `<a href="/games/">` ADR-0014 REQUIRES in chrome, a fix that breaks what it protects · the ceiling-2 selftest pinning `worker-src 'self'` as green was RE-SCOPED not deleted, and carries a second assert proving its fixture adds no directive name so its green cannot be the gh#67 check doing the work
next:
- [ ] owner-run: gh#9 domain -> gh#29 AdSense. `dig +short watduang.com NS` EMPTY, re-verified at this save. Blocks the ONE remaining box on each of #15 #16 #17 #18 — those four tools are BUILT and shipping, do not re-scope them as unbuilt
- [ ] owner-run: gh#13 last box — the real-device script under `docs/verification/` on one real iPhone. An agent CANNOT do this; the simulator tooling reaches simulators only
- [ ] owner: gh#12 keyword planner · gh#19 month-6 organic-clicks gate
- [ ] add one selftest assertion to `no-nav-in-stage-check.mjs` that reds if a quote-bearing regex literal lands in `src/games/*.ts` — `stripComments()` walks string state and an unbalanced quote desyncs it. Measured absent today (4 regex literals, no quotes). The debt harvest judged this cheaper than a ticket. Done when the assertion exists and is calibrated both ways
inflight: measured at save — tree clean apart from this save's own writes · open PRs 0 (checked `gh pr list`) · bg tasks 0 (checked) · HEAD `c408128` level with origin/main before this save's commit
spent: queue 6->4 (5 issues closed, 3 opened and closed same session) · batches 5 · dispatches 11 · fable 4 (2 SOLVE forks + 2 REFUTE on two separate artifacts, no third round on either) · GitHub writes 3 issues + 6 comments + 5 closes · ctx ~315k at save

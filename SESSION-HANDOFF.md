# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-09-07#2
done: NO src touched, NO push, NO deploy, NO CI run; only this save commits. What git cannot show: **the resume plan I wrote was wrong twice and the second one was a house-rule breach** — the CSP lives under `public/`, not the repo root, and "tick the boxes" contradicts the tracker guide, which says DoD boxes are NEVER ticked and an issue closes on an evidence comment; that rule is why every ticket reads 0-ticked and it was found only by reading the guide before the first write · **liveness bent or killed 4 of 5 rows before any brief** — gh#206's departure is already fully disclosed in the freeze gate's own comment block naming the owner's decision date, gh#184 box 5's vacuity was already posted as its last comment, gh#160 box 5 was already recorded · **the worktree scare inverted twice** — commit-hash reachability said 2 commits main lacks, so I halted the deletion and branched to preserve them; the CONTENT check then showed the one file byte-identical (sha prefix 94fce95c) and main re-landed it under a different hash, so the inherited claim was right and my refutation was wrong: hash reachability is not content identity · **10 more stale branches the handoff never mentioned**, 9 at 0-ahead deleted, `claude/rh-cafbc2` KEPT (8 real commits, its `pick-loser` files unplaceable in main) · **gate suite n=2 reproduces exactly** — declared=42 executed=40 failed=0 not-runnable-here=2, 577s wall, the gh#122 browser step 542s = 94% of it, identical to n=1's 542s, next slowest step 7s; still one machine, still not CI · **gh#181 box 6 FAILS on `cursed-number`** and the cause is structural: the divergence registry asserts a recorded fragment is PRESENT, so a deliberate ABSENCE is inexpressible in it; the mockup sets a viewport floor on the wrapper that the shipped stylesheet deliberately omits citing gh#182, and `overrides.css` loads after it without neutralising the restored value · class measured: stylesheets have 0 registered divergences across all 9 registered routes, while route stylesheets carry 9 deliberate-choice comments over 6 routes, 3 absence-shaped · **gh#202 carries the webfont rulings in its COMMENTS** (2026-09-05 self-host as subset, 2026-09-06 the face is Sarabun), so gh#213's citation of it is SOUND — I published the opposite mid-session and corrected it; both an agent and I searched titles and bodies but not comments · Sarabun has SHIPPED (**corrected S2026-09-07#3: 4 font binaries plus an OFL licence, so 5 tracked paths under `public/fonts` but not 5 font files**; 9 stylesheet refs, re-confirmed), so gh#202's "font binary still owed" comment is stale · **gh#202 box 5 and the font drift are ONE problem** — exactly 2 routes redefine the sans token and those 2 hold every row in the overflow exemption map, so the owner-ordered both-machines re-read cannot converge · **corrected S2026-09-07#3, that sentence is false as a conjunction: both halves are true of DIFFERENT pairs.** `--font-sans` is declared by `zero-trigger` and `wire-snip-panic`; the 4 `KNOWN_OVERFLOW_X` rows sit on `wire-snip-panic` and `pinocchio-luck`. Overlap is `wire-snip-panic` alone — `zero-trigger` shadows the token but holds no row, and `pinocchio-luck` holds two rows, reads no token at all (it hardcodes `font-family`) and is the only font-driven case. Posted to gh#202 · **gh#210's body calls its legs dormant** while the probe runner's own comment records the leg count going 24 to 26 with both legs wired · **Node 22.22.1 has no ResizeObserver**, so gh#219's Node unit test cannot be its must-red; the browser probe must be · 9 comments + 3 body edits + 1 new issue (#220), every one byte-verified on read-back from GitHub; 0 boxes ticked anywhere.
dec: **nothing ticked, anywhere** — the tracker guide forbids it; gh#205's 5 pre-existing ticks left untouched but flagged in its own comment as inconsistent with that guide · **gh#160 box 2 CSP NOT written** — its wording enumerates a Cloudflare-owned set (ADR-0056) and the beacon needs a token presupposing gh#9; the ticket's "an entry can be written before a beacon exists" is permission, not a reason · **gh#181 box 7 SPLIT into two boxes** rather than adjudicated — one closes on agent artifacts, one only on the owner's look; no ruling overturned, since all three agree the delegation covers the per-route verdict and two say nothing substitutes for the look · **gh#205 row 5 and gh#213 boxes 1/2/5 reworded, old text quoted verbatim in each comment** so every edit is reversible from the record · **`cursed-number` fix NOT applied** and **gh#217 NOT closed** — owner reserved both · **labels NOT changed** — maintainer's under the triage guide · **no push** — docs ride the next src push per ADR-0049.
next: [ ] **gh#213 — owner reads the sequencing.** "Sequenced after Bomb" means that ticket closes, or means one-bomb's route lands? The route landed. Verify: the ticket says which, and on the substantive reading it becomes ready-for-agent with every other reserved question already answered
- [ ] **gh#202 — one owner question.** Close on the instrument (6 of 7 boxes resolved) and spin box 5 out as a token-adoption ticket? Verify: the ticket records the split, and box 5's new home names the 2 sans-shadowing routes
- [ ] **gh#217 — owner closes it.** Evidence comment is already posted. Verify: `gh issue close 217` run with a comment citing that scoping comment
- [ ] **5 relabels, owner, one pass.** gh#214 gh#217 gh#202 to ready-for-human; gh#210 gh#219 to ready-for-agent; gh#160 off ready-for-agent. Verify: `gh issue list` shows them changed, and gh#214's own words "measure, report, do not gate, do not fix" are cited
- [ ] **gh#219 — waive or measure box 1, then build.** Its fix is one guarded statement in the one shared strip module covering the 3 routes that import it. Verify: a browser width change reflows and the counter recomputes, calibrated red first; the Node test cannot serve as that red
- [ ] **gh#210 — the write-up, agent-ready now.** CI-versus-local diff plus one searchable summary line per leg carrying the failure count and smallest slack. Verify: a green run prints those numbers without needing the failure-only artifact upload
- [ ] **gh#181 — owner restates box 1 and answers the new box 8.** Box 1 names a threshold this ticket family disowned; box 8 is the owner's look. Verify: box 1 reads as something evaluable, and box 8 is answered or waived
- [ ] **#220 — owner scopes it.** Narrow path converts each absence into a presence; structural path lets the registry assert a fragment must NOT appear. Verify: the ticket says which, and the reason the other was declined is written down
- [ ] **gh#218 box 4 discrepancy.** It says the rule "adds no third breakpoint" while the shipped fix adds a min-height breakpoint; no comment reconciles them. Verify: the ticket records which is binding
- [ ] owner physical blockers, unchanged and blocking the most: gh#9 domain still NXDOMAIN (blocks gh#160 gh#29 gh#12 gh#19) · gh#141 real-phone walk (blocks gh#204, gh#205 rows 1/2) · gh#182 320/390 scroll (blocks gh#181 box 5) · gh#150 read (blocks gh#213) · owner standing gh#192 gh#165 gh#112 gh#109 gh#106 and the fortune family gh#97-gh#104
inflight: measured during this save — `git status --short` clean before and after · `gh pr list --state open` = none · no probe or serve listeners (`pgrep` = 0) · branches: `main` + `claude/rh-cafbc2` only, deliberately kept · worktrees: one, the main tree · unpushed on main = 2 after this save (last session's docs save plus this one), both docs-only, NOT pushed per ADR-0049.
spent: queue 11 rows in, 10 out but recomposed (4 died, 2 landed, 5 owner rows added) · batches 2 waves, 7 dispatches, 7 attempts, 0 failed, 0 REFUTE rounds (no diff existed to review)
errors: wrote a resume plan naming the CSP at the repo root when it lives under `public/`, from trusting prose over a find · wrote a "tick the boxes" row that the tracker guide forbids outright, from not reading that guide before drafting the plan · read commit-hash reachability as content identity and raised a false do-not-delete alarm on a worktree whose file was byte-identical · filed gh#181 wholly owner-blocked when its own last comment leaves six boxes as unverified agent work · carried gh#206's superseded baseline arithmetic into the plan · briefed two tickets as "not owner-reserved" when three of their comments reserve parts, caught only by the agent refuting the premise · published a phantom-citation claim about gh#213 that was wrong because I searched titles and bodies but not comments · two bad instruments inside those: a grep that matched a code comment and was read as markup, and a font grep returning 11 routes where the real answer is 2. One class throughout — a count or search true in a narrower scope than stated; the control that caught all of them was re-deriving with a command before the claim crossed a boundary, and none reached a ticket or a file.

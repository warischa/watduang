# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-09-08#3
done: `10aae27` `a40ada8` `2a8b1bd` pushed as `3e0e1b0..2a8b1bd`, then `7076354` `267674f` as `2a8b1bd..267674f`; CI 34189143744 and 34191220971 both success, 53 steps each, 1 non-success apiece = `skipped Keep the probe output when anything fails` (failure-only step, correct), Deploy to Azure success — LIVE. `cef1e7a` is docs-only and unpushed by ADR-0049. What git cannot show: **I deleted a payload the owner's own crit said must not be deleted** — reverted `ci-verification.md` after a bad first attempt but kept the pointer stub in `CLAUDE.md`, so the fast-lane prose existed in NEITHER file for two turns, byte counts LOOKED better for it, and both files stayed individually valid so no per-file gate could see it · **routing out of `CLAUDE.md` MOVED the ceiling rather than closing it** — 96B→1223B free cost `ci-verification.md` its headroom (165B, then 342B), which is the only reason a second owner ruling was needed · **`check-citations` was RED on my first two pointers** ("§ the same sentence" is not a heading) and I read its exit code through `| tail`, getting 0 while it printed two dead citations · **`shell-traps.md` already cited `CLAUDE.md` for the rule I moved into it**, so the move needed a self-reference re-aimed and an "all five"→"all six" count fixed in the same edit · **the suite banner prints `not-runnable-here=2`, not per-step NORUN lines**, so my `grep -c '^NORUN'` returned 0 and nearly read a documented pair as missing · **gh#225's test alone would have shipped a hole**: a mutant reverting the call site to a literal left `REDUCED_DT` dead in the table with `node --test` GREEN, because the frame loop sits outside the sliced block — two registry fragments, constant AND connector, are what close it · **gh#223's fix falsified three comments and one assertion message** calling the ambiguous directory the ONLY path to a non-zero exit · **gh#222 was wider than its title**: all 11 paired routes drift, so the discriminator is whether drift is RECORDED, and exactly two have nothing — `short-stick` (3 files) and `pinocchio-luck` (2) · its record could not be a registry entry because `registryProblems` demands a fragment per entry and which bytes are hand-edits is the unknown, and `porting-a-mockup-game.md` had 522B free so a doc would have rebuilt the ceiling problem in a third file · re-extraction was ALREADY refused by the file-level pre-write seam, so the live hazard is a `--force` reading absent entries as nothing-to-lose · **OPT round, tracker-only, no src commits: 10 owner decisions recorded across 6 tickets, #226 and #227 filed, both `ready-for-agent`** · **FOUR of my own premises were stale inside that round** — gh#104's ADR-0040 violation is already gone from `categories.ts` and survives only in `design/`, which does not ship; gh#213's `one-bomb` had already shipped as a play route; gh#192's (b)(c)(d)(e) were already answered 2026-09-06 with only the checkboxes stale; gh#112's body was already reworked past my two-criterion proposal into eight measurable ones. **One cause for all four: I read truncated comment excerpts as the ticket's current position, and the tree or the body disagreed every time** · **gh#182 could not be put to the owner at all** — its blocking claim ("six per-route decisions", "three numbers that do not reproduce") is enumerated nowhere on the ticket, and the body forbids treating its only "six" as a work list because those were SETUP-screen numbers and the 2026-08-31 criterion lets setup screens scroll; gh#179 is CLOSED so the body's own entry point is agent work; label deliberately NOT flipped, since an absence of evidence in a comment is not proof the decisions do not exist · **gh#165's rulings 1 and 2 contradicted each other** (site-wide mute + untouched controls = muting from `timebomb` silences the site while ten buttons still read "sound on") and needed a fifth answer · measured **ten** routes carrying a real sound `<button>` against the ticket's own "the nine", and `timebomb` has no `markup.html` at all — its directory holds `main.ts` and `bomb-canvas.ts`, so adding a control there is not a sibling-route edit · `claude/rh-cafbc2` holds **8** commits by both `git cherry` and `rev-list`, not the 5 the previous entry recorded, and they are superseded not lost (ADR-0049/0050 on main, route rebuilt at `src/play/cannon-flag/`).
dec: `ci-verification.md` splits at an ADR-0012 seam, NOT a declared budget — owner ruling; ADR-0062's precedent applies only where no seam exists and the seam test found one (zero outside hits for NORUN/DENY/REQUIRE/GITHUB_OUTPUT/not-runnable) | the expensive-step paragraph deliberately stayed, it holds the 88% the fast-lane provenance points at | gh#222 = record as a finding, no registry entry, no re-extraction; record home is the `_readme` string in `_divergences.json`, which pays no `.md` budget and sits where `--force` is typed | gh#222 closed for BOTH routes | gh#224 = derive the 2D route list from the manifest at run time, rejecting a hand-maintained list | gh#213 = the one-bomb ROUTE landing satisfies "sequenced after Bomb", not that ticket closing — `#150`'s open boxes are upstream work, not decisions, so they are not the gate | gh#220 = extend the registry with a must-be-ABSENT field, over a separate gate or a finding-only record | gh#202 = close on the instrument, box 5 spun out to `#226` | gh#165 = (a) control on `timebomb` only · muting is site-wide · default ON (keeps the 2026-08-30 tick-is-KEPT ruling) · muting silences `boom` too · and the 1-vs-2 conflict resolves as timebomb first with the other ten tracked at `#227` | gh#112 = the eight reworked criteria are owner-endorsed; the answer ratified a re-scope only an agent had made | `#181` box 6 for `short-stick` stays with `#181` | no ADR for the ci-verification split: ADR-0012's rule applied, not changed.
next: [ ] **gh#182 is blocked on a CLAIM, not a person — settle it first, it is the cheapest unblock in the queue.** Either its relabel author enumerates the six decisions and names the three non-reproducing numbers, or run the gh#179 probe against the current tree and close gh#182 on its own written instruction. Verify: the ticket carries either an enumerated list or a probe run with its output
- [ ] **gh#224 — the class-wide 2D-ink leg.** Prerequisite nobody has built: the manifest carries no field naming a route's renderer, so the field comes first. Verify: the derived list is proved COMPLETE by bucketing every manifest entry against an unfiltered total, never merely non-empty
- [ ] **gh#213, gh#220, gh#165, gh#112, gh#226, gh#227 are all `ready-for-agent` and unblocked.** Verify: each closes against its own DoD, and gh#227 stays blocked by gh#165 rather than racing it
- [ ] **Re-count before briefing gh#165 or gh#227.** The tickets say "the nine"; disk says ten routes carry a sound `<button>`. Verify: the count in any brief came from a command run that turn
- [ ] **gh#181 is ambiguous and needs one owner sentence.** Its unticked boxes read as measurement work, but a comment calls boxes 1/2/5/6 "yours". Only box 8 (the 1440x900 look) is unambiguously the owner's. Verify: the ticket says which of those boxes are decisions and which are agent measurements
- [ ] **gh#192 and gh#104 need a state check, not a decision.** gh#192's (b)-(e) were answered 2026-09-06 with the boxes never ticked; gh#104's copy violation is already out of `src/`. Verify: each is closed, or its remaining box named
- [ ] **`docs/agents/ci-verification.md` 3284B free · `workflow-gate-classification.md` new at 4046B · `CLAUDE.md` 1223B free.** Verify: the next addition to any of them checks its budget before writing
- [ ] **gh#204 boxes 7 and 9 — owner's phone.** Frame rate on a mid-range Android; box 9's residual IS that number. Verify: both record a measurement or a waiver
- [ ] **gh#205 boxes 5 and 6 — owner's eye and phone.** Verify: box 5 records the OG-card look, box 6's rows 1-2 record the walk
- [ ] **gh#214, gh#210, gh#184, gh#219 — owner looks or settles.** Verify: each records the call
- [ ] **`claude/rh-cafbc2` — owner ruled keep, not delete.** 8 superseded commits from 2026-08-29. Verify: a later session lands or abandons them explicitly, never on a count alone
- [ ] owner physical blockers, unchanged: gh#9 domain has no A record on the system resolver or 1.1.1.1 and no NS either (blocks gh#160 gh#29 gh#12 gh#19) · gh#141 real-phone walk, 15 unchecked rows · owner standing gh#192 gh#106 gh#109 and the fortune family gh#97-gh#103
inflight: measured during this save — `git status --short` clean before staging this entry · `gh pr list --state open` = `[]` · this session pushed TWICE and `origin/main` reached `267674f`: `3e0e1b0..2a8b1bd` (src) then `2a8b1bd..267674f` (docs, riding it because the batch held a src change). Anything committed AFTER that is docs-only and deliberately unpushed per ADR-0049 — count it with `git rev-list --count origin/main..main`, never from a number written here, since a save is itself such a commit · branches: local `main` + `claude/rh-cafbc2`, remote-only `gh192-landing-rule` + `gh195-196-play-fixes` · **worktrees: 1, the main tree** · probe ports 4321/4322/4592/9222/9333/9344-9348/9555 have 0 listeners, 0 Chrome or serve processes · environment witness for the next resume: CI **34191220971** at `267674f`, Deploy to Azure success · `~/.claude/CLAUDE.md` is dirty with a 37+/37- rewrite this session did NOT make — left untouched, never staged · budget sweep per this repo's recipe: silent; a separate loop over all tracked `.md` finds 10 over budget, ALL pre-existing and all in exempt classes (`.scratch`, `sessions-archive`, `docs/verification`).
spent: queue 15 rows in at RH → 6 shipped, 10 owner decisions taken, 2 tickets filed, 9 still owner-blocked · 3 popup rounds (4+4+2 questions) · 0 subagent dispatches, everything SELF · 2 full suite runs · 4 advisor consults, which caught the missing seam test, the absent-root comment rot, the gh#225 twin check, and the run-id witness · tickets: gh#222 gh#223 gh#225 gh#202 closed with evidence; gh#213 gh#220 gh#165 gh#112 relabelled `ready-for-agent`; gh#226 gh#227 filed.
errors: deleted a routed payload by reverting its destination while keeping its stub, against the crit's own word "not deleted" · read `check-citations`' exit code through a pipe and got 0 while it printed 2 dead citations · shipped two pointers citing "§ the same sentence", which is not a heading · solved `CLAUDE.md`'s ceiling by recreating it in `ci-verification.md` at 165B free · grepped `^NORUN` for a banner that says `not-runnable-here` · claimed a repo-wide budget sweep from a command that gates ONE file per call · wrote "0 unpushed" into a line whose own commit was unpushed · **put four questions to the owner on premises the tree or the ticket body had already moved past, all four from reading a truncated comment excerpt as the ticket's current position — the fix is to read the body and the last comment IN FULL before drafting a popup, never a `.[0:700]` slice.**

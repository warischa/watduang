# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-08-19#3

done: **gh#48 + gh#49 FILED and CLOSED · 6 commits · every CI run green · deploy re-based on OIDC.** Queue was "fix one gate's failure text"; what the session actually produced is a confirmed silent data-loss bug, found → fixed → verified on two engines. `checkpoint-writer-check` no longer orders the design the owner declined (gh#24): `failureMessage()` extracted as a pure getter with no argv/exit seam, absence of the declined directive asserted **with a known-bad leg** so a typo in the pattern cannot make it hold vacuously. **gh#49** — a bfcache-restored page overwrote a NEWER checkpoint (holder 2→1, results 2→1, a drawn card back in the deck, no message); fixed by a generation counter as CAS token in `session.ts` `write()`, placed as a SIBLING of the identity branch because that branch is skipped on id-less records, and committed only after `setItem` returns. Wall-clock was rejected as the token: the clobbering write carried the *newer* stamp, and round end fires two writes in the same ms. gh#48 closed as documented-limit-not-wider-matcher (widening = the ADR-0019 inversion, six games red for a rule they keep). `needs` manifest field DELETED (`types.ts` + 7 games + validator). ADR-0021 NEW. ADR-0010 12039→7630B; `site-owner-checklist` 12216→10123B, background split to a companion. tests 153→**157**. ci.yml check scripts **13, unchanged** (a `node scripts/` grep says 12 and is wrong — one is referenced without the prefix). Evidence `docs/verification/evidence/49/`. Commits `1896dac` `40707d1` `0b34f78` `a6b7c05` `f9f4d5a` `5f4ce16`.

dec: ADR-0021 · gh#48 + gh#49 close comments carry the rest · **deploy identity = OIDC, zero stored credentials** — app registration has 0 secrets / 0 certs / 0 redirect URIs; SP is Contributor scoped to the SWA resource only, and that it can read the deployment token was MEASURED as the SP, not inferred (Microsoft documents no required role) · **site auth is NOT Entra ID** (owner, 2026-08-19) — `no SWA auth` in `CLAUDE.md` stands, `staticwebapp.config.json` untouched · the three OIDC identifiers live in **secrets not variables** so the armed signal stays in one list — but `actions/secrets total_count` is now a conservative HINT, not the answer: it over-reports, and the authoritative test is all three exact names · Key Vault created then deleted on the owner's call — the only real secret is fetchable on demand, so a stored copy adds a leak site and goes stale on rotate · **`gh run list` no longer 404s here** — that claim was in `CLAUDE.md` and `ci-verification.md` and is corrected; `/actions/runs/<id>/jobs` gives per-step outcomes

next:
- [ ] **owner-run · FOCUS · arm the deploy.** Add three repo secrets `AZURE_CLIENT_ID` `AZURE_TENANT_ID` `AZURE_SUBSCRIPTION_ID` — values and click-path in `docs/site-owner-checklist.md` §2, or `bash scripts/site-owner-wizard.sh` §2. ⚠ all three present = **every push to main IS a deploy**, and so is *Re-run all jobs* on an older main run; every push pre-auth is void from that moment, re-gate each one. Done when the wizard's §2 check reports `3 of 3` / ARMED
- [ ] **`Azure/static-web-apps-deploy@v1` has still never executed.** OIDC login + token fetch ARE proven from Actions (run 32273450017, token length 119, nothing deployed); the upload step is untested and its first run is the go-live. Done when a main run shows Deploy not `skipped`
- [ ] owner-run, unchanged: gh#9 domain → gh#29 AdSense. Arming alone unblocks the live-URL box on gh#13 · gh#15 · gh#16 · gh#17 · gh#18
- [ ] Azure client/tenant/subscription UUIDs are committed in `docs/site-owner-checklist.md` §2 and `scripts/site-owner-wizard.sh` §2 so the owner can copy them. Master SH forbids UUIDs in save targets; these are owner-facing docs, not save targets, and they are identifiers not credentials (the app has no secret). Decide: keep, or redact behind an `az` lookup and accept the worse UX. Done when the owner rules

inflight: measured at save — working tree carries only this save's own files · **open PRs: checked, 0** · branches: `main` only, local and remote (`oidc-smoke` created and deleted this session, with its temporary federated credential) · worktrees: 1 · local == origin/main, 0 ahead 0 behind · `actions/secrets total_count=0` **and** `variables total_count=0`, re-checked inside the same command as EVERY push, so no push this session was a deploy · federated credentials on the app: **1** (`github-actions-main`) · GitHub writes: 2 issues filed, 2 closed, 3 comments · committed direct to `main`, no PR · open issues 11, unchanged

spent: queue 4→4 (3 closed, 3 new) · commits 6 · fable calls 7 (2 SOLVE forks + 5 REFUTE across 5 distinct artifacts) · browser/simulator probe runs 5 · ctx ~440k of 1M at save

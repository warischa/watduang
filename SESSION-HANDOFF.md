# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-09-23#3
done: `71e7e43` `f96ad1f` `ff9e6de` `1de7983` local, unpushed — gh#104 enumeration re-run, gh#141 walk script, gh#109 UNREVIEWED candidates, one /orchestrate-qwen run off the RH pick "h1 2 4 6 9 5". What git cannot show: **six tracker comments, each read back identical** — gh#9 `5789453647` (whois still `No match` 2026-09-23; checkout checklist + Azure SWA apex DNS: TXT `@` then ALIAS `@`, re-read on the Microsoft page) · gh#103 `5789453941` (box-3 blind test, pass bar set before any picks) · gh#104 `5789824906` (directive slot 23→0, OG 4/6→0/0, 39 flagged of 440, 32 prediction-slot strings left for an owner ruling; one REFUTE round: two spot-fixes, the template hand list disclosed as a ceiling) · gh#109 `5790034902` (the ruling record) · gh#141 `5790035284` · gh#101 `5790035652` · **outside git, in `~/.claude/projects/-Users-waris-c-claude-free-game/`:** `review-101/` (`cards.html` 40 cards beside portraits · `checks.md` · `build.mjs` regenerates after an edit) · `blind-103/` (`sheet.jpg` · `key.json` · `make.py --score|--selftest`) — **`key.json` must not reach git before the owner's picks, it publishes the answer** · run ledger `orchestrate-ledger-d8e619be.md` there · **outside this repo, owner go:** `~/claude/skills/orchestrate-qwen` `bin/qw-read` now prints `CUT-RISK` and exits 2 when a return is empty or does not end in terminal punctuation; on the 7 saved returns it flagged only the cut REFUTE; `SKILL.md`'s exit-code line rewritten at net 0 bytes; uncommitted there; revert: `git -C ~/claude/skills/orchestrate-qwen checkout -- bin/qw-read SKILL.md`, or `~/claude/skills/orchestrate-qwen-backup-2026-09-23.tar.gz`.
dec: "9" in the pick = gh#9 (owner, popup) | the 2026-09-16 draft-candidates ruling now covers gh#109 (owner, popup; recorded in gh#109 comment `5790034902`) | tracker writes pre-authorized for gh#104 gh#9 gh#103 at intake and gh#109 gh#141 gh#101 at the drain gate (owner, popups — ruling recorded here, not on the tickets) | REFUTE finding "TEMPLATES is a hand list" disclosed, not fixed: the instrument is a dated evidence snapshot, not a gate (orchestrator) | blind-test bar: 5 or more of 10 correct = distinguishable, P=0.050 by guessing — proposed, the owner's to accept (gh#103 comment `5789453941`).
next: [ ] gh#104 — owner rules whether any of the 32 prediction-slot strings (set 1 of `docs/verification/evidence/104-2026-09-23/README.md`) "tells the reader to gather a group, pass a phone, or read anything aloud"; none → verify boxes 3-5 and close with evidence; some → those strings become owner Thai copy. Verify: the ruling recorded on gh#104, then each box checked the way its wording says · Blocked · ask: owner ruling on comment `5789824906` (none count / list the ones that do)
next: [ ] gh#103 box 3 — score the owner's ten picks with `blind-103/make.py --score`, commit `sheet.jpg` + `key.json` + the score under `docs/verification/evidence/` (read `docs/agents/assets.md` first — a binary), post the result against the bar in comment `5789453941`. Verify: the score and its guessing probability quoted on gh#103 · Blocked · ask: owner's ten picks (a reviewer who has never seen the deck is the stronger test)
next: [ ] gh#109 — land the signed-off copy: box 2's tail in the wheel page's single-name note, with the `bundle-freeze-check` re-baseline in the same commit; box 1 in the five `design/` artboards, or delete the line under option (b). Verify: `รีเฟรชหน้านี้แล้วเลือกชื่อใหม่` found nowhere in `src/` and `ชื่ออยู่ที่เครื่องนี้ชั่วคราว` in no artboard · Blocked · ask: owner edit of `docs/copy/tool-name-entry.md` (its `⚠ UNREVIEWED` banner count reads 0 = signed off, measured 1 at this save)
next: [ ] the stale `วิธีใช้` step 1 `เลือกชื่อจากกลุ่มเดิม` on `/tool/wheel/` `/tool/draw/` `/tool/team/` — the panel has no group (ADR-0039); found 2026-09-23, recorded in gh#109 comment `5790034902`, no ticket carries it. Verify: an issue exists, or gh#109's body names it · Blocked · ask: new ticket or fold into gh#109 (a/b)
next: [ ] gh#9 post-purchase — bind `watduang.com` and `www` in the SWA portal per comment `5789453647` ⚠confirm (production config); the registrar-side records need the owner's account. Verify: both Custom domains rows read `Ready` and `https://watduang.com` serves the site · Blocked · ask: after H: gh#9 — owner does the portal steps, or an agent on an explicit go (a/b)
next: [ ] orchestrate-qwen, outside this repo — commit `bin/qw-read` + `SKILL.md` in `~/claude/skills/orchestrate-qwen`; its `SKILL.md` carries inherited debt, 14,940B against the 12KB default (`check-budgets.sh`), heaviest `## Dispatch` 6,191B → move detail into its `reference/`. Verify: `git -C ~/claude/skills/orchestrate-qwen status --short` empty and the budget check no longer WARNs · Blocked · ask: commit now (a) or leave for review (b) — asked 2026-09-23, unanswered
next: [ ] gh#103 boxes 4 and 6 — move the accepted portraits into the publish surface at the size the page loads, and state what one reader downloads; no route for this game exists under `src/pages/`, per gh#103's comment of 2026-09-23. Verify: each deck file referenced by a card on the built page, and the per-reader byte count in the closing comment · Blocked · ask: unblocks when the gh#101 page exists — nothing to decide before then
next: [ ] gh#101 — build the page from the signed-off drafts, per the ticket body's "What to build"; re-render any portrait whose card's gender, age, build, occupation, manner or mark the owner edit moved, with the command in `images/IMAGES.md`'s gh#103 section. Verify: the ticket's seven boxes, each checked the way its own wording says · Blocked · ask: owner edit — consumes H: gh#101
next: [ ] gh#160 — Cloudflare Web Analytics, chosen and not wired; its other blocker #159 is confirmed dead. Verify: per ticket, once the domain resolves · Blocked · ask: unblocks when H: gh#9 lands — nothing to decide before then
next: [ ] gh#29 — ad slots need the domain AND an owner AdSense account, site review and publisher ID; two owner acts, not one, per its own body. Verify: publisher ID recorded and ad code live · Blocked · ask: unblocks only after gh#9 AND the owner's AdSense account — do not brief it as domain-only
next: [ ] gh#19 — month-6 organic-clicks gate; needs the domain, Search Console connected to it, and six months of traffic. Verify: organic clicks recorded at month 6 · Blocked · ask: nothing to decide until the domain and the traffic window both exist
next: [ ] H: gh#101 owner edits the four drafts in the tree — `docs/copy/nuea-khu.md`, `docs/copy/nuea-khu/cards.md`, `docs/copy/nuea-khu/cards-m6-m20.md`, `docs/copy/nuea-khu/cards-f6-f20.md`; review aid `review-101/cards.html` + `checks.md` (comment `5790035652`): ten cards aged 42-54 and four non-Thai nationalities flagged, no duplicate, every card mapped to a portrait. Verify: `grep -c '⚠ UNREVIEWED'` reads 0 on all four, measured 1 each at this save
next: [ ] H: gh#9 owner registers `watduang.com` — owner-only and financial, never an agent action; re-run whois immediately before paying, privacy on, auto-renew on, and check the registrar offers ALIAS/ANAME/flattening at the apex (comment `5789453647`); it fully unblocks gh#160 only, and is one of two owner acts gh#29 needs. Verify: `dig +short watduang.com A` resolves
next: [ ] H: gh#141 walk every box on a real phone with `docs/verification/gh141-real-device-script.md` — 20 boxes incl. gh#204 boxes 7 and 9 (one measurement, the frame rate), gh#205 box 1, gh#219 box 1 (comment `5790035284`); gh#204 gh#205 gh#219 carry NO agent slice per their 2026-09-21 triage comments — do not brief them as code work. Verify: every box noted with device and OS on its own ticket
next: [ ] H: gh#12 needs an owner Google Ads account, NOT the domain — its body says `Blocked by: —`. Verify: Keyword Planner volumes recorded under `docs/research/`, never `.scratch/`
inflight: measured during this save — `gh pr list --state open` empty; one worktree, the main checkout; no background tasks (the headless Chrome this session started was stopped by pid, 0 left); `origin/main` = `7566fe9`, 13 commits ahead plus this save, all riding the next src push per ADR-0049; tree dirty only with this save's file until its commit; `~/claude/skills/orchestrate-qwen` dirty with `bin/qw-read` and `SKILL.md`.
spent: queue 5→11 · batches 2 — 11 is the open non-`H:` count, enumerated: gh#104, gh#103 box 3, gh#109, the `วิธีใช้` finding, gh#9 post-purchase, the orchestrate-qwen commit, gh#103 boxes 4 and 6, gh#101, gh#160, gh#29, gh#19. Three former `H:` lines became agent rows once drafts and instruments existed (gh#103 box 3, gh#104 item 1, gh#109).
errors: **(1) a self-check built on a wrong model of its own instrument, two instances, both caught by their own red before any claim used them** — the blind scorer's selftest asserted P(X≥0) equal to P(X=0); the first `check-citations` must-red planted a markdown link, a form that gate does not own (its header's ceiling says so) · **(2) RH listed gh#104 item 1 as a human task from the previous `next:` line without probing the two tickets it named** — gh#97 and gh#98 had closed 2026-09-12 having rebuilt the deck; prevention now in `docs/agents/ticket-actionability.md` · **(3) quantitative claims written before measurement, two instances, both caught before they shipped** — a wall-clock figure in the run ledger (replaced with the DUR-field sum) and the save report's `qw-read` proposal, whose length-per-token signal measuring refuted (healthy 0.027-0.46, the cut one 0.03).

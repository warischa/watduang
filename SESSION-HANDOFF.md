# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-09-22#2
done: `da75294` `7f95a57`, both pushed; CI run `35690082586` on `7f95a57` GREEN — 58 success and 1 skipped, the skip being `Keep the probe output when anything fails`, so the skip IS the green signal; Deploy to Azure ran, gated `push && ref==main && HAS_DEPLOY_IDENTITY`, and `fdb9367..HEAD` touches no `src/` or `public/`, so it republished identical content. What git cannot show: **the routing destination was reversed mid-session** — both blocks first went to `docs/agents/triage-labels.md`, then `docs/sessions-archive.md` was found already ruling that file a label lookup table and the wrong seam under ADR-0012, and the move had made `CLAUDE.md` point there from two different task routes, so it was backed out to a new `docs/agents/ticket-actionability.md` and `triage-labels.md` is byte-identical to its pre-session content · **`## Citations in issue bodies` was never routable** — ADR-0026 pins it to `issue-tracker.md` by name at five lines, so the previous entry's candidate #2 did not exist and heaviest-section-first would have voided an ADR · **the 318B prevention line was never committed** — `git log -S` reaches only August, so it was re-authored rather than restored and cost 556B · **`scripts/check-citations.mjs` reports "all citations resolve" but does NOT match bare paths** — must-red'd with an injected dead `.md` path and it stayed green, so this round's ten pointers were resolved by hand; `scripts/added-lineno-citation-check.mjs` DOES cover a newly added file, must-red fired naming the new doc, but its "N added line(s) scanned" counter did not move when that file entered scope, so that counter can never be used to infer coverage · **the CI runner separates freeze-tap's draws and this Mac cannot** — press 1 reads 12px while presses 4, 5 and 6 read 0px, identical across attempts 1 and 2 of `da75294`, which is what finally met gh#239's second box · **gh#182's 2026-09-15 close was refuted by that same reading** — its third bullet claimed freeze-tap reads 0px worst-screen-included, a claim the probe of the day could not make, so the ticket was reopened · **the test that went red was pinning the old classification, not the behaviour it is named for** — a hard-coded `[pinned fits]` suffix — and the first must-red was INVALID because the mutation put the row in both sets and tripped the import-level disjointness guard instead of the assertion under test; the true must-red reverts the probe alone and reds 23/1 by name · **two `gh#238` citations in `scripts/play-screen-fit-probe.test.mjs` are CORRECT and must not be "fixed"** — they trace to `d1a7402`, whose subject is gh#238 itself, so they are not the เลขใบ miscitation class.
dec: freeze-tap 320x568 accepted at 12px and moved `FITS_ROWS`→`KNOWN_OVERFLOW` (owner 2026-09-22, popup — **ruling recorded in the reason string and in gh#182 comment `5771409188`, not in any ticket body**), and that reason explicitly does NOT rest on the 2026-09-10 primary-control rule because the owner was offered that measurement first and chose to rule directly, which makes it the only 320x568 entry in that map without that basis — the asymmetry is written into the reason string itself | gh#239 closed on the two-attempt evidence (owner 2026-09-22, popup) | the two dispatch-actionability blocks cut at the ADR-0012 task seam rather than at the heaviest section, into their own file, because both `issue-tracker.md` and `triage-labels.md` serve other task moments | `docs/agents/triage-labels.md` stays a pure lookup table.
next: [ ] gh#182 — decide its second close. Run `35690082586` is green with freeze-tap 320x568 now a recorded exception, which is what boxes 1 and 6 ask for (premise: that run's own per-step tally, read 2026-09-22). Its first close was REFUTED this session, so a second one needs its own evidence and must not reuse the 2026-09-15 bullet. Verify: boxes 1 and 6 evaluated against a named green run, then closed with an evidence comment · Blocked · ask: close on run `35690082586` (a), or hold until the one-screen-per-route gap its own 2026-09-15 close names is also addressed (b)
next: [ ] gh#181 — the eight other games sitting in a phone-width column on desktop. Open and `ready-for-human`, and absent from the previous entry's queue entirely; found by `gh issue list` this session, never read. Verify: per ticket · Blocked · ask: read its body and last comment IN FULL before briefing — nothing this session read them
next: [ ] gh#103 — the remaining thirty เนื้อคู่ cards and portraits. Style is DECIDED in S2026-09-22#1, so the only thing left blocking is the owner edit. Verify: thirty cards written and thirty portraits put to the owner · Blocked · ask: owner edit — consumes H: gh#101's output and cannot start before it
next: [ ] gh#160 — Cloudflare Web Analytics, chosen and not wired; its other blocker #159 is confirmed dead. Verify: per ticket, once the domain resolves · Blocked · ask: unblocks when H: gh#9 lands — nothing to decide before then
next: [ ] gh#29 — ad slots need the domain AND an owner AdSense account, site review and publisher ID; two owner acts, not one, per its own body. Verify: publisher ID recorded and ad code live · Blocked · ask: unblocks only after gh#9 AND the owner's AdSense account — do not brief it as domain-only
next: [ ] gh#19 — month-6 organic-clicks gate; needs the domain, Search Console connected to it, and six months of traffic. Verify: organic clicks recorded at month 6 · Blocked · ask: nothing to decide until the domain and the traffic window both exist
next: [ ] record only, no decision waits on it — `docs/agents/issue-tracker.md` still states เลขใบ+1 twice (in `## ✅ Migrated 2026-08-13 — GitHub Issues is canonical` and in `### เลขใบ vs issue number`) and the `.scratch/` keep-rule twice; deduping frees about 450B against a 12288B ceiling the file now sits 2397B under. Verify: none owed
next: [ ] record only, no decision waits on it — freeze-tap's press-1 primary control has never been measured against the 2026-09-10 above-the-fold rule; taking it would upgrade the `KNOWN_OVERFLOW` reason from bare acceptance to the same basis its three sibling 320x568 rows carry. Verify: none owed unless the owner asks to re-base that ruling
next: [ ] H: gh#101 owner edits the draft in the tree — `docs/copy/nuea-khu.md` and `docs/copy/nuea-khu/cards.md`, both still carrying `UNREVIEWED`; the agent half is done. A field-value edit costs ten re-renders per the trigger recorded in S2026-09-18#8. Verify: neither file still carries the banner
next: [ ] H: gh#9 owner registers `watduang.com` — owner-only and financial, never an agent action; re-run whois immediately before paying, privacy on, auto-renew on; it fully unblocks gh#160 only, and is one of two owner acts gh#29 needs. Verify: `dig +short watduang.com A` resolves
next: [ ] H: gh#141 walk all 20 boxes on a real phone; gh#204 boxes 7 and 9 and gh#205 box 1 and gh#219 box 1 need the same device and the same pass. gh#204's two are ONE measurement — box 9's residual is box 7's frame rate. Verify: every box noted with its device
next: [ ] H: gh#104 item 1 — the ดูดวง หมวด deck rewrite, 23 prompts and 5 predictions; owner-reserved Thai copy under the 2026-09-10 ruling, overlapping gh#97 gh#98 which already own เซียมซี's deck. Verify: the deck no longer promises a group
next: [ ] H: gh#109 is owner-only end to end — boxes 1-2 are Thai copy and boxes 3-5 are already satisfied per its 2026-08-28 comment; there is NO agent slice. Verify: nothing; do not brief it as mechanical work
next: [ ] H: gh#12 needs an owner Google Ads account, NOT the domain — its body says `Blocked by: —`. Verify: Keyword Planner volumes recorded under `docs/research/`, never `.scratch/`
inflight: measured during this save — `git status --short` carried this save's own two files plus untracked `.scratch/free-game/drafts/`, which holds the two comments already posted and read back and is LEFT UNTOUCHED, not swept into the save commit; `gh pr list --state open` empty; one worktree, the main checkout; no background tasks left running. `origin/main` = `7f95a57` = HEAD, nothing unpushed, and the newest CI run on it completed success.
spent: queue 3→8 · batches 1 — 3 was this session's RH §4 row count and all three closed (route the bytes, push the batch, close gh#239); 8 is the open non-`H:` count now, which is larger because the RH §4 under-listed, showing only the focus rows instead of every open item, and because gh#181 and the reopened gh#182 were added.
errors: **two classes.** (1) **Reproduced or refuted an artifact without first establishing what produced it** — two instances: the budget sweep's `find` was hand-rolled from a `sed` window that stopped before the block's closing fence, dropping the two `-not` clauses that exempt `images/IMAGES.md` and `sessions-archive.md`, which manufactured exactly the two phantom FAILs the override warns about by name two lines above that block; and the owner was told the previous entry's section bytes were false when `2312` is precisely `check-budgets.sh`'s own section-table figure and the divergent number was my `LC_ALL=C awk`, retracted in-session. Root cause both times: treating my own partial read, or my own instrument, as the reference rather than the source that wrote the artifact. (2) **A must-red whose mutation changed two things at once** — restoring the row to `FITS_ROWS` while leaving it in `KNOWN_OVERFLOW` tripped the import-level disjointness guard, so the red proved that guard rather than the assertion under test; redone as a single-variable revert that reds the named assertion 23/1. Separately, the RH brief's §4 listed only the three focus rows instead of every open non-`H:` item, which is what made `spent: queue` start from an understated base.

# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-08-18#2

done: **1 commit `b6da4ab`, pushed, 24 files. CI verdict UNVERIFIED** (`gh run list` 404s → `docs/agents/ci-verification.md`). **gh#42 CLOSED** by trailer, verified via `gh issue view`. NEW `armAllButtons(stage, except)` in `_arm-gate.ts` — gates every `<button>` in the stage subtree; walks `node.children` recursively, NOT `querySelectorAll`, which is why it works in both the FakeElement harness and a real DOM. Applied to daily-fortune · love-match · pick-loser · siamsi → ADR-0017 · **2 controls deliberately UNGATED** — daily-fortune's roster chips and pick-loser's `#pl-pick`, each one hand with no handoff → ADR-0016 § "Known premise exceptions" · **ADR-0016 amended**: its "phone in transit between two people" universal was FALSE on the first-draw path (the starter may be `players[0]`), qualified in both places it appeared; siamsi prediction SCORED — premise **HOLDS**, ADR-0014:34's ส่งต่อ→จั่วดวง overlap is *spatial* not temporal (`passToNext` advances the holder) · tests 149→**153**, all four rewritten onto short-stick's disabled-respecting harness + `t.mock.timers` · NEW `scripts/adslot-wheel-delay-probe.mjs`, `arm-gate-probe.mjs` extended · captures `evidence/{42,adslot/03-04}`; `evidence/39` backfilled with README's 5 provenance keys

dec: ADR-0017 — gate EVERY button, never "the consequential one": rules 1 and 2 enumerate **different sets**. "Games whose harm is recoverable" is owned by the evolving codebase and rots (that ledger went stale twice inside gh#37's own thread); "how long after a swap can a ghost arrive" is owned by physiology. Do not restate, cite · **owner: do NOT gate `#pl-pick`** despite unrecoverable harm (unseeded `Math.random` → a redraw names a different person) — gating a control whose premise is false is the trade ADR-0016 refuses · **owner: gh#38 RULED — add `@astrojs/check` as a devDependency and make `astro check` a real gate.** NOT implemented · **wheel.astro ad-slot measured, NOT a risk**: 1202.8ms (= `SPIN_MS`), shift is −24px **away** from the ad because `render()` appends "(ออกแล้ว)" and the list GROWS, 0/10 collisions → no issue filed, and `evidence/adslot/README.md`'s "shrink" wording corrected

next:
- [ ] **gh#38 — implement the ruling (focus).** Add `@astrojs/check` as a devDependency, wire `astro check` into `ci.yml`. ⚠ **Calibrate the gate RED before trusting it** — without the devDependency a bare `astro check` step exits 0 having checked nothing, which is the exact silent no-op this issue is about; a gate that cannot fail is the same bug in a new costume. Done when the step fails on a deliberately broken `.astro` ref and passes when it is fixed
- [ ] **`_arm-gate.ts` drift** — short-stick/timebomb still pass hand-picked arrays while the 4 new games use `armAllButtons`. The module header says it is shared "so the two cannot drift apart", so the drift is against its own stated purpose (ADR-0017 § Consequences). Done when both retrofit and their real-touch probes are re-run green (~50 min of probe time)
- [ ] **post the gh#38 ruling as a comment on the issue** — ⚠ NOT covered by last session's pre-auth (which covered push · gh#37 comment · closing gh#42 · filing a wheel issue). Needs an explicit go. Done when posted or explicitly dropped
- [ ] **owner-run, unchanged:** wizard §2 Azure token · gh#9 domain · gh#29 AdSense pub-ID (gh#29 closes gh#15–18, then epic gh#14). ⚠ the moment `gh secret list` is non-empty every push to main IS a deploy (`ci.yml:232` gates on `HAS_DEPLOY_TOKEN`) — every push pre-auth void, re-gate it. Done when `gh secret list` non-empty

inflight: measured at save — working tree carries only this save's own files · `gh secret list` **empty**, re-checked immediately before the push, so `b6da4ab` was not a deploy · **open PRs: checked, none** · branches: `main` only · worktrees: 2 created and both removed, `git worktree list` clean · GitHub writes this session: 1 comment (gh#37, `issuecomment-5324224429`, body read back untruncated) + gh#42 closed by trailer, not by hand · committed direct to main, no PR

spent: queue 5→4 · closed 1, filed 0, discovered 2 · batches 12 · ctx ~26% of 1M at save · ended early: no

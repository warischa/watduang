# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-08-29#1
done: **cannon-flag (ยิงธง) ported, wired, and committed as `c1d451c` — the seventh game.** `src/games/cannon-flag.ts` (14 unit tests, pure PRNG, ballistic 2D physics, raycast collision against terrain/water/obstacles, 18-unit direct-hit zone, hundredth-precision standings, sudden-death generator, Web Audio synth, canvas renderer gated on prefers-reduced-motion), `src/styles/games/cannon-flag.css` (scoped `cf-` rules, 320px-safe, 44-56px tap targets), `src/games/cannon-flag.test.mjs`, `public/og/cannon-flag.png`. Registered in `src/games/manifest.ts`, style imported in the game page. Both pinned-count gates re-baselined WITH attribution: `bundle-freeze-check` 20->23 chunks and 101094->153460 bytes (the extra two chunks are power-meter.js and audio.js, which entered the build when the previous commit landed power-meter's files and import.meta.glob found them — that is what reddened CI run 33176416234, and this commit closes it), `control-floor` CONTROL_COUNT 10->11. **The three gates were re-run independently this session before committing, not taken from the previous entry's claim: `npm run ci` exit 0 / 343 tests, `npx astro check` exit 0, `npm run ci-probes` exit 0 / 20 of 20 legs including 10 positive controls.** Also restored freeze-tap's full control-floor attribution block, which the same diff had compressed to one line.
dec: **Physics uses 8 sub-stepped raycasts per frame so a shell cannot tunnel through thin ridges or narrow obstacles.** Direct hit within 18 units scores 00.00. Sudden death regenerates the environment and isolates the tied worst scorers. Reduce gates the trauma shake and the particle emitters, never the simulation, so a reduced-motion player scores identically. **Process decision forced by this session's finding: an `inflight:` line is measured at the moment of writing or it is not written.** The previous version of this entry named a branch that has never existed (`feat/cannon-flag-port`), claimed a clean tree while nine files were dirty, and claimed 0 commits ahead while three sat unpushed — every perishable in it was false, and the entry was itself never committed. Re-measure, never recall.
next:
- [ ] agent: power-meter has 3 real defects and 7 mutants that kill nothing — fix, then wire. Worst defect: the widened perfect window overrides the score after the fact, so the bar paints 9.59 while 10.00 is recorded. Correct fix flattens the meter near its peak so paint and score agree by construction. Then the gauge, the detached spark canvas, and the per-render cleanup leak. Note the coupling: flattening the climb re-derives LOCK_FLOOR_HUNDREDTHS, and NATURAL_PERFECT_WINDOW_MS stops meaning what its test asserts. Verify: 32+ tests green, the named mutants all go red, and all three of `npm run ci` · `npx astro check` · `npm run ci-probes`
- [ ] agent: build the two once-only investments BEFORE the next port — a corrected fake DOM in a template test file (its innerHTML must expose children, its matchMedia must really register listeners, its clientWidth must be non-zero), and a script that extracts Thai strings and named constants out of a mockup HTML mechanically. Four of power-meter's seven surviving mutants were harness holes, identical in every game — cannon-flag's 14 tests inherit the same holes. Verify: replant those four mutants and watch each go red
- [ ] owner: merge `feat/freeze-tap-port` to main — that merge is what publishes มือลั่น AND ยิงธง to the live site, and nobody has played either in a browser yet
- [ ] owner: approve the Thai copy for screens the mockups never had — 9 strings for freeze-tap, 13 for power-meter. **The drafts are GONE.** They were left in a session scratchpad; three independent checks this session found every sibling scratchpad holds 0 files. They must be re-drafted before this can be approved
- [ ] owner: power-meter ships three infinite CSS animations its own graphic direction forbade. Rule which section governs: effects-exist versus effects-look
- [ ] owner: the pending FOCUS-BLOCKER queue from the archive is still untouched — Thai copy tickets, the font question, and the ad-slot chain
- [ ] agent: `ready-for-agent` backlog picked for this session — gh#132 (a stale tap on a two-tab tool page wipes the other tab's names; the tool list never inherited roster's re-read-inside-lock), gh#111 (GameNav still promises a same-วง handoff and lists ดูดวง pages as เกม), gh#89 (one rule for where the 2-10 party-size claim may appear)
inflight: branch `feat/freeze-tap-port`, carrying the cannon-flag port on top of the freeze-tap port · `origin/main` has neither, and deploy is gated to main, so NOTHING on this branch is live · 0 open PRs (checked) · 0 background tasks running (checked) · re-derive position with `git status --short` and `git rev-list --count origin/main..HEAD` rather than trusting this line
spent: queue 0->8 · this session is a RESCUE of an uncommitted save, not a fresh build · errors found in the inherited entry: a fabricated `inflight:` line, six `next:` items deleted without being archived, and a red CI run reported as green by citing the previous commit's run id · errors carried forward from the previous session and still true: invented a localStorage rule and briefed it twice · briefed against named lines instead of the class, twice · 3 probes returned fake zeros or read a pipe's exit code · told the owner a 200KB bundle projection was a per-visitor data cost when it is a CDN aggregate

# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-09-07#1
done: 2 pushes, both deployed and CI-green. `c2eca46..7dbaa32` (4 commits: `471e724` gh#217 disclosure · `f63b796` gate-step timing · `45ebd6a` gh#184 seed+3 probe disclosures · `7dbaa32` CLAUDE.md, CI 34046777786 success 12m9s, deploy step confirmed) · `7dbaa32..5175605` (gh#184 row-2 evidence note, CI 34047430934 success 11m49s). What git cannot show: **the liveness pass killed 9 of 11 inherited rows, including one the handoff ordered** — "close gh#181" is DEAD, its box 7 sits under a THREE-WAY ruling conflict (2026-09-04 and 2026-09-05 each say nothing substitutes for the owner's viewing step; 2026-09-06 delegates the per-route verdict and records the objection was shown and overridden), so whether the delegation discharges box 7 is an interpretation and was left to the owner · **the font class is 3 groups over 13 routes, not the 2 causes the last entry claimed** — `zero-trigger` ALSO redefines `--font-sans` in its own top-level `:root` (last entry named only `wire-snip-panic`), `pinocchio-luck` hardcodes and reads no token, and `cursed-number`+`how-close-is-near`+`power-meter` define a route-local `--font-family` (different token name) listing Sarabun BEHIND `-apple-system`; 3+3+7=13, nothing left over; whether any of it PAINTS is a runtime question a text read cannot answer · **gh#219's cause is a deliberate documented shortcut whose own `ponytail:` comment names this ticket as its trigger**, and box 4 answers by construction — 3 routes ship the strip, all import the one shared module · **gh#160 box 1 is unreachable** (`dig watduang.com` → NXDOMAIN, gh#9 is `ready-for-human`) and box 5 was a live false claim in CLAUDE.md, now fixed · **gh#205 row 5 was half over-called last entry** — ad slot/JSON-LD/prose ARE void per the landing route's `!playRoute` filter, but the OG card is TRACKED and the หมวด page links `playRoute`, so 2 sub-claims done / 3 void / 0 outstanding · gate timing shipped and says the complaint was aimed wrong: leg2 ~571s of which ~542s is ONE step (gh#122 browser probe), `npm run ci` ~26s, **n=1 this machine, not CI** · 7 tracker comments posted with byte-match read-backs, gh#205 rows 3/4/7/8/9 ticked (5 of 9, verified back FROM GitHub, body length unchanged) · local branch `claude/adoring-proskuriakova-033165` deleted (was e0291e9, its only file byte-identical in main, no remote ref).
dec: **gh#181 NOT closed** — three rulings conflict on box 7 and six other boxes are unverified; posted the conflict, left it open · **2 artifacts not 1** — artifact 1's REFUTE budget was already spent (both rounds, last session), so it could not share a push boundary with unreviewed code · **`canvas-frames-probe.mjs` deliberately NOT wired** — its frame floor counts frames in a fixed window, a number the browser scheduler and host machine own, so wiring it is ADR-0056's failure; row 6c asked for calibration evidence, not a CI leg, and the hand-run delivered it (must-red fired on `BD_STUB_PAINT`); if ever wired, gate the ink floors and report-not-gate the rate · **2 pushes in one batch, a knowing departure from deploy-once-per-batch** — the gh#184 comment cites a repo path that would otherwise be absent on the remote; cost was a second 12-min CI run · **no gh#184 box ticked** though 1/2/4/6 are measured, because the inherited handoff records rows there as owner-side and its wording does not disambiguate box numbers from its own enumeration · **evidence PNGs dropped, not force-added** — `.gitignore` excludes `docs/verification/evidence/**/*.png` with exactly one negated carve-out for gh#50 carrying its reason inline; that is a decision, so the note ships the numbers plus a reproduce command instead · **gh#160 label left alone** — `ready-for-agent` is wrong while gh#9 is open but relabelling is the maintainer's under `docs/agents/triage-labels.md`.
next: [ ] **gh#181 — owner rules whether the 2026-09-06 delegation discharges box 7.** Verify: the ticket says yes or no, and if yes an agent then ticks boxes 1-6 against evidence rather than against the verdict table's existence
- [ ] **gh#205 row 5 rewrite — owner.** 2 sub-claims done, 3 void, 0 outstanding; the row as written cannot be satisfied or closed. Verify: the row's text matches that split
- [ ] **gh#206 wording check — the freeze file discloses a DEPARTURE this session did not surface to the ticket.** `bundle-freeze-check`'s attribution block says the re-pin departs from an owner condition ("in its own change"); the last entry read gh#206 as "one adjudication, not one commit". Row 7 was ticked on attribution, which is done, but the two readings disagree. Verify: gh#206's own wording settles which, recorded there
- [ ] **Confirm the suite timing with a second run before anyone cuts scope on it.** The CLAUDE.md numbers are n=1 and say so. Verify: a second full `bash scripts/run-workflow-gates.sh` puts the same single step at the top
- [ ] **gh#160 label + box 2** — owner relabels (`ready-for-agent` is wrong while gh#9 is open); the CSP allowlist entry can be written before a beacon exists, only its live proof cannot. Verify: the label changed and the ticket says which boxes remain reachable
- [ ] gh#202 gh#210 gh#213 gh#214 gh#217 gh#219 — all `needs-triage`, all owner-to-scope; every one now carries a posted evidence comment. gh#214's own last word is "measure, report, do not gate, do not fix". Verify: each ticket says what it wants built
- [ ] gh#184 boxes 1/3/5 — box 1 is measured (`bad=0` over 9 combos) and unticked pending the owner-side question; box 3's REACH half routes to gh#210; **box 5 should NOT be ticked — the strip contributes zero controls, so the 44px floor there is satisfied by an empty set**. Verify: the ticket records box 5 as vacuous, not met
- [ ] gh#205 rows 1/2 and gh#204 — need a real phone in a real hand, which is gh#141's standing queue. gh#204's own last comment says "leave this ticket open". Verify: the gh#141 walk happens
- [ ] owner standing: gh#192 (b) · gh#218 shell pill · gh#182 gh#165 gh#150 gh#112 gh#109 gh#106 · fortune family gh#97-gh#104 · gh#141 · gh#9 domain NXDOMAIN blocking gh#29 gh#12 gh#19
inflight: measured during this save — `git status --short` clean · `gh pr list --state open` = none · unpushed on main = none (both pushes landed, both CI-green) · no probe/serve listeners (`pgrep` = 0) · worktree `hopeful-fermi-74ec5e` untouched, clean, detached at e0291e9 · this save's docs commit stacks locally per ADR-0049, NOT pushed.
spent: queue 12→9 · batches 4 waves, 2 dispatches, 2 agent attempts (1 failed), 0 REFUTE rounds
errors: briefed a subagent with a per-issue return format and it returned its own verification checklist instead of the data while claiming PASS — judged FAIL on contract, unresumable (`SendMessage` absent this session), absorbed SELF · ran a probe file directly instead of through `scripts/driver.mjs`, a silent no-op that reads exactly like a clean pass (empty output, exit 0, zero artifacts) — caught by checking the artifact count, not the exit code · asserted a must-red on the strip probe's control leg exit code when the inversion lives in the verdict script and the real observable is `bad` in its JSON · staged 2 PNGs into an evidence dir without consulting `.gitignore`, and had already committed a note describing them as committed — amended before pushing · asserted `scripts/` as the canvas probe's path when it lives under its route directory, unlabelled · a `grep -ciE` over a 5606-line log returned 0 on a file provably containing `# fail 0`, and was discarded rather than trusted.


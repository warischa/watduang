# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-09-08#2
done: `10aae27` `a40ada8` `2a8b1bd` pushed as `3e0e1b0..2a8b1bd`, CI 34189143744 at `2a8b1bd` success, 53 steps, 1 non-success = `skipped Keep the probe output when anything fails` (failure-only step, correct), Deploy to Azure success — LIVE; `7076354` committed after, rides the next push. What git cannot show: **I deleted a payload the owner's own crit said must not be deleted** — reverted `ci-verification.md` after a bad first attempt but kept the pointer stub in `CLAUDE.md`, so the fast-lane prose existed in NEITHER file for two turns and the stub aimed at a heading that did not exist; only re-reading the crit caught it · **routing out of `CLAUDE.md` MOVED the ceiling problem rather than closing it** — 96B→1223B free cost `ci-verification.md` its headroom (165B, then 342B after trimming my own provenance prose), which is how the second owner ruling this session came to exist · **`check-citations` was RED on my first two pointers** — "§ the same sentence" is not a heading; worse, I read its exit code through `| tail` and got 0 while the script was printing two dead citations, the exact trap `shell-traps.md` names · **`shell-traps.md` already cited `CLAUDE.md` for the rule I moved into it**, so the move needed a self-reference fixed and an "all five"→"all six" count corrected in the same edit, neither of which the seam test predicted · **the suite banner has no per-step NORUN lines** — it prints `not-runnable-here=2`, so my `grep -c '^NORUN'` returned 0 and I nearly read a documented pair of steps as missing · **gh#225's test alone would have shipped a hole**: a mutant reverting the call site to a literal left `REDUCED_DT` dead in the table with `node --test` GREEN, because the frame loop is outside the block the test slices — two registry fragments, constant AND connector, are what close it · **gh#223's fix falsified three comments and one assertion message** that called the ambiguous directory the ONLY path to a non-zero exit · **gh#222 is wider than its title**: all 11 paired routes drift, so the discriminator is whether drift is RECORDED, and exactly two have nothing — `short-stick` (3 files) and `pinocchio-luck` (2), the ticket named one · its record could not be a registry entry at all because `registryProblems` demands a fragment per entry and which bytes are hand-edits is the unknown; `porting-a-mockup-game.md` had 522B free, so a doc would have rebuilt the same ceiling problem in a third file · re-extraction was ALREADY refused by the file-level pre-write seam, so nothing needed building and the live hazard is a `--force` reading absent entries as nothing-to-lose · `claude/rh-cafbc2` holds **8** commits by both `git cherry` and `rev-list`, not the 5 the last entry recorded, and they are superseded not lost (ADR-0049/0050 are on main, the route was rebuilt at `src/play/cannon-flag/` instead of the branch's `src/pages/play/` path).
dec: `ci-verification.md` splits at an ADR-0012 seam, NOT a declared budget — owner ruling; ADR-0062's precedent applies only where no seam exists and the seam test found one (zero outside hits for NORUN/DENY/REQUIRE/GITHUB_OUTPUT/not-runnable) | the expensive-step paragraph deliberately stayed, it holds the 88% the fast-lane provenance line points at | gh#222 = record as a finding, no registry entry, no re-extraction — owner ruling; record home is the `_readme` string in `_divergences.json`, which pays no `.md` budget and sits where `--force` is typed | gh#222 closes for BOTH routes, not just the titled one | `#181` box 6 for `short-stick` stays with `#181`, no new ticket | gh#224 = derive the 2D route list from the manifest at run time — owner ruling, rejecting a hand-maintained list | no ADR for the split: ADR-0012's rule applied, not changed.
next: [ ] **gh#224 — the class-wide 2D-ink leg, `ready-for-agent` and scoped.** Prerequisite nobody has built: the manifest has no field saying which renderer a route uses, so the field comes first, then the leg. Verify: the derived list is proved COMPLETE by bucketing every manifest entry and reconciling against the unfiltered total, not merely non-empty
- [ ] **`docs/agents/ci-verification.md` is at 3284B free and `docs/agents/workflow-gate-classification.md` is new at 4046B.** Verify: the next addition to either checks its budget before writing
- [ ] **`CLAUDE.md` is at 1223B free.** Verify: `check-budgets.sh CLAUDE.md` before any edit; two `⚠` blocks are now pointers, so route rather than inline
- [ ] **gh#204 boxes 7 and 9 — owner's phone.** Frame rate on a mid-range Android; box 9's residual IS that number. Verify: both boxes record a measurement or a waiver
- [ ] **gh#205 boxes 5 and 6 — owner's eye and phone.** Verify: box 5 records the OG-card look, box 6's rows 1-2 record the walk
- [ ] **gh#181 boxes 1, 2, 6, 8 — owner.** Box 8 has eight fresh frames waiting. Verify: each box reads as evaluable, settled, or waived
- [ ] **gh#202, gh#213, gh#210, gh#219, gh#184, gh#220 — owner looks or settles.** Verify: each ticket records the call
- [ ] **`claude/rh-cafbc2` — owner ruled keep, not delete.** 8 superseded commits from 2026-08-29. Verify: a later session either lands or abandons them explicitly, never on a count alone
- [ ] owner physical blockers, unchanged: gh#9 domain has no A record on the system resolver or 1.1.1.1 and no NS either (blocks gh#160 gh#29 gh#12 gh#19) · gh#141 real-phone walk, 15 unchecked rows · owner standing gh#192 gh#165 gh#112 gh#109 gh#106 and the fortune family gh#97-gh#104
inflight: measured during this save — `git status --short` clean before staging this entry · `gh pr list --state open` = `[]` · local `main` = `origin/main` by `git rev-parse` ref equality after the push, then 1 docs commit `7076354` ahead awaiting the next src push per ADR-0049 · branches: local `main` + `claude/rh-cafbc2`, remote-only `gh192-landing-rule` + `gh195-196-play-fixes` · **worktrees: 1, the main tree** · probe ports 4321/4322/4592/9222/9333/9344-9348/9555 have 0 listeners, 0 Chrome or serve processes · environment witness for the next resume: CI **34189143744** at `2a8b1bd`, Deploy to Azure success · budget sweep per this repo's `save-session.md` recipe: silent, every routed doc inside its ceiling; a separate loop over all 147 tracked `.md` finds 10 over budget, ALL pre-existing and all in exempt classes (`.scratch`, `sessions-archive`, `docs/verification`).
spent: queue 15 rows in — 4 done and shipped, 2 owner-decided-then-done, 9 owner-blocked and untouched · 0 subagent dispatches, everything taken SELF · 2 full suite runs · 3 advisor consults, which caught the missing seam test, the absent-root comment rot, and the #225 twin check · tickets: gh#222 gh#223 gh#225 closed with evidence, gh#224 scoped and relabelled `ready-for-agent`.
errors: deleted a routed payload by reverting its destination while keeping its stub, against the crit's own words · read `check-citations`' exit code through a pipe and got 0 while it printed 2 dead citations · shipped two pointers citing "§ the same sentence", which is not a heading · solved `CLAUDE.md`'s ceiling by recreating it in `ci-verification.md` at 165B free · grepped `^NORUN` for a banner that says `not-runnable-here` and nearly reported 2 documented steps as missing · claimed a repo-wide budget sweep from a command that gates ONE file per call and had reported exactly one PASS.

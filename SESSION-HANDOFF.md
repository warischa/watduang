# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-09-09#2
done: `4047181` pushed as `c729431..4047181`; CI 34316952537 success at that sha, 57 steps, 56 success + 1 skipped (`Keep the probe output when anything fails` — failure-only, correct), Deploy to Azure Static Web Apps success, LIVE. What git cannot show: **the RH queue was a DECISION queue, not a work queue — 13 of 14 tickets carry `ready-for-human`, only gh#213 is agent work**, so the run's output is one artifact plus four owner rulings, not a merged backlog · **the OG breach was ONE generator bug plus staleness, not three cards**: `make-og.mjs` stamped the count line onto EVERY card from `game.players`, and all three fortune games have declared `players: [1, 1]` since `6a9a956` (2026-08-26) while every affected card predates it — so a naive regen ships `เล่นฟรี 1-1 คน`, which gh#104's own §5 had already predicted and the handoff's "regenerate" framing had not · **partition key is `category === 'party'`, never the range being 1-1**: `เล่นฟรี` is เกม language per ADR-0040, so a fortune card stays wrong with only its numbers corrected · regenerating all 16 producible cards changed exactly 3 and left 13 byte-identical, which is the control proving party cards keep their line · **gh#104's last comment reserved this work** under its "What is the owner's" heading and the handoff did not say so — the fork caught it, and the owner then approved from a rendered preview rather than a description, so no Thai was authored (the site card reuses the line already shipping on the landing page per gh#192 (a)(i)) · **REFUTE found the new gate's exemption returned before every check**, so the day gh#101 registers love-match the gate would green while that card still ships `เล่นฟรี 2-10 คน` — and **the selftest leg written to prove the exemption safe had encoded the bug** (its exempt fixture carried a source and was asserted to pass) · `claude/rh-cafbc2` deleted (tip `18b9e76`): both "still-valuable" files refuted on re-derivation — the mascot baseline lives at `src/play/_mascots.ts` with 20 entries on each side (only a `color` field differs), and the assess doc pins a URL main deleted · my own count was wrong once here, 21-vs-22 from a grep that counted the type-declaration line · `SendMessage` unavailable again, so every REFUTE spot-fix was mine.
dec: OG cards ship as previewed, owner-approved from the render | site card rewritten and `/c/fortune/` keeps inheriting it — no category card, so that row dissolves | gh#213 stays its own run and this batch never touched `bundle-freeze-check.mjs` or the frozen baseline | `claude/rh-cafbc2` deleted, nothing stranded | an exemption is bounded by its own premise — a source answering to an exempt id is now `exemption-stale`, not a skip | the card gate reads text and sha256 only and never renders, because CI has no librsvg and no Thai font | the gated set is enumerated from disk, so the lock can only add obligations, never narrow coverage.
next: [ ] gh#104 boxes 1, 3, 4 and 5 — owner copy; the 50-row work list is on the ticket and the deck rewrite overlaps gh#97 and gh#98 which already own that deck. Verify: each flagged string describes one person with one phone, or the ticket records why it stays
next: [ ] gh#104's one ruling — is a pass-phone panel present in the DOM but `hidden` reader-visible for box 1? Verify: the ticket records yes (gate it) or no (appendix note)
next: [ ] gh#213 — its own `/orchestrate` run, never folded into a batch; ADR-0060 vendoring event, baseline moves once. Verify: ships as its own artifact with its own deploy ask and a re-pinned baseline
next: [ ] gh#220 box 3 · #228 · #229 — owner calls, each question already stated on its own ticket, do not restate. Verify: each records a ruling or a mechanism
next: [ ] gh#192 (b), gh#182, gh#205 boxes 5-6, gh#204 boxes 7 and 9, gh#214, gh#210, gh#184, gh#219 — owner's eye, phone, or sentence. Verify: each records the call
next: [ ] the love-match card still ships a player-count claim and cannot be regenerated; the new gate exempts it by name and will FAIL the day gh#101 registers the game. Verify: gh#101 records whether that card is regenerated or deleted
next: [ ] remote-only branches `gh192-landing-rule` and `gh195-196-play-fixes` — their PRs are merged, the branches were left behind. Verify: deleted, or a reason recorded
next: [ ] owner physical blockers, unchanged: gh#9 domain has no A record and no NS (blocks gh#160 gh#29 gh#12 gh#19) · gh#141 real-phone walk · owner standing gh#106 gh#109 and the fortune family gh#97-gh#103
inflight: measured during this save — `git status --short` shows only this save's two docs · `gh pr list --state open` empty · `git rev-list --count origin/main..main` = 0, the src push already went out so no docs commit is riding · branches: local `main` only, remote-only `gh192-landing-rule` + `gh195-196-play-fixes` · worktrees: 1, the main tree · no background task running · environment witness: **CI 34316952537 at `4047181`, Deploy to Azure Static Web Apps success** · NOT verified: the deployed pixels were never fetched — gh#9 leaves no owner-facing URL to look at · `~/.claude` is dirty with 55 files (40 skills, 9 agents, 2 commands, settings, CLAUDE.md) from a rewrite this session did NOT make — last save recorded 5, it has grown; left untouched, never staged, none of it mine.
spent: queue 13 rows in at RH → 1 dead on arrival (gh#181 box 6, already recorded on its ticket), 1 artifact shipped, 1 branch resolved, 8 rows are owner decisions carried forward · 5 waves · 6 dispatches (2 bulk-reader — ticket liveness and the branch diff, 1 Explore, 1 adversary SOLVE, 1 deep-worker, 1 adversary REFUTE), all attempt 1, 0 escalations, 0 re-forks · REFUTE 1 of 2 rounds · 1 build, 1 full suite, 1 CI run.
errors: **ran a `for` loop over an unquoted variable under zsh**, which does not word-split, so the whole id list went to the generator as one bogus id and every render "failed" — read as a code failure for a moment; `verify-in-bash-not-zsh` already names the class · **quoted a mascot count of 21-vs-22 from a grep that counted the type-declaration line**, corrected to 20-vs-20 by a shape-matched grep — the counting method was the claim that needed checking · **grepped the suite log for the script name when the log prints the human step label**, getting a null that read exactly like "the new gate never ran"; caught by reading all 55 lines instead of trusting the null · **briefed the OG work from the handoff's summary rather than gh#104's last comment**, which had already reserved it to the owner — the fork caught it before any worker was dispatched.

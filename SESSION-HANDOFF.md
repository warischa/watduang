# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-09-09#3
done: `93330a5` `132837b` `b0cf6e5` shipped LIVE via CI 34367311273 at `41b47c3`, Deploy to Azure success; `6756330` (croc-bite) built, two REFUTE rounds, local fleet green, then REVERTED by `41b47c3`. What git cannot show: **the route is NOT broken** — measured under CI's literal flags (`--disable-gpu`, no swiftshader, both `getContext` null in-page) the ADR-0051 no-3D fallback works and driving start moved phase SETUP→PLAYER_TURN 3 of 3 runs, both probes green · **the blocker is harness latency inside the gated number**: CI's `play-exit` burst arrived 704ms late against the 80ms it was driven at on a 2-vCPU runner, hit an exit control that had legitimately armed by then, navigated to `/`, and destroyed the JS context holding the probe's own signal variables, which it reports as "screen unchanged" — twice, deterministically, so it will not self-resolve · **gh#213's body claim "Zero `<input>` elements" is FALSE** (the mockup builds fields with `createElement`), so it is a roster-bridge route and **no upstream mockup edit was needed** · REFUTE found that opening croc-bite once **permanently trimmed an 8-person group to 6** — `saveOnSetupComplete` wrote back only seated names, against `saveGroup`'s own doc promise — fixed in the shared layer across 13 callers with a REQUIRED ceiling param so a forgetful caller fails loudly, and that fix SHIPPED · `cursed-number` already declares `[2, 20]`, so croc-bite was never the first party game off 2-10 · **three gates were green over live violations**: `js-motion-guard-check` while the crocodile idle animation read neither motion flag, `bundle-freeze-check` rc 0 twice while its own pin no longer described the tree (the ±5% band is ~105KB wide and hid the drift), and the fleet caught 11 controls under the 44px tap floor that 15 source-scanning gates and both REFUTE rounds missed · **local fleet green 3× predicted CI wrongly**: every local browser probe used `--use-angle=swiftshader`, which GRANTS webgl2, so the no-WebGL path was never exercised in a real browser until the post-mortem.
dec: croc-bite reverted, not fixed — re-landing needs gh#122's probe to tolerate CI dispatch latency, or a route that does not depend on the arm window closing that fast | the shared write-back ceiling is the seats a page can SHOW, never the manifest range — `freeze-tap` and `cannon-flag` pass 20 against a declared 10 | ADR-0065 proposed, awaiting the owner's accept or reject | seat range `[2, 6]` and Thai name จระเข้งับ both per owner ruling, the shorter variant having been declined on search | freeze pin restored to 482127 by the revert, and CSS never moves it because the gate follows only `.js` chunks.
next: [ ] gh#122 — the play-exit burst cannot be dispatched inside the arm window on CI's 2-vCPU runner; owner call whether the probe tolerates that latency or the arm window widens. Verify: croc-bite measures rather than UNMEASURED on a CI run, or the ticket records the ruling
next: [ ] ADR-0065 accept or reject — it narrows ADR-0040's membership list from five items to four so a per-game range is legal. Verify: its Status line reads accepted or rejected
next: [ ] re-land croc-bite once gh#122 is settled — the whole diff is recoverable from the reverted commit and its message carries the full causal chain. Verify: CI green at the re-land sha with Deploy success
next: [ ] six hand-copied seat-ceiling constants are ungated against their engines' real caps, and the write-back fix makes that pairing load-bearing for data loss. Verify: a gate reads a seat cap out of an engine, or the ticket records why it cannot
next: [ ] the eight owner items carried from last session, unchanged — gh#104 boxes 1 and 3, gh#220 box 3, gh#228, gh#229, gh#192 (b), gh#182, gh#205, gh#204, gh#214, gh#210, gh#184, gh#219, gh#101, the fortune family gh#97 to gh#103, gh#106, gh#109, gh#150. Verify: each records its ruling
next: [ ] owner physical blockers, unchanged: gh#9 domain has no A record and no NS, blocking gh#160 gh#29 gh#12 gh#19 · gh#141 real-phone walk
inflight: measured during this save — `git status --short` clean apart from this save's docs · `gh pr list --state open` empty · one worktree, the main tree, at `41b47c3` · no background task running · environment witness: **CI 34367311273 at `41b47c3`, Deploy to Azure Static Web Apps success** · NOT verified: the deployed pixels, gh#9 leaving no owner-facing URL · `~/.claude` still dirty from a rewrite this repo did not make, left untouched.
spent: queue 10→9 · batches 8
errors: diagnosed the CI red as a broken no-WebGL fallback from a plausible mechanism rather than a measurement, and that premise reached the owner's revert decision before a driver refuted it · claimed all party games declare `[2, 10]` from a grep that never resolved `MIN_PLAYERS`/`MAX_PLAYERS`, the third text-grep-answering-a-runtime-question error of the session and the one that reached a popup · offered the owner a name the ticket's own ruling had already declined, without carrying that rejection into the prompt · put two lift approaches in the fork brief that ADR-0060 had already closed as rejected · predicted a fourth freeze re-pin the CSS change never needed, an inference two parties repeated and neither measured · piped a 12-minute fleet run through `tail -60` and threw away the detail I needed one turn later.

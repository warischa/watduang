# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-09-18#4
done: `228b81e` `27e9380` `3f5e704` — What git cannot show: **gh#181's route-count question was already answered** — the 2026-09-05 ruling sits in the ticket's own comment `5553308361` and was read back this session; it had only ever been cited from an agent-written README · **the ruling's eight and `frame-screen-readings.json`'s eight route keys are the same set**, verified by reading both rather than counting · **`how-close-is-near` and `cursed-number` were never an unmeasured gap** — gh#203's composition instrument read them at 0.949 and 0.978 and the ruling EXCLUDES them for filling the frame, so the old "never measured at 1440×900" framing was true of one instrument only · **my own earlier draft priced the eight branch as needing artifacts for six more routes and was wrong** — all eight are already written up in `docs/verification/evidence/181/README.md` § "Readings"; caught and corrected BEFORE the popup, because a false cost would have biased the owner's decision · **ticket body-edit history IS reachable** via GraphQL `userContentEdits` though REST does not expose it — six edits, four of them after the ruling, every one keeping "four" · **`check-citations` matches only `§ heading` and `:line` citation forms**, so a bare path mention is ungated and was verified by a separate existence loop · two public ticket comments posted and read back (`5725344560`, `5725344793`), differing from intent by the one trailing newline GitHub adds · gates green: check-citations (calibrated red first by injecting a §-form dead path, which named the file and printed `1 dead citation(s)`), added-lineno-citation-check 0 new, thai-comments 310 files 0.
dec: gh#181 route count = eight, restated at all six body sites plus the title (owner 2026-09-18, popup) | the 2026-08-31 four-row table is RELABELLED as the original reading, never extended — the frame-screen instrument measures span, not fraction-of-window-width, so adding rows would have invented numbers | gh#204 box 2 = the generator-claim wording its 2026-09-10 ruling ordered but never got, body edited (owner 2026-09-18, popup) | a restated box never cites its own number — gh#181's numbering already shifted, so the 2026-09-05 comment's "Box 7" is today's box 8 | gh#219 has NO agent slice — boxes 2-4 recorded met per its 2026-09-15 comment, box 1 is gh#141's device queue | gh#204 has NO agent slice — boxes 7 and 9 are one real-phone blocker per its 2026-09-06 comment | gh#205's agent half is built — OG card, `arm-reveal-paths.test.mjs`, `drift-rules.test.mjs`, `canvas-frames-probe.mjs` and the manifest entry all on disk | no box ticked or unticked on any ticket.
next: [ ] gh#181 re-measure `dice-loser` and `timebomb` spans on a clean screen — 0.460 and 0.389 were read over an open reset dialog, per the rider in gh#181's 2026-09-05 comment `5553308361`; in scope, numbers not trustworthy. Verify: both spans re-read with nothing in the composition table's dialog annotation
next: [ ] gh#101 owner edits the draft in the tree — `docs/copy/nuea-khu.md` and `docs/copy/nuea-khu/cards.md`, both carrying `⚠ UNREVIEWED`; the agent half is done. Verify: neither file still carries the UNREVIEWED banner
next: [ ] gh#9 owner registers `watduang.com` — owner-only and financial, never an agent action; re-run whois immediately before paying, privacy on, auto-renew on; unblocks gh#160 gh#29 gh#19 items 2-4. Verify: `dig +short watduang.com A` resolves
next: [ ] gh#141 walk all 20 boxes on a real phone; gh#204 boxes 7-9, gh#205 boxes 1-2 and gh#219 box 1 need the same device and the same pass. Verify: every box noted with its device
next: [ ] gh#104 item 1 — the ดูดวง หมวด deck rewrite, 23 prompts and 5 predictions; owner-reserved Thai copy under the 2026-09-10 ruling, overlapping gh#97 gh#98 which already own เซียมซี's deck. Verify: the deck no longer promises a group
next: [ ] gh#109 is owner-only end to end — boxes 1-2 are Thai copy and boxes 3-5 are already satisfied per its 2026-08-28 comment; there is NO agent slice. Verify: nothing; do not brief it as mechanical work
next: [ ] gh#102 gh#103 เนื้อคู่ portraits — the ten cards exist at `docs/copy/nuea-khu/cards.md` but are UNREVIEWED, so the block is the owner's edit; read `docs/agents/assets.md` before spending. Verify: ten portraits put to the owner
next: [ ] gh#12 needs an owner Google Ads account, NOT the domain — its body says `Blocked by: —`. Verify: Keyword Planner volumes recorded under `docs/research/`, never `.scratch/`
next: [ ] owner supplies the path to `bin/ca` — re-checked 2026-09-18, `ca` is still not on `PATH`. Verify: `ca-selftest` runs green with the new rows
inflight: measured during this save — `git status --short` clean before it, `gh pr list --state open` empty, no worktrees, no background tasks; `origin/main` = `5ecd439` and HEAD was 14 ahead before this save, all docs commits staying local per ADR-0049, this save's included.
spent: queue 9→9 (gh#181's route count closed and gh#204 box 2 closed; the `dice-loser`/`timebomb` re-measure opened) · batches 1 · dispatches 0
errors: **three verification commands whose variable never reached the inner shell** — `bash -c '...$SP...'` single-quoted, so the paths were empty, and one printed a hardcoded "(0 each = clean)" line beneath failing greps · **word-split a pair string in zsh** with `set -- $pair`, which does not split unquoted parameters there, so a read-back loop diffed against a bare `.md` · **injected a must-red the gate could not see** — a bare path citation against a matcher that only reads `§ heading` and `:line` forms, so the first green proved nothing until the probe was rewritten.

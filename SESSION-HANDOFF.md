# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-09-18#8
done: `0963572` `e9673ff` `a780682` `88b484b` `87abb47` — the first src-adjacent batch in three sessions: `images/IMAGES.md` created (the registry `docs/agents/assets.md` names and gh#102 owed, which did not exist), ten portraits rendered and committed, gh#102 CLOSED, comments `5729040185` on gh#102 and `5729046521` on gh#181 posted and read back byte-identical apart from GitHub's trailing newline. What git cannot show: **`0963572` is the PREVIOUS session's save, which was written to disk and never committed** — RH found `SESSION-HANDOFF.md` and `docs/sessions-archive.md` dirty with a complete `#7` entry, so #7 was finished, not re-done · **gh#102's criterion 3 is undefined at its own proof case and the ticket was closed on it unverified** — it grades the portrait "at the size the canvas specifies" and there is no canvas: `docs/copy/nuea-khu.md` ships the portrait box empty and no เนื้อคู่ route exists under `src/pages/`; judged at 256px as a stand-in, which is an assumption, and the gap was stated to the owner BEFORE any credit was spent · **the anchor render caught two defects before nine more were spent** — the brand block asked for flat editorial vector and Images 2.5 returned glossy anime, and a self-authored `NO musical instrument` constraint had stripped the only occupation cue from a criterion that grades occupation; re-rendering the anchor was forced because re-approving it later invalidates the whole set · **haze was cleared with a level remap on the alpha channel, NOT the `-threshold` recipe in the assets doc** — on these files the 1-25 band (9.5k-16k px) was LARGER than the true anti-aliasing band (6.1k px), so thresholding would have zeroed the haze and binarised the edges into jaggies; the deviation is recorded in the registry · **the haze claim was first argued from mechanism plus a look at 256px, where edge and haze pixels are equally invisible — that look decided nothing**; dilating the solid mask 4px and counting band pixels outside it returns 0 on all ten, which is the check that could have gone red · **`api_call.model` is NOT a record of what ran** — it is a transport-B field the transport actually used ignores; Codex's built-in `image_gen` reports no model id, so the model is inferred, not readable · **Codex rewrote every prompt** with its own preamble before calling the tool and wrote `images/portrait-generation-prompts.json` into the repo unasked, which is now the as-sent record for criterion 5; it logs `IMG_01_002` twice because that render was retried · **gh#181 is mostly done-and-unticked, not seven boxes of open work** — box 7 CONFIRMED satisfied (all eight routes carry a 1440x900 verdict cited to the committed CDP log; all four named artifacts exist and are tracked; the standard doc exists), box 2 CONFIRMED satisfied by record (both cited files exist, both routes pinned in `KNOWN_OVERFLOW` by owner ruling), boxes 1/3/4/6 INFERRED to be in tension with the 2026-09-05 ruling and gh#203 and left as the owner's call, box 5 not verified · **gh#181's own ruling comment `5553308361` carries `cannon-flag` 0.132, `zero-trigger` 0.372, `short-stick` 0.511 — the committed readings file's values, not gh#203's closing comment's 0.333/0.622/0.989**; S2026-09-18#7 concluded the ruling used its own table, drawn from the gh#203 comment, and this is evidence on the other side.
dec: all ten portraits ACCEPTED and gh#102 closed (owner 2026-09-18, popup — ruling recorded here and in comment `5729040185`, not in the ticket body) | portraits started BEFORE sign-off on the `⚠ UNREVIEWED` copy, on the owner's stated premise that the ten cards' six visual field values are settled and the expected edit is tone and phrasing — **that premise IS the re-render trigger**: a `cards.md` edit moving gender, age, build, occupation, manner or mark invalidates that card's portrait, a wording edit does not | hair sameness across all ten, undifferentiated age, and wardrobe-only occupation on M4 F3 F4 F5 were put to the owner and accepted rather than fixed | no prompt names a nationality or any ethnic cue, per `docs/copy/nuea-khu.md` | the ten PNGs were held back from the first commit and committed only after the accept | gh#181 triage POSTED, no box ticked or unticked, nothing re-scoped.
next: [ ] gh#181 — boxes 1/3/4/6 need an owner ruling on whether the 2026-09-05 ruling and gh#203 void them; box 7 needs only a tick, box 5 is genuinely unchecked. Premise sourced from comment `5729046521` posted this session. Verify: those four boxes either ticked, struck, or carrying a comment saying they stand · Blocked · ask: do the two 2026-09-05 rulings void the redesign boxes (a) yes, strike them (b) no, they stand — agent executes the edit either way
next: [ ] gh#103 — the remaining thirty เนื้อคู่ cards and portraits; gh#102 closed this session so its blocker is gone, and the ten-portrait style is now proven and committed under `images/`. Verify: thirty cards written and thirty portraits put to the owner · Blocked · ask: reuse the accepted style as-is, or fix hair sameness and age spread first — the ten were accepted WITH those defects, so carrying them to forty is a choice, not a default
next: [ ] gh#160 — Cloudflare Web Analytics, chosen and not wired; its other blocker #159 is confirmed dead, so nothing sits behind gh#9. Verify: per ticket, once the domain resolves · Blocked · ask: unblocks when gh#9 lands — nothing to decide before then
next: [ ] gh#29 — ad slots need the domain AND an owner AdSense account, site review and publisher ID; two owner acts, not one, per its own body. Verify: publisher ID recorded and ad code live · Blocked · ask: unblocks only after gh#9 AND the owner's AdSense account — do not brief it as domain-only
next: [ ] gh#19 — month-6 organic-clicks gate; needs the domain, Google Search Console connected to it, and six months of traffic before the gate can read. Verify: organic clicks recorded at month 6 · Blocked · ask: nothing to decide until the domain and the traffic window both exist
next: [ ] H: gh#101 owner edits the draft in the tree — `docs/copy/nuea-khu.md` and `docs/copy/nuea-khu/cards.md`, both still carrying `⚠ UNREVIEWED`; the agent half is done. **A field-value edit now costs ten re-renders** per the re-render trigger in `dec:`. Verify: neither file still carries the UNREVIEWED banner
next: [ ] H: gh#9 owner registers `watduang.com` — owner-only and financial, never an agent action; re-run whois immediately before paying, privacy on, auto-renew on; it fully unblocks gh#160 only, and is one of two owner acts gh#29 needs. Verify: `dig +short watduang.com A` resolves — probed empty this session
next: [ ] H: gh#141 walk all 20 boxes on a real phone; gh#204 boxes 7-9, gh#205 boxes 1-2 and gh#219 box 1 need the same device and the same pass. Verify: every box noted with its device
next: [ ] H: gh#104 item 1 — the ดูดวง หมวด deck rewrite, 23 prompts and 5 predictions; owner-reserved Thai copy under the 2026-09-10 ruling, overlapping gh#97 gh#98 which already own เซียมซี's deck. Verify: the deck no longer promises a group
next: [ ] H: gh#109 is owner-only end to end — boxes 1-2 are Thai copy and boxes 3-5 are already satisfied per its 2026-08-28 comment; there is NO agent slice. Verify: nothing; do not brief it as mechanical work
next: [ ] H: gh#12 needs an owner Google Ads account, NOT the domain — its body says `Blocked by: —`. Verify: Keyword Planner volumes recorded under `docs/research/`, never `.scratch/`
inflight: measured during this save — `git status --short` clean apart from this save's own files, `gh pr list` open empty, no worktrees, no background tasks left running. `origin/main` = `5ecd439`, HEAD 28 ahead before this save, all docs commits staying local per ADR-0049; `87abb47` carries ten PNGs (8.4MB) and is the first non-text commit in that stack, so the next src push is heavier than usual. Nothing was written into `public/`. `~/.claude` untouched this session apart from memory writes and stays uncommitted there per the standing rule.
spent: queue 6→5 · batches 2 · dispatches 0 — counted as the last RH run's Execute Plan row count, non-`H:` only. That run rendered 6 rows: commit the stranded save, gh#181 triage, the portraits, and gh#160/gh#29/gh#19. Now 5 — three closed (the save, the triage, the portraits), and gh#181's box ruling plus gh#103 opened because closing gh#102 unblocked it.
errors: **ran a validator once and nearly trusted its first green** — the registry passed `--validate` on the first try and the calibration (illegal size, missing section) was an afterthought; both mutants did red, so the green was real, but the order was wrong · **built a contact sheet with `montage -flatten`, which merged the tiles into one image instead of compositing each**, and the first proof sheet showed one oversized portrait and nine blanks; caught by looking, not by an exit code, which is the assets-doc rule working · **asserted set membership from bare grep counts again** — `gender:1 build:1 manner:1` in `cards.md` were preamble hits and the cards are Thai; the same class the previous session logged, caught only because the counts were implausible, not because the rule was recalled.

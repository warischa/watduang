# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state


### S2026-09-17#1
done: `d1a7402` gh#238 behavioural emission proof · `a4c4b5c` ADR-0015 amendment + fog evidence into the repo · `bd20c5e` gh#181 320px readings · comments on gh#106 gh#160 gh#181 ×3 gh#204 gh#238. What git cannot show: **this repo never ticks DoD boxes** and I asked the owner to authorize ticking anyway — `docs/agents/issue-tracker.md` says so plainly and gh#219's 2026-09-15 comment says it again, both readable before the popup was written · **all 17 open issues carry `ready-for-human`, none carries `ready-for-agent`** — 11 of the 19 RH rows were owner-reserved or blocked behind one, so "all" could not mean execute-all · **gh#238 has now produced seven false greens**, four found this session by REFUTE; two of the last three were mine, in code written specifically to prevent them, and my own three-mutant calibration passed both times because every mutant I picked attacked the call and none attacked the defaults the new seam introduced · **gh#181 box 2's criterion predates the rule that replaced it** — both routes sit in KNOWN_OVERFLOW under owner rulings 2026-09-01 and 2026-09-15, and the binding requirement is the 2026-09-10 primary-control-above-the-fold rule, not fitting, so no 320px reading can close that box · **the evidence-image policy already exists in `.gitignore`** with its rationale and one carve-out that names CDP probes as never qualifying, so no ADR was written · **gh#106's counts had rotted 15→16** between its last comment and today.
dec: gh#106 = current state is option 3 de facto, ADR-0015 amended rather than left describing a predicate the tree no longer matches; gh#97 closed without removing the solo round | gh#181 box 2 = treat as not passed, measured, and now recommended for restatement against the 2026-09-10 rule or striking as box 1's threshold already was — owner's wording | PNG policy = verdict rows cite what a fresh clone can open, images are illustration; rule already existed, executed not re-decided | gh#238 direction = production seam, chosen at the fork, with the fork's correction that an injectable sink alone cannot drive `main` past its environment | the seam's env channel is refused when the module is the entry point, mirroring the rule `play-exit-probe.mjs` already applies to its own calibration knobs | REFUTE cap 2/2 spent on artifact A, so round-2 spot-fixes were applied and shipped without a third round | no REFUTE on the mockup fog fix — four-line block move, no commit, no deploy, and the silent-wrong budget was already paid by a positive control | writers stayed on agy except the non-git mockup tree, which went to codex because acceptance rule 5 has nothing to diff against there.
next: [ ] push 8 commits to main — asked with a true green-at-HEAD evidence line, not yet answered. Verify: `git rev-parse origin/main` equals local HEAD
next: [ ] close gh#238 once the push lands — owner answered close-after-push. Verify: `gh issue view 238` reports CLOSED
next: [ ] gh#181 box 2 — restate against the 2026-09-10 reachability rule or strike it; owner's wording, no further measurement helps. Verify: the ticket records the choice
next: [ ] gh#181 box 7 — reword so the per-route verdict cites the tracked readings rather than the excluded images; proposed on the ticket, not applied. Verify: the body no longer asks for screenshots
next: [ ] gh#9 owner registers `watduang.com` — re-run whois immediately before paying, privacy on, auto-renew on; unblocks gh#160 gh#29 gh#12 gh#19. Verify: `dig +short watduang.com A` resolves
next: [ ] gh#101 owner edits the เนื้อคู่ draft and answers two forks — the age band, and whether a pair meter has anything to measure. Verify: the ticket records both
next: [ ] gh#141 walk all 20 boxes on a real phone; gh#204 boxes 7 and 9 and gh#205 rows 1-2 need the same device. Verify: every box noted with its device
next: [ ] gh#102 gh#103 เนื้อคู่ portraits — blocked on the copy; read the assets doc before spending. Verify: ten portraits put to the owner
next: [ ] arm the owner's opus-4.6-on-agy fallback — add `agy:reasoner` and `agy:edge` rows to the role table in `bin/ca` and update `bin/ca-selftest`, which pins that table; Sol roles only, `adversary` is Astra and would go to copilot. Verify: `bin/ca-selftest` green with the new rows
inflight: measured during this save — `git status --short` clean before the save, `gh pr list --state open` empty, no worktrees, no background tasks · `origin/main` = `b4bf5df` with 8 local commits ahead, none pushed · full suite green at `bd20c5e`: exit 0, declared=48 executed=46 failed=0 not-runnable-here=2, browser leg 561s.
spent: queue 19→9 · batches 0 pushes · dispatches 9
errors: **put a box-tick approval to the owner on a premise the repo's own tracker doc already refuted** — asked before grepping, and the owner spent a decision on an act this repo does not perform · **calibrated a detector on three mutants that all attacked the same axis** — the call, never the defaults the new seam added, so two false greens survived my green and died to REFUTE in one line each · **rewrote a failing assertion in a way that re-imported its own blind spot** — filtered candidate lines on the viewport string the identity mutant sets every row to; caught by reading it, not by a test · **claimed a route was never measured** when only one capture set lacked it, corrected on the ticket in the same thread · **briefed two RH rows from the resume plan without reading the tickets' last comments** — gh#205's said phone-blocked, not done, and gh#238 carried the same human label as every other row.

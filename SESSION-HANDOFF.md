# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-09-23#4
done: `09eeb34` local, unpushed — gh#109 box 3 candidates, one sentence for all three tool pages. RH pick "Opt all B" → one popup, four owner answers (dec). What git cannot show: **gh#104 CLOSED**, evidence comment `5791691612` read back identical — owner ruled none of the 32 prediction strings count; `enumerate.mjs` re-run rc=0, calibration `overall: PASS`, prediction 32, directive 0, no `src/` commit since `71e7e43`; boxes 3-5 checked against the tree at `41ba416` (party: the one added line `listHeading: 'เกมในหมวดนี้'` equals the heading the template hardcoded; box 4 pages `/c/fortune/` `/game/siamsi/` `/game/daily-fortune/`, `love-match` unregistered; box 5 by two paths) · **gh#109 body edited twice, each read back identical** — a new "Added 2026-09-23" block names the stale `วิธีใช้` step 1 with DoD box 3, then its "no candidate" sentence re-pointed at `09eeb34` · **outside this repo:** `~/claude/skills/orchestrate-qwen` commit `7b927ac` (the `qw-read` CUT-RISK change + the `SKILL.md` exit-code line; `SKILL.md` 14,940B on both sides, net 0 measured) on branch `master`, not pushed.
dec: gh#104 = none of the 32 count (owner, popup; recorded on gh#104 `5791691612`) | `วิธีใช้` step 1 → folded into gh#109, no new ticket (owner, popup; recorded in gh#109's body) | orchestrate-qwen = commit local only; push and the `SKILL.md` cut not taken (owner, popup — recorded here, not on any ticket) | gh#9 portal binding = the owner does it, an agent verifies (owner, popup — recorded here, not on gh#9) | drain gate = draft gh#109 box 3, then SH (owner, popup) | still standing: blind-test bar 5 of 10 = distinguishable, P=0.050, proposed and the owner's to accept (gh#103 comment `5789453941`) · the 2026-09-16 draft-candidates ruling covers gh#109 (comment `5790034902`).
next: [ ] gh#109 — land the signed-off copy: box 2's tail in the wheel page's single-name note, with the `bundle-freeze-check` re-baseline in the same commit; box 1 in the five `design/` artboards, or delete the line under option (b); box 3 on the three tool pages' step 1. Verify: `รีเฟรชหน้านี้แล้วเลือกชื่อใหม่` nowhere in `src/`, `เลือกชื่อจากกลุ่มเดิม` nowhere under `src/pages/tool/` (it stays correct in `PlayerSetup.astro` and `timebomb.ts`), `ชื่ออยู่ที่เครื่องนี้ชั่วคราว` in no artboard · Blocked · ask: owner edit of `docs/copy/tool-name-entry.md` (`grep -c '⚠ UNREVIEWED'` reads 0 = signed off, measured 1 at this save)
next: [ ] gh#103 box 3 — score the owner's ten picks with `blind-103/make.py --score` (outside git, in `~/.claude/projects/-Users-waris-c-claude-free-game/`), commit `sheet.jpg` + `key.json` + the score under `docs/verification/evidence/` (read `docs/agents/assets.md` first — a binary); **`key.json` must not reach git before the picks, it publishes the answer**; post the result against the bar in comment `5789453941`. Verify: the score and its guessing probability quoted on gh#103 · Blocked · ask: owner's ten picks (a reviewer who has never seen the deck is the stronger test)
next: [ ] gh#101 — build the page from the signed-off drafts, per the ticket body's "What to build"; re-render any portrait whose card's gender, age, build, occupation, manner or mark the owner edit moved, with the command in `images/IMAGES.md`'s gh#103 section; **also carries gh#104's conditions for this game's own copy** — gh#104 closed while `love-match` was unregistered, so its `seo.*` was never evaluated and `love-match.ts` carries one `เกม` (grep 2026-09-23); `public/og/love-match.png` is the OG gate's named exemption and reds the gate on registration. Verify: the ticket's seven boxes, each checked the way its own wording says, and `docs/verification/evidence/104-2026-09-23/enumerate.mjs` re-run after registration with no new flag · Blocked · ask: owner edit — consumes H: gh#101
next: [ ] gh#103 boxes 4 and 6 — move the accepted portraits into the publish surface at the size the page loads, and state what one reader downloads; no route for this game exists under `src/pages/`, per gh#103's comment of 2026-09-23. Verify: each deck file referenced by a card on the built page, and the per-reader byte count in the closing comment · Blocked · ask: unblocks when the gh#101 page exists — nothing to decide before then
next: [ ] gh#9 close — after H: gh#9, verify and close with an evidence comment. Verify: `dig +short watduang.com A` resolves, the owner reports both SWA Custom domains rows `Ready`, `https://watduang.com` serves the site · Blocked · ask: unblocks when H: gh#9 lands — nothing to decide before then
next: [ ] gh#160 — Cloudflare Web Analytics, chosen and not wired; its other blocker #159 is confirmed dead. Verify: per ticket, once the domain resolves · Blocked · ask: unblocks when H: gh#9 lands — nothing to decide before then
next: [ ] gh#29 — ad slots need the domain AND an owner AdSense account, site review and publisher ID; two owner acts, not one, per its own body. Verify: publisher ID recorded and ad code live · Blocked · ask: unblocks only after gh#9 AND the owner's AdSense account — do not brief it as domain-only
next: [ ] gh#19 — month-6 organic-clicks gate; needs the domain, Search Console connected to it, and six months of traffic. Verify: organic clicks recorded at month 6 · Blocked · ask: nothing to decide until the domain and the traffic window both exist
next: [ ] orchestrate-qwen, outside this repo — 4 commits ahead of `origin/master`, `7b927ac` among them (rev-list, this save); `SKILL.md` 14,940B against the 12KB default, heaviest `## Dispatch` 6,118B (`check-budgets.sh`, this save) → move detail into its `reference/`. Verify: 0 ahead and the budget check no longer FAILs · Blocked · ask: the owner took commit-only 2026-09-23 — push (a), the cut (b), or leave (c)
next: [ ] H: gh#101 owner edits the four drafts in the tree — `docs/copy/nuea-khu.md`, `docs/copy/nuea-khu/cards.md`, `docs/copy/nuea-khu/cards-m6-m20.md`, `docs/copy/nuea-khu/cards-f6-f20.md`; review aid `review-101/cards.html` + `checks.md`, outside git beside `blind-103/` (comment `5790035652`): ten cards aged 42-54 and four non-Thai nationalities flagged, no duplicate, every card mapped to a portrait. Verify: `grep -c '⚠ UNREVIEWED'` reads 0 on all four, measured 1 each at this save
next: [ ] H: gh#9 owner registers `watduang.com` AND binds the apex and `www` in the SWA portal per comment `5789453647` (ruling 2026-09-23 recorded here, not on gh#9) — owner-only and financial, never an agent action; re-run whois immediately before paying (`No match` 2026-09-23), privacy on, auto-renew on, and check the registrar offers ALIAS/ANAME/flattening at the apex. Hand back: `dig +short watduang.com A` resolves and both Custom domains rows read `Ready`
next: [ ] H: gh#141 walk every box on a real phone with `docs/verification/gh141-real-device-script.md` — 20 boxes incl. gh#204 boxes 7 and 9 (one measurement, the frame rate), gh#205 box 1, gh#219 box 1 (comment `5790035284`); gh#204 and gh#205 carry NO agent slice per their 2026-09-21 triage comments, gh#219 per its 2026-09-15 comment — do not brief them as code work. Verify: every box noted with device and OS on its own ticket
next: [ ] H: gh#12 needs an owner Google Ads account, NOT the domain — its body says `Blocked by: —`. Verify: Keyword Planner volumes recorded under `docs/research/`, never `.scratch/`
inflight: measured during this save — `gh pr list --state open` empty; one worktree, the main checkout; no background tasks (pgrep: no headless Chrome, astro or vite); `origin/main` = `7566fe9`, HEAD 15 ahead plus this save, all riding the next src push per ADR-0049; tree dirty only with this save's files until its commit; `~/claude/skills/orchestrate-qwen` clean, 4 ahead of `origin/master`.
spent: queue 11→9 · batches 2 — 9 is the open non-`H:` count, enumerated: gh#109, gh#103 box 3, gh#101, gh#103 boxes 4 and 6, gh#9 close, gh#160, gh#29, gh#19, orchestrate-qwen. gh#104 closed; the `วิธีใช้` line folded into gh#109; gh#9's portal binding moved into H: gh#9, leaving the close as the agent row.
errors: **(1) numbers carried into artifacts before this turn measured them, three instances** — the popup's `14,940B` and the commit message's "net 0 bytes", both true when measured afterwards, and the handoff's `## Dispatch 6,191B`, which measured 6,118B; the same class as S2026-09-23#3's error (3), now recurring · **(2) a probe shaped for this repo run against another** — `origin/main...HEAD` in a repo whose branch is `master`, rc=128 in a batch that also committed; separated before reporting · **(3) zsh globbing swallowed an unquoted `--include=*.ts`** — one wasted call, the known gotcha of the "Verify in bash, not zsh" memory.

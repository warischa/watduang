# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state

### S2026-09-06#4
done: 2 pushes, both deployed. `b89f8c6..9270bb8` (3 commits, CI 34034995124) · `9270bb8..c2eca46` (4 commits, CI 34038158498, `Deploy to Azure Static Web Apps` = success read off the jobs API, not inferred). What git cannot show: **gh#181's rail number was measured wrong TWICE, the same way both times** — 312 from a sweep that only saw tier 1, then 356 from a re-sweep that reached tier 3 / round 11 but never saw `cycleCount` go two-digit (`advanceTurn` increments it, only `startNextRound` resets, so ~1 round in 7 renders two two-digit numbers, ~7px wider than anything captured); **380 adopted as the LARGEST tested-clean, not the smallest** — the rail sits in ~1060px and uses 728, so narrowness buys nothing while 7px short is the wrap the token exists to prevent · **the fortune `seo.description` "party-size carve-out" NEVER EXISTED** — false sentence originates in gh#192's own ruling comment, I copied it into two more files before REFUTE caught it; the standing exemption there is gh#201's about the word for "game" · **gh#217 audit found a SECOND, wider ceiling nobody was looking for** — `SINK_WRITE` needs a backtick immediately after the write, so markup composed away from the sink is never scanned at all (3 routes), and ADR-0061 records BOTH ceilings because one alone accepts less than it thinks · **gh#202's gate was passing vacuously and said so in its own output**; now live, `all 63 reachable Thai codepoint(s) present`, subset = whole Thai block (87) not today's 63 so it cannot rot on the next Thai word · **gh#218's 48px regressed short viewports** (1/10 → 10/10 at 320x420 measured against the route's own `worldToScreen`), 38px height-gated shipped instead · **`SKIP_EXPENSIVE` already existed** and I proposed building it — second time this run something shipped was offered as new.
dec: owner 2026-09-06, all recorded on their tickets — gh#217 accept the ceiling via ADR (declined the name-predicate: its exempt list is the hand-list the file's own header says rotted once) · gh#192 (b) 3 shipped pills, (c) keep ADR-0033 accents, (d) keep both label and panel order, (e) keep the SVG wheel · gh#213 id `croc-bite`, name จระเข้งับ (chosen over the mockup's เข้งับ on search: เข้ is colloquial), หมวด `party` · gh#202 face = Sarabun SIL OFL 1.1, ruled out Angsana/Cordia (MS EULA) and TH Sarabun New (DIP-SIPA, Fedora+Debian call it non-free) · gh#218 accepted loss signed off TWICE, the second time because my first scope was understated — the un-fixed set is ANY viewport under 568 tall, incl. small phones in a normal browser and essentially any phone in landscape · relax the gate rule → `SKIP_EXPENSIVE` documented in `CLAUDE.md`. Mine: backed the peer's 48px out of a green batch rather than ship a measured regression · took the peer branch's END STATE not its patch, since the wrong commit sits underneath · dropped the route count from `CLAUDE.md` entirely instead of correcting 11→13, because a number in prose has no gate.
next: [ ] **gh#202 overflow re-measure — the ticket's own criteria owe it now the font has landed.** New text metrics move every line. Verify: `pinocchio-luck` and `wire-snip-panic` horizontal-overflow rows re-read at 320x568 and 390x844 on both machines, recorded on gh#202
- [ ] gh#213 port จระเข้งับ as `croc-bite` — all four names given, lift is HAND-AUTHOR, brief ready at `NEXT-croc-bite-port.md` in project-state. ⚠ ADR-0060 admits `three` for it: 498,672 B tree-shaken + 68,850 B game code against a 482,127 B baseline, so the route roughly DOUBLES the reachable total. Verify: route dir exists and the porting recipe's gates pass
- [ ] gh#218 stays OPEN — below 568 tall the pill still covers the label. Verify: the ticket says whether the shell pill moves, which would revisit the precedent that a route moves its own elements
- [ ] gh#204 cannot close — criteria 7 and 9 need a real-phone frame rate; gh#141's บางกอกดริฟต์ row was amended 2026-09-06 to carry it. Verify: that walk is done and recorded
- [ ] gh#192 (b) NOT built — the popup framed it as a copy choice but `src/pages/index.astro` has ZERO list elements, so the strip does not exist, and 2 of the 3 chosen pills already sit in `hero-lead` verbatim. Verify: the ticket says whether the strip earns its place
- [ ] add per-step timing to `run-workflow-gates.sh` — "the suite is slow" has no numbers behind it today. Verify: the runner reports a per-step duration
- [ ] gh#210 chip cap (unblocks gh#184) · gh#219 the one-shot `+N` counter goes stale on a rotate · owner standing: gh#9 domain still NXDOMAIN blocking gh#160 gh#29 gh#12 gh#19 · fortune family gh#97-gh#104 · gh#112 gh#109 gh#106 gh#165 gh#150 gh#182
inflight: measured during this save — tree clean · unpushed 0 · `gh pr list --state open` not checked · CI 34038158498 SUCCESS with deploy confirmed · 1 peer session OPEN (`local_bb9ca0ca`) standing by on gh#218, its `3d2eab8`+`e0291e9` unpushed on `claude/adoring-proskuriakova-033165`, NOT archive-safe until someone decides the shell-pill question · owner worktree `hopeful-fermi-74ec5e` was taken over by that session but nothing was lost (`92b9f00` is an ancestor of main, its branch label still points at it, 0 stashes).
spent: queue 10→7 · batches 2 pushes, ~30 dispatches, 2 REFUTE rounds (both FAILED the diff, both correct), 1 fork, 4 driver runs, 3 full suite runs where 1 was needed
errors: proposed building `SKIP_EXPENSIVE` which already shipped, and had warned myself about that exact class earlier in the same run · copied a false carve-out sentence out of a ticket into two files without checking it · killed a suite and cleaned only 2 of 7 probe ports, so the next run died on a stale port and cost a third · let `git checkout <sha> -- <file>` stage a file silently, so gh#218's work committed under gh#202's message; caught only because I read each commit's exit code · told the owner the gh#218 loss was "in-app browsers" when it is any viewport under 568 tall including landscape, and had to re-ask for the sign-off · measured a regression against a geometry set missing the cannon, so my "never overlapped" was a completeness claim the probe had not earned.


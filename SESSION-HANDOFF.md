# Session handoff — วัดดวง

**This is the home of live state, not a supplement** — `CLAUDE.md` no longer has a § Current state; resume reads this file as the primary source.

Format · window · budget · roll: `.claude/commands/save-session.md` · Rationale for every decision lives in GitHub issues and `docs/adr/` — **never restate it here, cite the number** · map = [#1](https://github.com/warischa/watduang/issues/1) · archive: `docs/sessions-archive.md`

## Current state


### S2026-09-12#1
done: pushed `2838b8a..944352e` (one src commit, carrying the previous session's stacked docs save); CI run success incl. Deploy to Azure, 59 steps 58 success 1 skipped 0 failed. Filed #236 #237; closed gh#97 gh#98 gh#99 gh#213 with evidence comments. What git cannot show: **the RH queue I adopted was ≥⅓ dead and THREE instruments disagreed before the tree settled it** — a bulk-reader liveness sweep returned a self-contradictory STATUS column (gh#97 "boxes 7/7 unchecked · DEAD"), my own re-derivation from labels+boxes said ALIVE, and both were wrong because **DoD boxes are never ticked in this repo** (`docs/agents/issue-tracker.md`), so a box count is not a liveness signal at all and the artifact is the only instrument · **gh#97 gh#98 gh#99 were already BUILT** — four screens shipped, deck already 28 in exact proportions, pools already กิน/เงิน/ทาง with luck values; the remainder was verification, and gh#97's own comment claiming the shipped เซียมซี still renders วง copy was stale, that string lives in timebomb · **gh#214 is a recorded carve-out, not a gate hole** — width is reported-not-gated by the 2026-09-06 ruling, so the RH line briefing it would have narrowed an owner exemption · **gh#182 CANNOT close and my plan line saying otherwise was wrong** — the fit probe is green because it tolerates any RECORDED row, while box 1 refuses a reason that is merely `open`; 9 rows across 6 routes are recorded-not-blessed · **the font-coverage gate measures a file the browser never loads** — Resource Timing on the live route shows woff2 loaded and ttf never, and the gate prints that caveat itself while nothing consumes it (now #237) · **the positive control RAN and did NOT fire** — the pre-scrollIntoView probe restored from `bea5fbe^` still reads siamsi measurable here, so the handoff's "unmeasurable locally" does not reproduce, the fix is inert today, and the probe→verdict connector has never carried a real unmeasurable run on ANY machine (CI included, now recorded on #236) · **REFUTE round 1 found the fix bound fail and inconclusive but not the partition**, so a renamed state string would empty all three lists silently, and my own new test pinned a shape the probe cannot emit · **the worker refused my ordering instruction and was right** — placing that check before the inconclusive check would have swallowed the message naming the unmeasured game · CI ran both steps that cannot run locally (deployed-artifact probes, WebGL pixel readback) and both passed.
dec: verdict fix = fork approach (a) narrowest — bind the `inconclusive` list the probe ALREADY emits rather than re-deriving measurability or making the probe exit non-zero; control leg unchanged because its measurability term is already the identity it asserts; claim 2 stays ungated per its standing decision | `coordinateHitsMeasurable` flipped to the positive set because `inViewport` is written only by that probe, so the set converges — the short tap shape carried no field and counted measurable | gh#99 box 5 PASSES (owner 2026-09-12): partly visible does not violate per the gh#182 ruling, the page has no clipping context so the 8px is reachable not clipped per closed gh#195, and the first line is fully visible | owner authorized the push, all four closes, and both new tickets in one drain gate.
next: [ ] #236 open box — the inconclusive path is unexercised end-to-end on every machine, and CI now reads that route measurable too, so it is LESS likely to fire than before. Needs a deterministic way to force an unmeasurable walk; the obstacle is that the value is a local inside a function requiring a live CDP session. Verify: a run exercises the inconclusive path, or a seam exists that drives it without a browser
next: [ ] #237 font gate reads ttf while the browser loads woff2 — every past green is a claim about bytes readers never receive. Verify: the gate reads what the browser loads or proves the cmaps identical, calibrated with a codepoint present in one and absent from the other
next: [ ] gh#182 — 6 routes carry recorded-not-blessed overflow rows (freeze-tap, how-close-is-near, pinocchio-luck, short-stick, wire-snip-panic, zero-trigger, one-bomb); each needs an owner ruling or a fix, and pinocchio-luck's is the hard one per closed gh#195. Verify: every row reads a ruling rather than `open`
next: [ ] #232 #233 #234 #235 still untriaged, zero comments since filing. Verify: each triaged
next: [ ] gh#97's ruling says gh#101 waits on gh#102 gh#103 while the previous handoff said gh#102 gh#103 are blocked until gh#101 ships — one is wrong, owner picks. Verify: a comment records which
next: [ ] fortune family remainder gh#101 gh#104 gh#106 gh#109. Verify: each ships Thai copy and the owner reviews strings
next: [ ] owner physical blockers unchanged — gh#9 no A record no NS, blocking gh#160 gh#29 gh#12 gh#19. Verify: A record resolves
next: [ ] gh#141 real-phone walk owes gh#219 box 1, gh#205 box 6 rows 1-2, gh#204 box 7, gh#210 box 2, and croc-bite's 1976ms handled-pointer-gap row. Verify: each records a reading
inflight: measured during this save — `git status --short` shows only this save's files · `gh pr list --state open` empty, checked · `main` = `origin/main` at `944352e` before this docs commit, CI success, Deploy success · port 4178 free, one worktree (main), no background agents · NOT verified: whether the woff2 and ttf cmaps agree, whether a real phone feels croc-bite's first tap as laggy.
spent: queue 8→8 · batches 5
errors: adopted two agent returns over the handoff, which had already named the correct remainder · reported "24/24 probe legs" from a `grep | tail` that truncated the list, real count 28 · planned a live-run calibration a commit had already voided, caught only by the fork · briefed a check ordering that would have swallowed the message naming the unmeasured game, caught only by the worker refusing · wrote a plan line saying gh#182 closes on a clean probe run, conflating a gate green on recorded rows with a box demanding ruled ones.

::error::/game/freeze-tap/play/ at 390x844 no longer fits: 339px to scroll and 0px clipped away on a play screen (press 1). This row is pinned as fitting in FITS_ROWS from a recorded run, and the pin only ever moves toward MORE rows. Find what grew, then re-record with the reason — never to make a run pass.
::error::/game/freeze-tap/play/ at 1440x900 no longer fits: 238px to scroll and 0px clipped away on a play screen (press 1). This row is pinned as fitting in FITS_ROWS from a recorded run, and the pin only ever moves toward MORE rows. Find what grew, then re-record with the reason — never to make a run pass.

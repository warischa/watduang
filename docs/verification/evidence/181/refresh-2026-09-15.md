# gh#181 box 7 — refreshed 1440x900 captures and per-route verdicts (2026-09-15)

**Supersedes `README.md`'s captures and verdicts.** That file's PNGs were captured 2026-09-07 against
`7f654f7`; the eight in-scope routes' sources have since moved (see "What changed since the prior
pass" below), and the ticket's own directive is that a stale screenshot cannot be handed to the owner
for box 8. This file's captures and verdicts are the current record. `README.md`'s prose (frame
selectors, board/rail tokens, the disclosures) is not re-derived here where nothing invalidates it —
see below for exactly what was re-checked and what was carried forward.

## Stamp

Built and captured at commit `4f6027cbeaa7bc1fbb2984e1c3d6855c3196a824` (`git log -1 --format=%H`,
clean tree). `npm run build` (never `npx astro build`), served with `npx serve dist/ -l 4711`,
headless Chrome `--headless=new --use-gl=angle --use-angle=swiftshader --enable-unsafe-swiftshader`
(never `--disable-gpu` — required for `pinocchio-luck`'s WebGL context per
`docs/agents/browser-verification.md`) on `--remote-debugging-port=9711`. Driver:
`scripts/driver.mjs` running `capture-1440-screenshots.mjs` (committed beside this file). All eight
routes were captured in the same run, against the same build.

## What changed since the prior pass, and why it does not touch the box-7 verdict

`git log --since=2026-09-08 --name-only -- src/play/*/overrides.css src/play/*/play.css`, filtered to
the eight in-scope routes, returns exactly two files: `freeze-tap/overrides.css` (two commits made
today, gh#182) and `timebomb/play.css` (one commit, gh#165, seven days ago). Both were read in full:

- **`freeze-tap/overrides.css`**: both added blocks are `@media (max-width: 639px)` — this project's
  phone/tablet breakpoint, not the `min-width: 1100px` desktop one `desktop-sizing-decisions.md`
  scores against. They fix a below-the-fold primary control at 320x568 and state plainly they are
  scoped there on purpose (to avoid reaching the desktop `.btn-primary` raise). No rule in either
  block can apply at 1440px.
- **`timebomb/play.css`**: adds an absolutely-positioned mute toggle (`.tb-mute`, 44x44px, out of
  flow) to the top bar. It does not move, resize, or reflow `.tb-canvas` or any layout token.

The other six routes (`power-meter`, `zero-trigger`, `dice-loser`, `cannon-flag`, `pinocchio-luck`,
`short-stick`) have had **zero** changes to their layout stylesheets since the 2026-09-08 pass that
produced `frame-screen-readings.json`. Everything else that touched `src/play/<route>/` in between
(gh#227 sound control, gh#224 2D-ink leg, gh#232 arm-gate probe removal, a roster-group fix) is
logic/markup outside the two layout files.

## Which screen was captured per route, and how it was reached

Every load re-asserted `innerWidth === 1440 && innerHeight === 900` in-page before the screenshot; a
load that failed the assertion would have thrown (none did — see `capture-1440-screenshots.mjs`'s
`assertViewport`).

| route | screen captured | how reached |
|---|---|---|
| `dice-loser` | play/roll screen, `#dl-play` | explicit `#dl-begin` click (the control a player presses) — never the reset button, which opens a confirm dialog over the setup screen |
| `timebomb` | bomb-holding stage, `#tb-stage` / `.tb-canvas` | explicit `#tb-begin`, then a 700ms wait past the 400ms arm window (`src/games/_arm-gate.ts`), then `#tb-start` — same two controls a player presses |
| `power-meter` | attempt/tap-the-gauge screen (`.meter-arena`) | roster seeded (3 names), 1 press past the "pass the device" handoff card (`renderTurnIntroView`) — walk gated on `.meter-arena`, which only the real attempt screen renders |
| `zero-trigger` | board-and-rail play screen (`#screen-game`) | roster seeded, screen left "fresh" with 0 further presses needed |
| `cannon-flag` | gameplay/aim screen, `#screen-gameplay` | roster seeded, 1 press past the pass-device screen (`#screen-pass-device`) — walk gated on `#screen-gameplay` carrying the `.active` class, since all four screens exist in the DOM at once and only `.active` is shown |
| `pinocchio-luck` | question screen (`.answers`, 3D stage behind it) | roster seeded, 1 press past the "ready" handoff card (`renderPass`) — walk gated on `.answers`, which only `renderQuestion` renders |
| `short-stick` | draw screen (`.draw-layout`) | roster seeded; gh#184's roster-bridge auto-seeds/auto-presses this route on load, so the screen was already off the fresh signature at 0 presses (matches the 2026-09-08 pass's own recorded behaviour for this route) |
| `freeze-tap` | round screen, `#gameTargetSurface` | roster seeded, 2 presses through both transition screens ("รับทราบ! เริ่มส่งเครื่อง" then "พร้อมแล้ว") — walk gated on `#gameTargetSurface`, matching the 2026-09-08 pass's own two-press finding for this route |

**First attempt on this pass landed `power-meter`, `cannon-flag` and `pinocchio-luck` on their
pass-the-device handoff screens instead** (a generic "did the signature change from fresh" walk
stops as soon as ANY screen differs, and the handoff card already differs). Caught by opening each
PNG and reading it, corrected by adding a per-route gate (mirroring the one `freeze-tap` already
needed) that presses past the handoff to the real interactive screen. The table above reflects the
corrected, second run — the one whose PNGs are committed.

## `dice-loser` and `timebomb`: confirmed no reset dialog open

Both routes' own reset `<dialog>` (`#dl-reset-dialog`, `#tb-reset-dialog`) was present in the document
(`allDialogIds`) but **not open** (`openDialogIds: []`) at the moment each screenshot was taken —
reached via the named begin controls, never the reset button. Read from
`capture-1440-screenshots.mjs`'s own `DIALOG_CHECK` evaluated immediately before each screenshot.

## Per-route verdict against `docs/agents/desktop-sizing-decisions.md` (gh#181 box 7)

All eight: **reads-as-designed.** Reasoning, per route:

| route | verdict | basis |
|---|---|---|
| `cannon-flag` | reads-as-designed | full-bleed canvas course, HUD pinned top corners, controls span the bottom edge — layout stylesheet unchanged since the 2026-09-08 pass; fresh screenshot shows the same composition |
| `dice-loser` | reads-as-designed | `.dl-play` card capped and centred with generous margins either side, sticks graphic clear — stylesheet unchanged; fresh screenshot confirms |
| `freeze-tap` | reads-as-designed — carried-forward caveat unchanged | full-bleed tap surface; the two readouts pinned to opposite frame edges (`.game-hud-top`, `justify-content: space-between`) is the same disclosed observation as the 2026-09-08 pass, left to the owner's box-8 read since `desktop-sizing-decisions.md` reserves "whether it reads as designed" to that look. Today's two commits are 320px-scoped and do not touch this screen at 1440px |
| `pinocchio-luck` | reads-as-designed | `#stageFrame` capped at 780px, question card centred beneath it, both stylesheet and composition unchanged since 2026-09-08 |
| `power-meter` | reads-as-designed | `.glass-card`/`#view-root` capped and centred, gauge and attempt-dots all inside the cap; stylesheet unchanged |
| `short-stick` | reads-as-designed | `.draw-layout` card capped and centred; stylesheet unchanged |
| `timebomb` | reads-as-designed | `.tb-canvas` still centred with ~440px margins each side; the one change since 2026-09-08 (a 44x44px mute toggle, out of flow) does not move or resize the board |
| `zero-trigger` | reads-as-designed — carried-forward departure disclosed | board+rail grid departs the standard's rule 2 template (`justify-content: center` instead of `1fr`), verdict rests on rule 3 (uniform, centred, no non-uniform stretch) per the 2026-09-08 disclosure; stylesheet unchanged and fresh screenshot shows the same centred composition |

**Box 8 (the owner's own look) is not decided here** — these captures exist for that look.

## PNGs (gitignored, not committed)

All eight overwrite the stale 2026-09-07 files, same names, in this directory:
`cannon-flag-1440x900.png`, `dice-loser-1440x900.png`, `freeze-tap-1440x900.png`,
`pinocchio-luck-1440x900.png`, `power-meter-1440x900.png`, `short-stick-1440x900.png`,
`timebomb-1440x900.png`, `zero-trigger-1440x900.png`.

## What this did not cover

Same scope limits as the 2026-09-08 pass: no measurement at any viewport other than 1440x900, no
source edit, no fix applied, and box 8 (the owner's look) is untouched.

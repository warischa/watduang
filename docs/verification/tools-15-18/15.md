# Issue #15 — วงล้อสุ่ม `/tool/wheel/` — DoD verification

Method: `dist/` built by the coordinator (not rebuilt here) + `npx serve dist/ -l 4321` (shared,
read-only) + `node scripts/driver.mjs <script>` against a dedicated headless Chrome
(`--remote-debugging-port=9222`, `--user-data-dir=.../chrome-15`, this agent's alone). True 320px
reflow via `session.setWidth`, reduced motion via a separate Chrome relaunch with
`--force-prefers-reduced-motion` (both per `DRIVER-PLAYBOOK.md`). Evidence under
`../evidence/15/` (text artifacts only — see `docs/verification/README.md`).

**Boxes in / PROVEN / FAILED / UNPROVABLE / UNDECIDED: 10 / 8 / 0 / 2 / 0**

Rule applied throughout: a box asserting more than one thing is verdicted on its weakest
assertion — every clause must be individually observed before the whole box reads PROVEN.

---

## #15-01
text: เปิด `/tool/wheel/` บนเครื่องที่ไม่เคยเข้าเว็บ ใส่ชื่ออย่างน้อย 2 ชื่อ กดหมุน แล้วได้ผลเป็นหนึ่งชื่อที่อยู่ในรายการที่ใส่ไว้
verdict: PROVEN
observation: Fresh profile, `session.wipe()` on-origin (localStorage cleared on `localhost:4321`
before adding names — a wipe issued on `about:blank` does not touch the target origin, confirmed
the hard way on the first run, see note below), then added "เอ"/"บี" to the roster, ticked both,
pressed "ใส่ชื่อลงวงล้อ", pressed "หมุน". Result: `#wheel-result` → `"โดน: บี"`, and `#wheel-names`
lists exactly `["เอ", "บี"]` — the result is a member of the entered list.
evidence: `../evidence/15/01-wheel-first-spin-dom.txt` (screenshot captured during the walk, not
retained — see docs/verification/README.md)

## #15-02
text: มีรายการน้อยกว่า 2 กดหมุนไม่ได้ และบอกเหตุผลให้ผู้ใช้เห็น ไม่ใช่เงียบหรือคืนค่าว่าง
verdict: PROVEN
observation: Two assertions, both checked in one run. Added a single name ("เอ"), pressed "ใส่ชื่อลงวงล้อ".
(a) *cannot spin*: `#player-setup` stayed visible (`rootHidden: false`, the wheel never started) and
`#wheel-spin` stayed at its template default `disabled: true`. (b) *reason shown, not silent/empty*:
`#setup-error` became visible (`errorHidden: false`) with text `"ยังเริ่มไม่ได้ — เลือกไว้ 1 ชื่อ แต่ต้องมีอย่างน้อย 2 คน เลือกเพิ่มอีก 1 ชื่อ"`;
clicking the disabled spin button anyway left `#wheel-result` at `""` — no silent/empty result slipped through.
scope: refusal was observed only for the partial-tick case (1 name ticked). Pressing start with zero
names ticked instead takes the #22 numbered-count path — a generated `คนที่ N` set — and starts, per
`src/shell/PlayerSetup.astro:225-234`; that path was not exercised here.
evidence: `../evidence/15/02-wheel-below-min-dom.txt` (screenshot captured during the walk, not
retained — see docs/verification/README.md)

## #15-03
text: เปิดเครื่องมือหลังจากเคยเล่นเกมในเว็บนี้มาก่อน เห็นรายชื่อกลุ่มเดิมรออยู่โดยไม่ต้องกรอกใหม่
verdict: PROVEN
observation: Started a round on `/game/timebomb/` (a game page, not the tool) with "เอ"/"บี" ticked —
this saves the group via `saveGroup()`. Navigated straight to `/tool/wheel/` with no re-entry. Both
names appeared in `#roster-list`, both pre-ticked (`checked: true`), matching the group started on
the game page.
evidence: `../evidence/15/03-wheel-group-prefill-dom.txt` (screenshot captured during the walk, not
retained — see docs/verification/README.md)

## #15-04
text: รายชื่อที่กรอกในเครื่องมือยังอยู่หลังรีเฟรชหน้า
verdict: PROVEN
observation: Added "เอ"/"บี"/"ซี" to the roster on `/tool/wheel/`, then `session.nav()`'d to the same
URL again (the driver's documented refresh-and-resume equivalent — same tab, same origin, no wipe in
between). `#roster-list` still listed all three names after the reload.
caveat: the artifact alone cannot distinguish a real reload from a stale DOM — it is 68 bytes and holds
only the post-refresh roster, no discriminator that a reload actually occurred. The verdict holds by
transfer: `#18-07` calibrated `session.nav()` on the same driver with a `window.__probe` sentinel and
showed it really does reload — see `../evidence/18/07-refresh-resume.json`.
evidence: `../evidence/15/04-wheel-roster-refresh-dom.txt` (screenshot captured during the walk, not
retained — see docs/verification/README.md)

## #15-05
text: เปิดตัวเลือกเอาผู้ที่ออกแล้วออกจากวงล้อ แล้วหมุนต่อได้จนครบทุกคนในวงโดยไม่มีชื่อซ้ำ
verdict: PROVEN
observation: 3 names ("เอ"/"บี"/"ซี"), ticked "เอาคนที่ออกแล้วออกจากวงล้อ", spun 3 times. Results in
order: `โดน: เอ` → `โดน: บี` (note: `เหลือ ซี คนเดียวในวงล้อ…`) → `โดน: ซี (คนสุดท้าย)` (note:
`ออกครบทั้ง 3 คนแล้ว…`, `#wheel-spin` now disabled). All three names hit exactly once, no repeats,
spin proceeds until the group is exhausted.
evidence: `../evidence/15/05-wheel-eliminate-all-dom.txt` (screenshot captured during the walk, not
retained — see docs/verification/README.md)

## #15-06
text: หมุนซ้ำได้ทันทีโดยไม่ต้องตั้งค่าหรือกรอกรายชื่อใหม่
verdict: PROVEN
observation: 2 names, eliminate OFF, spun 3 times in a row with no setup between spins. Calibrated
against a dead-button false positive (a stale `"โดน: เอ"` repeated 3× would look identical to a
working re-spin) by reading state **mid-flight**, before the 1.2s reveal timer: each of the 3 clicks
showed the synchronous transition `resultText: ""`, `noteText: "กำลังหมุน…"` before settling back to
a result (`โดน: เอ`, `โดน: บี`, `โดน: บี`) with `#wheel-spin` re-enabled each time. Three real
transitions, not one stale value repeated.
evidence: `../evidence/15/06-wheel-respin-dom.txt` (screenshot captured during the walk, not
retained — see docs/verification/README.md)

## #15-07
text: หลังได้ผล มีทางกดต่อเข้าเกม และเมื่อถึงหน้าเกมแล้วรายชื่อวงเดิมยังอยู่ครบ ไม่ต้องพิมพ์ใหม่
verdict: PROVEN
observation: Two assertions. (a) *a way to continue into a game exists*: `.wheel-next a` lists
`/game/timebomb/` and `/game/siamsi/`. (b) *the same group arrives complete, no retyping*: followed
`/game/timebomb/` after starting the wheel round with "เอ"/"บี"/"ซี" — the game page's
`#roster-list` showed all three names, all pre-ticked (`checked: true`).
evidence: `../evidence/15/07-wheel-continue-into-game-dom.txt` (screenshot captured during the walk,
not retained — see docs/verification/README.md)

## #15-09
text: โฆษณาอยู่ใต้ตัวเครื่องมือ พื้นที่ถูกจองไว้ล่วงหน้าจนเนื้อหาไม่กระโดดตอนโฆษณาโหลด และไม่มีอะไรถูกแทรกเหนือเนื้อหาหลังโหลด
verdict: UNPROVABLE
Three assertions bundled; verdicted on the weakest.
- *ad below the tool* — observed and passing: `.ad-slot`'s bounding-rect top (1072.1px) is below
  `#wheel` section's bottom (1056.1px); DOM order also has `.ad-slot` after `#wheel`.
- *space reserved so content doesn't jump when the ad loads* — **UNPROVABLE locally**. `min-height:
  250px` is present (a computed-style read confirmed `250px`), which is the reservation mechanism,
  but "doesn't jump when the ad loads" is a claim about behaviour during a real ad load, and no ad
  script ships anywhere in `dist/tool/wheel/index.html` (`grep -o 'adsbygoogle\|googlesyndication\|pagead'`
  on the built file returned nothing). There is nothing here that loads to *not* jump against.
- *nothing inserted above content after load* — **UNPROVABLE locally** for the same reason: checking
  `document.body.firstElementChild === <main>` on a page where no ad script ever executes is an
  always-true probe (nothing runs that could insert anything), not evidence about what happens once
  a real ad script executes post-deploy.
owner: Google AdSense's actual ad script + a real deploy where it loads. Unblocking event: production
deploy with the AdSense snippet live, then a re-check with the real script executing.
evidence: `../evidence/15/09-wheel-ad-slot-dom.txt` (records the observed-and-passing half only —
position and reserved height, not load-time behaviour; screenshot captured during the walk, not
retained — see docs/verification/README.md).

## #15-11
text: ผลลัพธ์อ่านได้จากตัวหนังสือเสมอ ไม่ต้องอาศัยสีหรือการเคลื่อนไหวเพื่อรู้ว่าใครโดน
verdict: PROVEN
observation: Two assertions, both checked. (a) *not color-dependent*: `#wheel-result` is
`role="status" aria-live="polite"` and its `textContent` states the result in words
(`"โดน: บี"`); an eliminated name in `#wheel-names` carries the literal suffix `"(ออกแล้ว)"` in
addition to (not instead of) the `is-out` strikethrough/muted-color styling, so the text alone
conveys status without needing the color/strikethrough. (b) *not motion-dependent*: relaunched this
agent's own Chrome (same port 9222, same profile dir) with `--force-prefers-reduced-motion`,
confirmed `matchMedia('(prefers-reduced-motion: reduce)').matches === true` on `/tool/wheel/`, then
spun — the result appeared immediately as text (`"โดน: บี"`) with `#wheel-disc` never gaining the
`is-spinning` class (the code's `reduceMotion.matches` branch calls `reveal()` synchronously, no
1.2s wait), i.e. the same textual result is legible with the animation entirely absent.
evidence: `../evidence/15/11-wheel-result-text-dom.txt` (animated run; screenshot captured during
the walk, not retained — see docs/verification/README.md), `../evidence/15/11-wheel-reduced-motion-dom.txt`
(reduced-motion run; screenshot captured during the walk, not retained — see docs/verification/README.md)

## #15-14
text: เทสถูกพิสูจน์แบบสองทาง — ทำให้ตกก่อนแล้วยืนยันว่าตกด้วยเหตุผลที่ถูก แล้วค่อยทำให้ผ่าน
verdict: UNPROVABLE
owner: the immutable git history of `src/tools/wheel.test.mjs` and `src/tools/wheel.ts` — this box
asserts a red-green sequence actually happened during development, which only past commits can
attest to and this session cannot fabricate or re-derive from the current working tree. A sibling
agent is separately proving the underlying test-non-vacuity invariant for this batch of issues;
this box should be read together with that result rather than re-litigated per-issue.
unblocking event: the sibling agent's git-history proof (or an equivalent log of the original
red→green run) becoming available.
evidence: none applicable — no local observation can settle this box.

> **Annotated 2026-08-15:** this `UNPROVABLE` verdict was reached against the box text as it read before the rewording. The reworded box asserts a re-checkable artifact property instead of git history: revert the guarding clamp/guard and the test must fail, restore it and the test must pass. `docs/verification/red-green-non-vacuity.md:36-50` found this MIXED for wheel — the `MIN_NAMES` guard revert was NON-VACUOUS, but the `Math.min(names.length - 1, …)` clamp revert was VACUOUS (no test in the suite injects the exact `1` the clamp guards against), and flagged this as a cross-cutting gap shared with `draw.ts`/`number.ts`. `docs/verification/dod-closeout-15-18.md:61` records a subsequent revert of that same clamp in `wheel.ts` where a new test failed and then passed on restore — the cross-cutting gap comes out **closed** for wheel (and, per rows 62-63 of the same table, for draw and number too). The verdict line above is left as written — it records the walk.

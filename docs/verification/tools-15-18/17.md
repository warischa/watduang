# Issue #17 — แบ่งทีม `/tool/team/` — DoD verification

Boxes in: 11 · PROVEN: 8 · FAILED: 0 · UNPROVABLE: 2 · UNDECIDED: 1

**Weakest-assertion rule applied** (per coordinator's mid-flight clarification): every box asserting
more than one thing is verdicted per-assertion below, and the box's overall verdict is the weakest of
its assertions — PROVEN only if every assertion in it is PROVEN. This changed two boxes from the
initial pass: `#17-11` (ad placement/reservation PROVEN, but the "nothing injected above after load"
half has no local proof set → box drops to UNPROVABLE) and `#17-12` (text-readability PROVEN, but the
reduced-motion half is N/A — team.astro ships zero animation — which per the playbook is "N/A, not
pass," so the box cannot be called PROVEN → UNDECIDED). All other 9 boxes bundle assertions that were
each independently observed to be true in this run, so their overall verdict is unchanged at PROVEN
(8 of them; #17-01's two assertions — nobody missing, nobody duplicated — were both directly observed
in the same DOM read, not inferred from one covering the other).

Method: `npm run build` output (`dist/`, pre-built, not rebuilt by this run) served at
`http://localhost:4321`, driven via `node scripts/driver.mjs` against a dedicated headless Chrome
(`--remote-debugging-port=9224`, `--user-data-dir=.../scratchpad/chrome-17`). Source read for context:
`src/tools/team.ts`, `src/tools/team.test.mjs`, `src/pages/tool/team.astro`, `src/shell/PlayerSetup.astro`,
`src/shell/roster.ts`. Evidence lives under `../evidence/17/` (text artifacts only — see
`docs/verification/README.md`):
- `main-run.json` — first-pass combined output (boxes 01/03/04/05/06/07/08/09/11/12, first-direction only)
- `followup-run.json` — second-pass output added after the weakest-assertion rule: `box08_editPropagation`
  (the direction `main-run.json` was missing), `box09_linkAffordance` (the UI-path half), `box03_evenSplitCalibration`
  (even-split control, zero extra-tags)
- `box12-reduced-motion.json` — reduced-motion `matchMedia` calibration (flag on vs off)
- `01-03-team-split-result.png` — screenshot of an uneven (7→3) split result, captured during the
  walk, not retained — see `docs/verification/README.md`

## #17-01
text: ใส่รายชื่ออย่างน้อย 2 ชื่อ กำหนดจำนวนทีม กดแบ่ง แล้วทุกคนถูกจัดลงทีมครบ ไม่มีใครหาย และไม่มีใครอยู่สองทีม
verdict: PROVEN
assertions:
- "nobody missing" — PROVEN. `main-run.json` → `box01_verify.noneMissing: true` (sorted flattened
  result equals sorted input, 7-of-7).
- "nobody in two teams" — PROVEN. `main-run.json` → `box01_verify.noDuplicates: true`
  (`new Set(flat).size === flat.length`, 7 === 7).
observation: Seeded roster with 7 names, ticked all, pressed "ใส่ชื่อลงสนาม", set team count to 3,
pressed "แบ่งทีม" — both properties read from the one resulting DOM snapshot.
evidence: `../evidence/17/main-run.json` → key `box01_verify` (fields `noneMissing`,
`noDuplicates`); raw teams at `box01_box03_firstSplit.teams`; screenshot `01-03-team-split-result.png`
captured during the walk, not retained — see docs/verification/README.md.

## #17-03
text: เห็นได้ว่าทีมไหนเป็นทีมที่ได้คนเกินมา
verdict: PROVEN
assertions: single assertion — PROVEN both ways (calibrated).
observation: Uneven split (7 into 3 → sizes 3/2/2): exactly the one size-3 team's heading carries
"(ได้คนเกิน)" (`extraTagCount: 1`). Calibration control — even split (6 into 3 → sizes 2/2/2): zero
headings carry the tag (`extraTagCount: 0`), confirming the marker is not always-on.
evidence: `../evidence/17/main-run.json` → key `box03_verify` (uneven case) and
`box01_box03_firstSplit.headings`; `../evidence/17/followup-run.json` → key
`box03_evenSplitCalibration` (even case, the "must be false/absent at some point" control).

## #17-04
text: เปลี่ยนจำนวนทีมแล้วแบ่งใหม่ได้ทันทีโดยไม่ต้องกรอกรายชื่อใหม่
verdict: PROVEN
assertions: single assertion — PROVEN.
observation: After an existing split (`hasResult === true`), changed `#team-count` from 3 to 2 and
fired its native `change` event (no re-entry of names). Result immediately re-rendered to 2 teams
covering all 7 names.
evidence: `../evidence/17/main-run.json` → key `box04_verify`
(`teamCount: 2, flatLen: 7`).

## #17-05
text: แบ่งซ้ำด้วยค่าเดิมได้ผลการแบ่งชุดใหม่ ไม่ใช่ชุดเดิมค้าง
verdict: PROVEN
assertions: single assertion — PROVEN.
observation: Clicked "แบ่งทีม" twice with team count unchanged (3); the two resulting team arrays
differ (`identical: false`).
evidence: `../evidence/17/main-run.json` → key `box05_verify` (fields `split1`, `split2`,
`identical`).

## #17-06
text: กำหนดจำนวนทีมมากกว่าจำนวนคนไม่ได้ และบอกเหตุผลให้ผู้ใช้เห็น
verdict: PROVEN
assertions:
- "cannot request more teams than people" — PROVEN. `#team-split` disabled at count=99, players=7.
- "reason shown" — PROVEN. Note text reads "ขอ 99 ทีมไม่ได้ เพราะมีคนแค่ 7 คน".
evidence: `../evidence/17/main-run.json` → key `box06_verify` (fields `disabled`,
`noteText`).

## #17-07
text: มีรายการน้อยกว่า 2 กดแบ่งไม่ได้ และบอกเหตุผล
verdict: PROVEN
assertions:
- "cannot split under 2 names" — PROVEN. Fresh wiped state, `players.length === 0`, `#team-split`
  disabled.
- "reason shown" — PROVEN. Note reads "ยังแบ่งไม่ได้ — เลือกหรือเพิ่มชื่ออย่างน้อย 2 ชื่อด้านบน แล้วกด
  \"ใส่ชื่อลงสนาม\"" on load, before any interaction.
scope: this run only observed the load-time state (0 names ticked, before any press). It did not press
start with zero names ticked — that press instead takes the #22 numbered-count path (a generated
`คนที่ N` set) and starts, per `src/shell/PlayerSetup.astro:225-234`; not tested here.
evidence: `../evidence/17/main-run.json` → key `box07_freshState` (fields `disabled`,
`noteText`).

## #17-08
text: เปิดหน้ามาแล้วเห็นรายชื่อกลุ่มเดิมรออยู่ และรายชื่อที่แก้ที่นี่มีผลกับเครื่องมือและเกมตัวอื่นด้วย
verdict: PROVEN
assertions (two directions, both required, both separately observed — not one inferred from the other):
- "opening the page shows the same group waiting" — PROVEN. Seeded `watduang:roster` externally,
  reloaded `/tool/team/` cold, all 7 names appeared as roster checkboxes with no other setup.
- "edits made here affect other tools and games too" — PROVEN (this half was NOT in the first pass;
  the first pass only tested the reverse direction and inferred this one from reading `roster.ts` — a
  source-read is not a PROVEN observation, so a live check was added). Typed a new name into
  `#roster-add-name` and clicked `#roster-add-btn` on `/tool/team/`, then navigated to `/tool/draw/`
  in the same tab: the added name appeared in its `#roster-list` (`nameFlowedAcross: true`).
evidence: `../evidence/17/main-run.json` → key `box08_rosterAfterReload` (first direction);
`../evidence/17/followup-run.json` → key `box08_editPropagation` (fields
`addedOnTeamPage`, `seenOnDrawPage`, `nameFlowedAcross` — second direction).

## #17-09
text: มีทางกดต่อเข้าเกมโดยรายชื่อวงไหลต่อไปครบ
verdict: PROVEN
assertions:
- "a pressable path into a game exists" — PROVEN (NOT in the first pass; the first pass navigated by
  URL directly, which never confirmed the UI affordance itself). `.team-next a` elements exist with
  `href` values `/game/timebomb/` and `/game/siamsi/` — real links on the rendered page, not an
  inferred route.
- "the group flows forward completely" — PROVEN. Ticked all 7 roster names, pressed "ใส่ชื่อลงสนาม"
  (which calls `saveGroup([...selected])`), navigated to `/game/timebomb/`: all 7 names were
  pre-ticked on that page's own roster list.
evidence: `../evidence/17/followup-run.json` → key `box09_linkAffordance` (field `hrefs`,
the path-exists half); `../evidence/17/main-run.json` → key `box09_verify` (field
`checkedOnGamePage`, the flow-completely half).

## #17-11
text: โฆษณาอยู่ใต้ตัวเครื่องมือ พื้นที่ถูกจองไว้ล่วงหน้า และไม่มีอะไรแทรกเหนือเนื้อหาหลังโหลด
verdict: UNPROVABLE
assertions:
- "ad sits below the tool" (placement) — PROVEN. `<main>`'s child order is `h1, p, #player-setup,
  #clear-group, #clear-choice, script, #team (tool section), .ad-slot, #how-to-use` —
  `compareDocumentPosition` confirms `.ad-slot` follows `#team` (`adIsAfterTeamSection: true`).
- "space reserved in advance" (reservation) — PROVEN. Computed `min-height: 250px` on `.ad-slot`,
  present in the static markup before any ad script would run.
- "nothing injected above content after load" (injection-safety once a real ad loads) — UNPROVABLE.
  `dist/tool/team/index.html` contains zero occurrences of `adsbygoogle` or `pagead` — there is no
  AdSense script in the local build at all, so there is nothing whose post-load injection behaviour
  could be observed. Owner: Google/AdSense, plus a production deploy that does not exist yet.
  Unblocking event: a live deploy carrying the real AdSense script, checked in the browser against the
  actual ad network.
overall: weakest of the three assertions is UNPROVABLE, so the box is UNPROVABLE overall, even though
placement and reservation both pass locally.
evidence: `../evidence/17/main-run.json` → key `box11_verify` (fields `children`,
`adMinHeight`, `adIsAfterTeamSection` — the two PROVEN halves); `dist/tool/team/index.html` grepped
live for `adsbygoogle|pagead` → zero matches (the UNPROVABLE half's concrete basis, not itself written
to a file since it is a negative grep result, reproducible with
`grep -o 'adsbygoogle\|pagead' dist/tool/team/index.html`).

## #17-12
text: เคารพการตั้งค่าลดการเคลื่อนไหว และผลการแบ่งอ่านได้จากตัวหนังสือเสมอ
verdict: UNDECIDED
assertions:
- "result is always readable as text" — PROVEN. The split result's only status cue is the literal
  string "(ได้คนเกิน)" appended via `textContent`, not a CSS color/class toggle
  (`box12_verify.sampleHeadingText`).
- "respects the reduced-motion setting" — N/A, and per the playbook "N/A, not pass": grepping the
  actual built artifact `dist/tool/team/index.html` (inline `<style>` block included, this page ships
  no separate CSS file) for `transition|animation|keyframes|reduced-motion` returns zero matches. The
  grep is calibrated (not a broken probe): the identical pattern against `dist/tool/wheel/index.html`
  correctly finds `@keyframes wheel-spin`, `animation:wheel-spin...`, and a
  `@media(prefers-reduced-motion:reduce){...animation:none}` guard — so the zero-match result on
  team.astro is a real "no motion exists here," not a grep miss. `matchMedia
  ('(prefers-reduced-motion: reduce)')` was independently calibrated both ways on this Chrome/driver
  stack (`false` unflagged, `true` with `--force-prefers-reduced-motion`), confirming the media query
  itself is wired correctly at the platform level — but there is no in-page consumer of it to verdict,
  so this assertion cannot be called PROVEN or FAILED. It also has no external owner or unblocking
  event (nothing is blocking observation — there is simply nothing to observe), so it does not fit
  UNPROVABLE either; it is UNDECIDED by the brief's own definition (cannot observe, cannot name whose
  proof set it belongs to).
overall: weakest of the two assertions is UNDECIDED (not PROVEN, not a violation), so the box is
UNDECIDED overall.
evidence: `../evidence/17/main-run.json` → key `box12_verify` (field
`sampleHeadingText`, the PROVEN half); `../evidence/17/box12-reduced-motion.json`
(`{"reducedMotionMatchesWithFlag": true}`, vs `box12_verify.reducedMotionMatches: false` unflagged —
media-query calibration); live grep `grep -o 'transition\|animation\|keyframes\|reduced-motion'
dist/tool/team/index.html` (zero matches) vs the same grep against `dist/tool/wheel/index.html`
(non-zero — the calibration control) — both reproducible, not written to a file.

## #17-14
text: ตรรกะการแบ่งรับตัวสุ่มเข้าไปได้ มีเทสที่ยืนยันว่าไม่มีใครหายและขนาดทีมต่างกันไม่เกินหนึ่ง และถูกพิสูจน์แบบสองทาง
verdict: UNPROVABLE
assertions:
- "logic accepts injected randomness" — PROVEN. `splitTeams(names, teamCount, random)` takes an
  injectable `random: () => number = Math.random` parameter (`team.ts` line 13).
- "tests confirm nobody missing and max size-diff ≤ 1" — PROVEN, and non-vacuously so. Per
  `docs/verification/red-green-non-vacuity.md` (a sibling agent's revert-and-observe-red run, not this
  session's green-only run): `#17-14 | หารไม่ลงตัว: ขนาดทีมต่างกันไม่เกิน 1 คน | NON-VACUOUS` and
  `#17-14 | ทุกคนต้องอยู่ในผลลัพธ์ครบพอดี ไม่หาย ไม่ซ้ำ | NON-VACUOUS` — both tests were shown to fail
  when the corresponding fix was reverted, not just to pass at rest. (This session independently
  re-ran `node --test src/tools/team.test.mjs`, 9/9 green — that confirms the tests currently pass, but
  a green run alone never shows non-vacuity, so it is cited here only as "still green," not as evidence
  for this assertion; the non-vacuity claim itself rests on the sibling's revert-based document.)
- "proven two-way" (red-then-green history of how the tests were originally written) — UNPROVABLE.
  This is distinct from the non-vacuity check above: non-vacuity asks "does reverting the fix make the
  test fail today," which was answered; "พิสูจน์แบบสองทาง" as a DoD box is about the git history of the
  test's original authorship (was it red before it was made to pass), which no local artifact or
  revert-and-observe run can reconstruct. Owner: the immutable past (commit history for
  `team.test.mjs`). Unblocking event: a site-owner-visible git-log/PR-review artifact showing the
  original red state, which this run has no access to and is out of scope to produce.
overall: weakest of the three is UNPROVABLE, so the box is UNPROVABLE overall.
evidence: `src/tools/team.ts` line 13 (injectable `random` param, read directly);
`docs/verification/red-green-non-vacuity.md` lines 111-112 (the two NON-VACUOUS rows for `#17-14`);
local `node --test src/tools/team.test.mjs` run this session (9/9 pass, terminal output only, cited as
context, not as proof of non-vacuity or history).

> **Annotated 2026-08-15:** this `UNPROVABLE` verdict was reached against the box text as it read before the rewording. The reworded box asserts a re-checkable artifact property instead of git history: revert the guarding hunk and the test must fail, restore it and the test must pass. `docs/verification/red-green-non-vacuity.md:75-90` already found both named properties (size balance, nobody missing) NON-VACUOUS for team. Beyond that, `docs/verification/dod-closeout-15-18.md:64,66-69` records a third, previously untested hunk — team's own clamp shape, `Math.min(i, …)` in the Fisher-Yates loop at `team.ts:28` — being reverted, which the *first* pass had missed entirely (team's suite stayed fully green on that revert because the clamp had no test at all); a new test now fails 1/10 on revert and passes 10/10 on restore, closing that gap too. The verdict line above is left as written — it records the walk.

{
  "name": "watduang",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "dev": "astro dev",
    "prebuild": "node scripts/validate-games.mjs",
    "build": "astro build",
    "preview": "astro preview",
    "test": "node --test 'src/**/*.test.mjs'",
    "validate": "node scripts/validate-games.mjs",
    "check-citations": "node scripts/check-citations.mjs",
    "thai-comments": "node scripts/thai-comments.mjs",
    "dangling-css-var-check": "node scripts/dangling-css-var-check.mjs",
    "public-orphan-check": "node scripts/public-orphan-check.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.0.0",
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^5.0.0"
  }
}
